import requests
import time
import json
import os
import re
from enum import Enum
from datetime import datetime, timedelta
import traceback
import calendar

TEXTS = {
    'ru': {
        # Общие команды
        'welcome': "👋 *Здравствуйте, {name}!* Добро пожаловать в бот по аренде автомобилей в Худжанде!",
        'guest': "Гость",
        'choose_your_role': "Чтобы начать, пожалуйста, выберите вашу роль:",
        'start_btn_client': "👤 Я Водитель",
        'start_btn_owner': "🚗 Я Собственник",
        'go_to_main_menu': "🏠 Главное меню", 
        'role_selection_made': "✅ *Выбрана роль: {role_name}*",
        'role_client': "Клиент",
        'role_owner': "Собственник",
        'help_intro': "ℹ️ *Руководство по использованию бота*",
        'register_cmd': "*/register* - Регистрация в качестве клиента для аренды авто.",
        'owner_register_cmd': "*/owner_register* - Регистрация как собственник для сдачи авто в такси.",
        'rent_cmd': "*/rent* - Выбрать автомобиль для аренды.",
        'listcars_cmd': "*/listcars* - Посмотреть список автомобилей и цены.",
        'myrentals_cmd': "*/myrentals* - Посмотреть статус своих заявок на аренду.",
        'profile_cmd': "*/profile* - Посмотреть свой профиль.",
        'edit_profile_cmd': "*/editprofile* - Изменить данные своего профиля.",
        'message_cmd': "*/message <текст>* - Отправить сообщение администратору.",
        'contact_cmd': "*/contact* - Показать контактную информацию.",
        'faq_cmd': "*/faq* - Ответы на часто задаваемые вопросы.",
        'cancel_cmd': "*/cancel* - Отменить текущую операцию.",
        'lang_cmd': "*/language* - Изменить язык.",
        'admin_help_cmd': "\n\n*Команды администратора:* /adminhelp",
        'owner_help_intro': "\n\n*Команды собственника:*",
        'owner_payouts_cmd': "*/payouts* - Посмотреть начисления по вашему авто.",
        'owner_drivers_cmd': "*/drivers* - Узнать, кто сейчас на вашей машине.",
        'owner_service_cmd': "*/service* - Подать заявку на техобслуживание.",
        'owner_calendar_cmd': "*/calendar* - Посмотреть график использования машины.",
        'owner_contract_cmd': "*/contract* - Получить копию договора.",
        'unknown_command': "🤷 Неизвестная команда. Используйте /help для просмотра списка команд.",
        'no_active_operation': "🤷 Нет активной операции для отмена.",
        'choose_language': "🇷🇺 Выберите язык / 🇹🇯 Забонро интихоб кунед:",
        'language_set': "✅ Язык установлен на *Русский*.",
        'must_be_owner': "❗️Эта команда доступна только для зарегистрированных собственников автомобилей.",

        # Тексты для кнопок главного меню
        'menu_rent': "🚙 Арендовать автомобиль",
        'menu_list_cars': "📋 Список автомобилей",
        'menu_my_rentals': "📄 Мои заявки",
        'menu_profile': "👤 Мой профиль",
        'menu_contact': "📞 Контакты",
        'menu_help': "ℹ️ Помощь",
        'menu_search': "🔍 Поиск", 
        'menu_register_client': "✍️ Регистрация Водителя",   
        'menu_register_owner': "✍️ Регистрация Собственника", 
        'menu_chat_manager': "💬 Чат с менеджером",
        'menu_language': "🌐 Сменить язык",

        'enter_chat_mode': "💬 Вы вошли в режим чата с менеджером. Все ваши дальнейшие сообщения будут пересланы. \n\nДля выхода, нажмите кнопку ниже или введите команду /endchat.", 
        'exit_chat_mode': "💬 Вы вышли из режима чата.", 
        'end_chat_button': "❌ Завершить чат", 
        'admin_btn_reply': "✍️ Ответить",
        'admin_prompt_reply': "✍️ Введите ваш ответ для пользователя {user_name} (ID: {user_id}).\n\n_(Для отмены используйте /cancel)_",
        'admin_reply_sent_successfully': "✅ Ваш ответ успешно отправлен пользователю {user_name}.",
        'admin_enter_chat_session': "💬 *Вы вошли в режим чата с пользователем {user_name}.*\n\nВсе ваши сообщения теперь будут пересылаться ему. Чтобы завершить чат, нажмите кнопку ниже или введите /cancel.",
        'admin_exit_chat_session': "💬 Вы завершили чат с пользователем {user_name}.",
        'admin_end_chat_button': "❌ Завершить чат с {user_name}",
        'admin_user_ended_chat': "🔴 Пользователь {user_name} (ID: {user_id}) завершил чат.",

        # Слова для клавиатур
        'day': 'День',
        'week': 'Неделя',
        'month': 'Месяц',
        'yes': 'Да',
        'no': 'Нет',
        'ice_car': 'ДВС',
        'electric_car': 'Электромобиль',

        # Регистрация клиента
        'already_registered': "✅ Вы уже зарегистрированы и ваш профиль активен. Для аренды используйте /rent.",
        'registration_pending': "⏳ Ваша заявка на регистрацию уже отправлена и находится на рассмотрении. Пожалуйста, ожидайте одобрения от администратора.",
        'registration_approved': "✅ Ваша регистрация была одобрена! Теперь вы можете пользоваться всеми функциями бота.",
        'registration_rejected': "❌ К сожалению, ваша заявка на регистрацию была отклонена. Для уточнения деталей свяжитесь с администрацией.",
        'user_approved_by_admin': "✅ Пользователь одобрен администратором {admin_name}.",
        'user_rejected_by_admin': "❌ Пользователь отклонен администратором {admin_name}.",
        'user_already_processed': "❗️Этот пользователь уже был обработан.",
        'cant_rent_pending': "⏳ Ваша регистрация еще не одобрена администратором. Вы не сможете арендовать автомобиль, пока ваша заявка не будет подтверждена. Пожалуйста, ожидайте.",
        'start_registration': "✍️ *Начало регистрации в качестве клиента.* Пожалуйста, введите своё полное имя (ФИО):",
        'ask_phone': '📞 *Введите 9 цифр вашего номера (например, 927777777). Префикс +992 будет добавлен автоматически:*',
        'invalid_phone': '❗️*Ошибка. Пожалуйста, введите ровно 9 цифр (например, 927777777):*',
        'ask_city': '📍 *Выберите ваш город:*',
        'select_city_from_list': '❗️*Пожалуйста, выберите город с помощью кнопок ниже.*',
        'ask_address': '🏠 *Отлично, город {city} выбран. Теперь введите ваш точный адрес проживания (улица, дом):*',
        'ask_age': '🎂 *Введите вашу дату рождения в формате ДД.ММ.ГГГГ (например, 02.04.2002):*',
        'invalid_age': '❗️*Неверный формат даты или некорректный возраст. Пожалуйста, введите дату в формате ДД.ММ.ГГГГ. Вам должно быть не менее 18 лет.*',
        'ask_driving_experience': '짬 *Введите ваш стаж вождения (в годах):*',
        'invalid_driving_experience': '❗️*Ошибка. Пожалуйста, введите ваш реальный стаж в виде числа (от 0 до 60).*',
        'ask_license': '💳 *Введите номер водительского удостоверения (серия и номер):*',
        'invalid_license': '❗️*Ошибка. Введите номер в формате AA1234567 (2 английские буквы и 7 цифр).*',
        'ask_license_category': '📋 *Укажите категорию водительского удостоверения (например, B):*',
        'ask_preferred_car_type': '🚗 *Какой тип автомобиля предпочитаете для работы?*',
        'ask_taxi_experience': '🚕 *Есть ли у вас опыт работы в такси (Яндекс, Uber, Максим, и т.д.)?*',
        'ask_driver_id': '🆔 *Если есть опыт, укажите ваш ID водителя в одной из служб. Если нет - напишите "нет":*',
        'ask_deposit': '💰 *Готовы ли вы вносить депозит/залог?*',
        'ask_passport_photo': "📸 *Отлично! Теперь по шагам загрузите документы.*\n\nШаг 1/4: Отправьте фото вашего *паспорта* (главная страница).",
        'ask_passport_back_photo': "📸 Шаг 2/4: Теперь отправьте фото *паспорта* (обратная сторона).",
        'ask_license_front_photo': "📸 Шаг 3/4: Теперь отправьте фото *водительского удостоверения* (лицевая сторона).",
        'ask_license_back_photo': "📸 Шаг 4/4: И последнее - фото *водительского удостоверения* (обратная сторона).",
        'registration_complete': "🎉 *Вы успешно зарегистрированы!*\nВаша заявка отправлена на рассмотрение. Мы свяжемся с вами в ближайшее время.\nТеперь вы можете арендовать автомобиль: /rent",
        'cancel_button': "❌ Отмена",
        'back_button': "⬅️ Назад",
        'operation_cancelled': "✅ Операция отменена.",

        # Регистрация собственника
        'owner_start_registration': "✍️ *Начало регистрации в качестве собственника авто для такси.*\n\nПожалуйста, введите ваше ФИО (как регистратора):",
        'owner_ask_owner_name': "👤 Введите ФИО собственника автомобиля (если это другой человек, иначе введите ваше же ФИО).",
        'owner_ask_car_brand': "🚗 Введите марку и модель автомобиля (например, Toyota Camry).",
        'owner_ask_car_plate': "🔢 Введите государственный номер автомобиля.",
        'invalid_plate_format_tj': "❗️*Ошибка.* Пожалуйста, введите номер, используя *английские (латинские) буквы*. Например: `1001AB02` (3 или 4 цифры, 2 буквы, 2 цифры).",
        'owner_ask_car_vin': "🔍 Введите VIN-код автомобиля.",
        'owner_ask_car_year': "📅 Введите год выпуска автомобиля.",
        'invalid_year': '❗️*Ошибка. Пожалуйста, введите корректный год выпуска в виде числа (например, 2021).*',
        'owner_ask_car_mileage': "🛣️ Введите текущий пробег автомобиля (в км).",
        'invalid_mileage': '❗️*Ошибка. Пожалуйста, введите пробег в виде целого числа (например, 75000).*',
        'owner_ask_car_transmission': "⚙️ Укажите тип коробки передач (Автомат / Механика).",
        'owner_ask_car_insurance': "🛡️ Есть ли у вас страховка ОСАГО / КАСКО?",
        'owner_ask_gps_camera': "🛰️ Готовы ли вы установить в авто GPS-трекер и камеру от нашего парка?",
        'owner_ask_car_color': "🎨 Введите цвет автомобиля по тех паспорту.",
        'owner_ask_car_type': "⛽️ Укажите тип автомобиля (ДВС / Электромобиль / Гибрид).",
        'owner_ask_car_photos': "📸 *Шаг 1/3: Фотографии авто.*\n\nОтправьте до 5 фотографий вашего автомобиля. Отправляйте по одной. Когда закончите, нажмите кнопку *«✅ Готово»*.",
        'finish_photo_upload_btn': "✅ Готово",
        'owner_photo_received': "✅ Фото {count} из 5 получено. Отправьте следующее или нажмите *«✅ Готово»*.",
        'max_photos_reached': "✅ Максимальное количество (5) фото загружено. Переходим к следующему шагу...",
        'owner_ask_desired_price': "💰 *Шаг 2/3: Желаемая цена.*\n\nУкажите вашу *желаемую* сумму аренды в *день* (в сомони). Например: `250`\n\n_(Итоговая сумма будет согласована с вами и указана в договоре.)_",
        'invalid_price': '❗️*Ошибка.* Пожалуйста, введите сумму в виде целого числа (например, 250).',
        'ask_docs_after_price': "🎉 *Отлично!* Анкета и фото авто заполнены.\n\n*Шаг 3/3: Документы.*\n\nТеперь, как и раньше, по шагам загрузите документы.",
        'ask_tech_passport_photo': "📸 Шаг 1/4: Отправьте фото *технического паспорта* автомобиля (лицевая сторона).",
        'ask_tech_passport_back_photo': "📸 Шаг 2/4: Теперь отправьте фото *технического паспорта* (обратная сторона).",
        'ask_owner_passport_photo': "📸 Шаг 3/4: Теперь отправьте фото *паспорта собственника* автомобиля (лицевая сторона).",
        'ask_owner_passport_back_photo': "📸 Шаг 4/4: И последнее - фото *паспорта собственника* (обратная сторона).",
        
        'photo_received_proceed': "✅ Фото получено. Переходим к следующему шагу...",
        'not_a_photo_error': "❗️Пожалуйста, отправьте именно *фотографию*, а не файл или текст.",
        'registration_complete': "🎉 *Вы успешно зарегистрированы!*\nВаша заявка со всеми документами отправлена на рассмотрение. Мы свяжемся с вами в ближайшее время.",
        
        # Команды собственника
        'payouts_info': "📊 *Начисления по вашему авто:*\n\nНа данный момент эта функция находится в разработке. Подробная информация о начислениях будет доступна здесь в ближайшее время.",
        'drivers_info_assigned': "👤 *Водитель на вашей машине*\n\nВаш автомобиль `{car_brand}` (гос. номер: `{car_plate}`) сейчас использует водитель: *Алиев Алишер*.\nТелефон для связи: `+992 92 123 45 67`.",
        'drivers_info_free': "👤 *Водитель на вашей машине*\n\nВаш автомобиль `{car_brand}` (гос. номер: `{car_plate}`) в данный момент свободен и находится на стоянке парка.",
        'service_request_prompt': "⚙️ *Заявка на техобслуживание*\n\nПожалуйста, опишите проблему или причину для ТО одним сообщением. Например: 'Нужно поменять масло и проверить тормоза'.\n\n_(Для отмены /cancel)_",
        'service_request_sent': "✅ Ваша заявка на техобслуживание отправлена. Менеджер свяжется с вами для согласования времени.",
        'calendar_info': "📆 *График использования машины*\n\nНа данный момент эта функция находится в разработке. Здесь будет отображаться календарь занятости вашего автомобиля.",
        'contract_info': "📝 *Договор с собственником*\n\n*1. ПРЕДМЕТ ДОГОВОРА*\n1.1. Собственник передает, а Компания принимает в управление автомобиль (далее - 'Авто') для использования в сервисе такси.\n\n*2. ОБЯЗАННОСТИ СТОРОН*\n2.1. Компания обязуется своевременно выплачивать Собственнику доход от использования Авто за вычетом комиссии и расходов на обслуживание...\n\n_(Это пример текста. Полная версия договора будет предоставлена в офисе.)_",

        # Аренда
        'must_register_first': '❌ *Для аренды сначала пройдите регистрацию: /register*',
        'you_are_blocked': '❌ *Вы были заблокированы администратором.*',
        'no_cars_available': "❗️К сожалению, на данный момент нет доступных автомобилей для аренды.",
        'choose_car_for_rent': "🚙 *Выберите автомобиль:*",
        'select_car_from_list': '❗️*Пожалуйста, выберите автомобиль из списка на клавиатуре.*',
        'car_details_price': "💰 *Прайс-лист:*",
        'price_day': "• День: *{price} сомони*",
        'price_week': "• Неделя: *{price} сомони*",
        'price_month': "• Месяц: *{price} сомони*",
        'car_specs': "🔧 *Характеристики:*",
        'car_year': "• Год выпуска: {year}",
        'car_fuel': "• Тип топлива: {fuel_type}",
        'car_engine': "• Двигатель: {engine}",
        'car_transmission': "• Коробка передач: {transmission}",
        'car_seats': "• Места: {seats}",
        'choose_rent_period': '⏳ *Выберите период аренды:*',
        'select_period_from_list': '❗️*Пожалуйста, выберите период из списка ниже:*',
        'rental_request_accepted': "✅ *Ваша заявка №{id} принята.*\nНаш менеджер свяжется с вами. Проверяйте статус командой /myrentals.",
        'rental_status_changed': "Статус вашей заявки №{id} на *{car}* изменен:\n\n*{emoji} {status}*",
        'status_approved_details': "\nНаш менеджер свяжется с вами для уточнения деталей.",
        'status_rejected_details': "\nДля дополнительной информации свяжитесь с нами через /contact.",

        # Профиль и заявки
        'profile_title': "👤 *Ваш профиль*",
        'profile_role': "▪️ *Ваша роль:* {role}",
        'profile_name': "▪️ *ФИО:* {name}",
        'profile_phone': "▪️ *Телефон:* `{phone}`",
        'profile_license': "▪️ *В/У:* `{license}`",
        'profile_city': '▪️ *Город:* {city}',
        'profile_address': '▪️ *Адрес:* {address}',
        'profile_age': "▪️ *Дата рождения:* {dob}", 
        'profile_driving_experience': "▪️ *Стаж:* {experience} лет",
        'profile_license_category': "▪️ *Категория В/У:* {category}",
        'profile_preferred_car': "▪️ *Предпочитаемый авто:* {car_type}",
        'profile_taxi_experience': "▪️ *Опыт в такси:* {experience}",
        'profile_driver_id': "▪️ *ID Водителя:* {driver_id}",
        'profile_deposit': "▪️ *Готовность к залогу:* {deposit}",
        'profile_status': "▪️ *Статус:* {status}",
        'not_registered': "Вы не зарегистрированы. /register",
        'your_rentals_title': "📋 *Ваши заявки на аренду:*",
        'rental_list_item': "• *Заявка №{id}* ({car}) - Статус: *{status}* (дата: {date})",
        'no_rentals_yet': "Заявок пока нет.",
        

        # Редактирование профиля
        'edit_profile_btn': "✏️ Редактировать профиль",
        'choose_field_to_edit': "✏️ *Какой параметр вы хотите изменить?*",
        'prompt_new_value': "✍️ *Введите новое значение для поля '{field_name}':*",
        'profile_updated_successfully': "✅ *Ваш профиль успешно обновлен!*",
        'invalid_value': "❗️*Неверное значение.* Пожалуйста, попробуйте еще раз.",
        'field_name': "ФИО",
        'field_phone': "Номер телефона",
        'field_city': "Город",
        'field_age': "Дата рождения",
        'field_driving_experience': "Стаж вождения",
        'field_license': "Номер В/У",
        'field_address': "Точный адрес", 
        'field_registrator_name': "ФИО Регистратора",
        'field_owner_name': "ФИО Собственника",

        # Другое
        'loading_car_list': "⏳ *Пожалуйста, подождите, список автомобилей загружается...*",
        'full_car_list_end': "🏁 *Полный список автомобилей.*",
        'message_to_admin_usage': "Использование: `/message <текст сообщения>`",
        'no_admins_online': "❗️ В данный момент нет администраторов.",
        'message_sent_to_admin': "✅ Ваше сообщение отправлено администратору.",
        'contact_info_title': "📞 *Контактная информация:*",
        'contact_info_details': "*Телефон:* +992927997371\n*Email:* [job@ioyandasoz.com](mailto:job@ioyandasoz.com)\n*Телеграмм:* [https://t.me/Academy_iO](https://t.me/Academy_iO)\n*Адрес:* ул. Исмоили Сомони 262, Худжанд\n\nВы также можете связаться с нами через *'💬 Чат с менеджером'* в главном меню или использовать команду /chat.",
        'faq_title': "❓ *Часто задаваемые вопросы:*",
        'faq_content': "*1. Как арендовать автомобиль?*\nДля аренды сначала зарегистрируйтесь с помощью команды /register, а затем выберите автомобиль с помощью /rent. Наш менеджер свяжется с вами.\n\n*2. Какие документы необходимы?*\nВодительское удостоверение и национальный паспорт.\n\n*3. Как связаться с технической поддержкой?*\nОтправьте сообщение с помощью /message <текст> или позвоните по номерам из /contact.",
        'admin_reply_prefix': "📥 *Сообщение от администратора:*\n\n{text}",

        # Админские тексты
        'admin_broadcast_confirm': "Отправить это сообщение *{count}* активным пользователям?\n\n`{text}`\n\nНапишите 'да' для подтверждения или /cancel для отмены.",
        'admin_broadcast_start': "⏳ Начинаю рассылку для {count} пользователей...",
        'admin_broadcast_finish': "✅ Рассылка завершена. Сообщение отправлено {count} пользователям.",
        'admin_broadcast_cancelled': "❌ Рассылка отменена.",
        'admin_user_not_found': "❗️Пользователь '{user}' не найден.",
        'admin_user_deleted': "✅ Пользователь *{name}* (`{id}`) удален.",
        'admin_user_profile_deleted_notification': "⚠️ Ваш профиль был удален администратором.",
        'admin_status_changed_notification': "✅ Статус для {name} ({id}) изменен на *{status}*.",
        'admin_user_status_changed_notification': "⚠️ Ваш статус изменен администратором на *{status}*.",
        'admin_reply_sent': "✅ Ответ отправлен {name} (`{id}`).",
        'admin_reply_failed': "❌ Не удалось отправить ответ.",
        'admin_add_staff_usage': "Использование: `/addstaff <ID> <role>`\nДоступные роли: manager, mechanic, accountant",
        'admin_staff_added': "✅ Сотрудник {name} (ID: {id}) добавлен как {role}.",
        'admin_staff_exists': "❗️ Пользователь уже является сотрудником с ролью {role}.",
        'admin_staff_removed': "✅ Сотрудник {name} (ID: {id}) удален.",
        'admin_staff_not_found': "❗️ Сотрудник не найден.",
        'staff_help_manager': "\n\n*Команды менеджера:*\n"
                                "/approverental <ID> - Одобрить заявку\n"
                                "/rejectrental <ID> - Отклонить заявку\n"
                                "/listpending - Список ожидающих заявок\n\n"
                                "*Управление пользователями:*\n"
                                "/listusers - Показать всех пользователей\n"
                                "/userinfo <ID|@user> - Инфо о пользователе\n"
                                "/block <ID|@user> - Заблокировать\n"
                                "/unblock <ID|@user> - Разблокировать\n"
                                "/deleteuser <ID|@user> - Удалить\n"
                                "/reply <ID|@user> <текст> - Ответить пользователю\n"
                                "/broadcast <текст> - Сообщение всем активным\n\n"
                                "*Управление автопарком:*\n"
                                "/addcar | /editcar | /deletecar\n"
                                "/listallrentals - Все заявки на аренду\n\n"
                                "*Управление персоналом:*\n"
                                "/addstaff <ID> <роль> - Добавить сотрудника\n"
                                "/removestaff <ID> - Удалить сотрудника\n"
                                "/liststaff - Список сотрудников\n\n"
                                "*Финансы и ТО:*\n"
                                "/payout <ID> <сумма> - Уведомить о выплате\n"
                                "/servicedone <ID> - Уведомить о завершении ТО",
        'staff_help_mechanic': "\n\n*Команды механика:*\n"
                               "/servicecomplete <ID> - Отметить ТО выполненным\n"
                               "/listmaintenance - Список заявок на ТО\n\n"
                               "*/dailycheck* - Ежедневная проверка авто на рейс\n"
                               "*/acceptfromowner* - Приемка авто у собственника (чек-лист)\n"
                               "*/givetodriver* - Выдача/Приемка авто у водителя\n"
                               "*/edithandover* - Изменить данные приемки/выдачи",
        'staff_help_accountant': "\n\n*Команды бухгалтера:*\n/addpayment <ID> <сумма> - Добавить платеж\n/generatereport - Сгенерировать отчет",
        'mechanic_dailycheck_cmd': "Ежедневная проверка авто на рейс",
        'mechanic_acceptfromowner_cmd': "Приемка авто у собственника (чек-лист)",
        'mechanic_givetodriver_cmd': "Выдача/Приемка авто у водителя",
        
        # Уведомления для Водителя
        'driver_rent_payment_due': "❗️ *Напоминание об оплате*\n\nПожалуйста, не забудьте произвести оплату за аренду автомобиля `{car_brand}`.",
        'driver_maintenance_due_dvs': "🔧 *Техобслуживание*\n\nНа автомобиле `{car_brand}` (`{car_plate}`) необходимо произвести замену масла в двигателе. Пожалуйста, свяжитесь с менеджером.",
        'driver_maintenance_due_electro': "🔧 *Техобслуживание*\n\nНа электромобиле `{car_brand}` (`{car_plate}`) необходимо произвести замену масла в редукторе. Пожалуйста, свяжитесь с менеджером.",
        'driver_inspection_due': "📋 *Техосмотр*\n\nСрок государственного технического осмотра для автомобиля `{car_brand}` (`{car_plate}`) истекает `{date}`. Пожалуйста, свяжитесь с менеджером.",
        'driver_license_due': "📄 *Лицензия*\n\nСрок действия лицензии на таксомоторную деятельность для автомобиля `{car_brand}` (`{car_plate}`) истекает `{date}`.",
        'driver_car_is_free': "🚙 *Свободный автомобиль*\n\nИнформируем вас, что автомобиль `{car_brand}` теперь доступен для аренды. Используйте /rent для бронирования.",

        # Уведомления для Собственника
        'owner_driver_assigned': "👤 *Назначен водитель*\n\nНа ваш автомобиль `{car_brand}` (`{car_plate}`) назначен новый водитель: {driver_name} (тел: `{driver_phone}`).",
        'owner_maintenance_completed': "✅ *Техобслуживание завершено*\n\nРаботы по техническому обслуживанию вашего автомобиля `{car_brand}` (`{car_plate}`) были успешно завершены.",
        'owner_license_due': "📄 *Лицензия*\n\nВнимание! Срок действия лицензии на таксомоторную деятельность для вашего автомобиля `{car_brand}` (`{car_plate}`) истекает `{date}`.",
        'owner_payout_received': "💰 *Поступление средств*\n\nНа ваш счет зачислена оплата за аренду автомобиля `{car_brand}` в размере *{amount} сомони*.",
        'owner_inspection_due': "📋 *Техосмотр*\n\nВнимание! Срок государственного технического осмотра для вашего автомобиля `{car_brand}` (`{car_plate}`) истекает `{date}`.",
        
        'choose_rent_type': 'Пожалуйста, выберите тип аренды:',
        'short_term': 'Краткосрочный',
        'long_term': 'Долгосрочный',
        'from_6_months': 'До 6 месяцев',
        'up_to_1_year': 'От 6 месяцев до 1 года',
        'choose_number_of_days': "🔢 Выберите количество дней для аренды:",
        'invalid_vin': "❗️*Ошибка.* VIN-код должен состоять ровно из 17 символов (латинские буквы и цифры, кроме I, O, Q). Пожалуйста, проверьте и введите снова.",      
        'edit_as_client_btn': "✍️ Редактировать как Водитель",
        'edit_as_owner_btn': "🚗 Редактировать как Собственник",
        'not_a_client_error': "❗️ Вы не зарегистрированы как водитель. Для регистрации используйте /register.",
        'not_a_owner_error': "❗️ Вы не зарегистрированы как собственник. Для регистрации используйте /owner_register.",
        'consent_prompt': "Нажимая кнопку «Подтверждаю», я выражаю своё согласие на обработку моих персональных данных и на получение SMS-уведомлений от нашей компании.\n\nПожалуйста, подтвердите ваше согласие.",
        'consent_confirm_btn': "✅ Подтверждаю",
        
        # --- Тексты для функции "Сообщить об ошибке" ---
        'admin_btn_report_error': "✉️ Сообщить об ошибке",
        'admin_prompt_custom_error': "✍️ Введите текст сообщения для пользователя (ID: {target_id}).\n\n_(Ваше сообщение будет отправлено ему от имени бота. Для отмены используйте /cancel)_",
        'admin_custom_error_sent': "✅ Сообщение отправлено пользователю {target_id}.",
        'admin_select_error_reason': "Выберите причину, чтобы уведомить пользователя:",
        'error_reason_photo': "Нечеткие фото документов",
        'error_reason_mismatch': "Данные в анкете и документах не совпадают",
        'error_reason_other': "Другое (связаться лично)",
        'admin_btn_cancel_error': "Отмена",
        'admin_error_reported': "✅ Уведомление об ошибке отправлено пользователю.",
        'user_error_notification_prefix': "❗️ *Уведомление по вашей заявке на регистрацию*",
        'error_message_photo': "Здравствуйте! Мы рассмотрели вашу заявку. К сожалению, фотографии ваших документов получились нечеткими или размытыми. Пожалуйста, сделайте новые, четкие снимки при хорошем освещении и свяжитесь с нами снова через /message, чтобы мы могли обновить вашу заявку.",
        'error_message_mismatch': "Здравствуйте! Мы рассмотрели вашу заявку и заметили несоответствие между данными, которые вы ввели в анкете (например, ФИО или номер В/У), и данными на фотографиях документов. Пожалуйста, проверьте информацию и свяжитесь с нами через /message, чтобы мы могли исправить вашу анкету.",
        'error_message_other': "Здравствуйте! По вашей заявке на регистрацию возникли некоторые вопросы. Пожалуйста, свяжитесь с нашим менеджером для уточнения деталей. Контакты: /contact",
        'registration_rejected_client': "❌ К сожалению, ваша заявка была отклонена. Пожалуйста, исправьте указанные недочеты и пройдите регистрацию заново с помощью команды /register.",
        'registration_rejected_owner': "❌ К сожалению, ваша заявка была отклонена. Пожалуйста, исправьте указанные недочеты и пройдите регистрацию как собственник заново с помощью команды /owner_register.",
        'registration_rejected_owner': "...",
        'city_selected': "✅ Выбрано: {city_name}",
        'category_selected': "✅ *Выбрана категория: {category}*", 
        'choice_selected': "✅ Выбрано.",
        'cancel_hint': "\n\n_(Для отмены введите /cancel)_",
        'already_registered_as_driver': "✅ Вы уже зарегистрированы как Водитель.",
        'transmission_auto': "Автомат", 
        'transmission_manual': "Механика", 
        'transmission_selected': "✅ *Выбрано: {choice_text}*",
        'main_menu_prompt': "👇 Выберите действие:",
        'i_am_the_owner_btn': "👤 Я и есть собственник",
        

    
    },   
    'tj': {
        # Тексты для кнопок главного меню
        'menu_rent': "🚙 Мошинро ба иҷора гирифтан",
        'menu_list_cars': "📋 Рӯйхати мошинҳо",
        'menu_my_rentals': "📄 Дархостҳои ман",
        'menu_profile': "👤 Профили ман",
        'menu_contact': "📞 Тамос",
        'menu_help': "ℹ️ Ёрӣ",
        'menu_search': "🔍 Ҷустуҷӯ", 
        'menu_register_client': "✍️ Бақайдгирии Ронанда",     
        'menu_register_owner': "✍️ Бақайдгирии Соҳибмулк",  
        'menu_chat_manager': "💬 Чат бо менеҷер",
        'menu_language': "🌐 Иваз кардани забон",   

        'enter_chat_mode': "💬 Шумо ба режими чат бо менеҷер ворид шудед. Ҳамаи паёмҳои минбаъдаи шумо фиристода мешаванд. \n\nБарои баромадан, тугмаи поёнро пахш кунед ё фармони /endchat-ро ворид кунед.", 
        'exit_chat_mode': "💬 Шумо аз режими чат баромадед.", 
        'end_chat_button': "❌ Қатъи чат", 
        'admin_btn_reply': "✍️ Ҷавоб додан",
        'admin_prompt_reply': "✍️ Ҷавоби худро барои истифодабарандаи {user_name} (ID: {user_id}) ворид кунед.\n\n_(Барои бекор кардан /cancel истифода баред)_",
        'admin_reply_sent_successfully': "✅ Ҷавоби шумо ба истифодабарандаи {user_name} бомуваффақият фиристода шуд.",
        'admin_enter_chat_session': "💬 *Шумо ба режими чат бо истифодабарандаи {user_name} ворид шудед.*\n\nҲамаи паёмҳои шумо акнун ба ӯ фиристода мешаванд. Барои қатъ кардани чат, тугмаи поёнро пахш кунед ё /cancel-ро ворид кунед.",
        'admin_exit_chat_session': "💬 Шумо чатро бо истифодабарандаи {user_name} ба анҷом расондед.",
        'admin_end_chat_button': "❌ Қатъи чат бо {user_name}",
        'admin_user_ended_chat': "🔴 Истифодабаранда {user_name} (ID: {user_id}) чатро ба анҷом расонд.",
        ## << КОНЕЦ ИЗМЕНЕНИЙ ##
        
        'welcome': "👋 *Салом, {name}!* Ба боти иҷораи мошинҳо дар Хуҷанд хуш омадед!",
        'guest': "Меҳмон",
        'choose_your_role': "Барои оғоз, лутфан нақши худро интихоб кунед:",
        'start_btn_client': "👤 Ман Ронанда",
        'start_btn_owner': "🚗 Ман Соҳибмулк",
        'go_to_main_menu': "🏠 Менюи асосӣ",
        'role_selection_made': "✅ *Нақш интихоб шуд: {role_name}*",
        'role_client': "Муштарӣ",
        'role_owner': "Соҳибмулк",
        'help_intro': "ℹ️ *Дастур оиди истифодаи бот*",
        'register_cmd': "*/register* - Бақайдгирии ҳамчун муштарӣ барои иҷораи мошин.",
        'owner_register_cmd': "*/owner_register* - Бақайдгирӣ ҳамчун соҳиби мошин барои супоридан ба таксӣ.",
        'rent_cmd': "*/rent* - Интихоби мошин барои иҷора.",
        'listcars_cmd': "*/listcars* - Дидани руйхати мошинҳо ва нархҳо.",
        'myrentals_cmd': "*/myrentals* - Дидани статуси дархостҳои иҷораи худ.",
        'profile_cmd': "*/profile* - Дидани профили худ.",
        'edit_profile_cmd': "*/editprofile* - Тағйир додани маълумоти профили худ.",
        'message_cmd': "*/message <матн>* - Фиристодани паём ба администратор.",
        'contact_cmd': "*/contact* - Намоиши маълумот барои тамос.",
        'faq_cmd': "*/faq* - Ҷавоб ба саволҳои зуд-зуд додашаванда.",
        'cancel_cmd': "*/cancel* - Бекор кардани амалиёти ҷорӣ.",
        'lang_cmd': "*/language* - Тағйир додани забон.",
        'admin_help_cmd': "\n\n*Фармонҳои администратор:* /adminhelp",
        'owner_help_intro': "\n\n*Фармонҳои соҳиби мошин:*",
        'owner_payouts_cmd': "*/payouts* - Дидани ҳисоббаробаркуниҳо барои мошини шумо.",
        'owner_drivers_cmd': "*/drivers* - Фаҳмидани он ки ҳоло кӣ дар мошини шумост.",
        'owner_service_cmd': "*/service* - Фиристодани дархост барои хидматрасонии техникӣ.",
        'owner_calendar_cmd': "*/calendar* - Дидани ҷадвали истифодаи мошин.",
        'owner_contract_cmd': "*/contract* - Гирифтани нусхаи шартнома.",
        'unknown_command': "🤷 Фармони номаълум. Барои дидани рӯйхати фармонҳо /help -ро истифода баред.",
        'operation_cancelled': "✅ Амалиёт бекор карда шуд.",
        'no_active_operation': "🤷 Амалиёти фаъол барои бекор кардан вуҷуд надорад.",
        'choose_language': "🇷🇺 Выберите язык / 🇹🇯 Забонро интихоб кунед:",
        'language_set': "✅ Забон ба *Тоҷикӣ* гузошта шуд.",
        'must_be_owner': "❗️Ин фармон танҳо барои соҳибони мошинҳои ба қайд гирифташуда дастрас аст.",

        # Слова для клавиатур
        'day': 'Рӯз',
        'week': 'Ҳафта',
        'month': 'Моҳ',
        'yes': 'Бале',
        'no': 'Не',
        'ice_car': 'СБД (ДВС)',
        'electric_car': 'Электромобил',
        
        # Регистрация клиента
        'already_registered': "✅ Шумо аллакай ба қайд гирифта шудаед ва профили шумо фаъол аст. Барои иҷора /rent -ро истифода баред.",
        'registration_pending': "⏳ Дархости шумо барои бақайдгирӣ аллакай фиристода шудааст ва дар ҳоли баррасӣ қарор дорад. Лутфан, тасдиқи администраторро интизор шавед.",
        'cant_rent_pending': "⏳ Бақайдгирии шумо ҳанӯз аз ҷониби администратор тасдиқ нашудааст. То тасдиқ шудани дархостатон, шумо мошинро ба иҷора гирифта наметавонед. Лутфан, интизор шавед.",
        'registration_approved': "✅ Бақайдгирии шумо тасдиқ карда шуд! Акнун шумо метавонед аз ҳамаи вазифаҳои бот истифода баред.",
        'registration_rejected': "❌ Мутаассифона, дархости шумо барои бақайдгирӣ рад карда шуд. Барои тафсилоти бештар бо администратор тамос гиред.",
        'user_approved_by_admin': "✅ Истифодабаранда аз ҷониби администратор {admin_name} тасдиқ карда шуд.",
        'user_rejected_by_admin': "❌ Истифодабаранда аз ҷониби администратор {admin_name} рад карда шуд.",
        'user_already_processed': "❗️Ин истифодабаранда аллакай коркард шудааст.",
        'start_registration': "✍️ *Оғози бақайдгирӣ ҳамчун муштарӣ.* Лутфан номи пурраи худро (ФИО) ворид кунед:",
        'ask_phone': '📞 *9 рақами телефони худро ворид кунед (масалан, 927777777). Префикси +992 ба таври худкор илова карда мешавад:*',
        'invalid_phone': '❗️*Хатогӣ. Лутфан, 9 рақамро дуруст ворид кунед (масалан, 927777777):*',
        'ask_city': '📍 *Шаҳри худро интихоб кунед:*',
        'select_city_from_list': '❗️*Лутфан, шаҳрро бо истифода аз тугмаҳои зер интихоб кунед.*',
        'ask_address': '🏠 *Олӣ, шаҳри {city} интихоб шуд. Акнун суроғаи дақиқи истиқомати худро ворид кунед (кӯча, хона):*',
        'ask_age': '🎂 *Санаи таваллуди худро дар формати РР.ММ.СССС ворид кунед (масалан, 02.04.2002):*',
        'invalid_age': '❗️*Формати сана нодуруст ё синну соли номувофиқ. Лутфан, санаро дар формати РР.ММ.СССС ворид кунед. Шумо бояд на камтар аз 18-сола бошед.*',
        'ask_driving_experience': '짬 *Собиқаи ронандагии худро ворид кунед (бо сол):*',
        'invalid_driving_experience': '❗️*Хатогӣ. Лутфан, собиқаи воқеии худро дар шакли рақам ворид кунед (аз 0 то 60).*',
        'ask_license': '💳 *Рақами шаҳодатномаи ронандагиро ворид кунед (серия ва рақам):*',
        'invalid_license': '❗️*Хатогӣ. Рақамро дар формати AA1234567 ворид кунед (2 ҳарфи англисӣ/лотинӣ ва 7 рақам).*',
        'ask_license_category': '📋 *Категорияи шаҳодатномаи ронандагии худро нишон диҳед (масалан, B):*',
        'ask_preferred_car_type': '🚗 *Кадом намуди мошинро барои кор бартарӣ медиҳед?*',
        'ask_taxi_experience': '🚕 *Оё шумо собиқаи кор дар таксӣ доред (Яндекс, Uber, Максим ва ғ.)?*',
        'ask_driver_id': '🆔 *Агар собиқа дошта бошед, ID-и ронандагии худро дар яке аз хадамот нишон диҳед. Агар не - "не" нависед:*',
        'ask_deposit': '💰 *Оё шумо омодаед, ки депозит/гарав гузоред?*', 
        'ask_passport_photo': "📸 *Олӣ! Акнун ҳуҷҷатҳоро марҳила ба марҳила бор кунед.*\n\nҚадами 1/4: Акси *шиносномаи* худро ирсол кунед (саҳифаи асосӣ).",
        'ask_passport_back_photo': "📸 Қадами 2/4: Акнун акси *шиносномаро* (тарафи қафо) ирсол кунед.",
        'ask_license_front_photo': "📸 Қадами 3/4: Акнун акси *шаҳодатномаи ронандагӣ*-ро ирсол кунед (тарафи пеш).",
        'ask_license_back_photo': "📸 Қадами 4/4: Ва охирон - акси *шаҳодатномаи ронандагӣ* (тарафи қафо).",
        'registration_photo_received': '✅ *Акс қабул карда шуд. Бақайдгирӣ анҷом меёбад...*',
        'registration_complete': "🎉 *Шумо бомуваффақият ба қайд гирифта шудед!*\nДархости шумо барои баррасӣ фиристода шуд. Мо ба наздикӣ бо шумо дар тамос хоҳем шуд.\nАкнун метавонед мошинро ба иҷора гиред: /rent",

        # Бақайдгирии соҳибмулк
        'owner_start_registration': "✍️ *Оғози бақайдгирӣ ҳамчун соҳиби мошин барои таксӣ.*\n\nЛутфан, ному насаби худро (ҳамчун бақайдгиранда) ворид кунед:",
        'owner_ask_owner_name': "👤 Ному насаби соҳиби мошинро ворид кунед (агар шахси дигар бошад, дар акси ҳол ному насаби худро ворид кунед).",
        'owner_ask_car_brand': "🚗 Марка ва модели мошинро ворид кунед (масалан, Toyota Camry).",
        'owner_ask_car_plate': "🔢 Рақами давлатии мошинро ворид кунед.",
        'invalid_plate_format_tj': "❗️*Хатогӣ.* Лутфан, рақамро бо истифода аз *ҳарфҳои англисӣ (лотинӣ)* ворид кунед. Масалан: `1001AB02` (3 ё 4 рақам, 2 ҳарф, 2 рақам).",
        'owner_ask_car_vin': "🔍 VIN-коди мошинро ворид кунед.",
        'owner_ask_car_year': "📅 Соли барориши мошинро ворид кунед.",
        'invalid_year': '❗️*Хатогӣ. Лутфан, соли барориши дурустро дар шакли рақам ворид кунед (масалан, 2021).*',
        'owner_ask_car_mileage': "🛣️ Масофаи ҳозираи мошинро ворид кунед (бо км).",
        'invalid_mileage': '❗️*Хатогӣ. Лутфан, масофаи тайшударо дар шакли рақами бутун ворид кунед (масалан, 75000).*',
        'owner_ask_car_transmission': "⚙️ Навъи қуттии фишангҳоро нишон диҳед (Автомат / Механика).",
        'owner_ask_car_insurance': "🛡️ Оё шумо суғуртаи ОСАГО / КАСКО доред?",
        'owner_ask_gps_camera': "🛰️ Оё шумо омодаед, ки дар мошин GPS-трекер ва камераро аз парки мо насб кунед?",
        'owner_ask_car_color': "🎨 Ранги мошинро аз руи варакаи техникӣ дохил кунед .",
        'owner_ask_car_type': "⛽️ Навъи мошинро нишон диҳед (СБД / Электромобил / Гибрид).",

        'owner_ask_car_photos': "📸 *Қадами 1/3: Аксҳои мошин.*\n\nТо 5 дона акси автомобили худро ирсол кунед. Як-донагӣ фиристед. Пас аз анҷом, тугмаи *«✅ Тайёр»*-ро пахш кунед.",
        'finish_photo_upload_btn': "✅ Тайёр",
        'owner_photo_received': "✅ Акси {count} аз 5 қабул шуд. Акси навбатиро фиристед ё *«✅ Тайёр»*-ро пахш кунед.",
        'max_photos_reached': "✅ Миқдори максималии аксҳо (5) бор карда шуд. Ба қадами оянда мегузарем...",
        'owner_ask_desired_price': "💰 *Қадами 2/3: Нархи дилхоҳ.*\n\nМаблағи *хоҳишмандонаи* иҷораи *як рӯзаро* нишон диҳед (бо сомонӣ). Масалан: `250`\n\n_(Маблағи ниҳоӣ бо шумо мувофиқа карда шуда, дар шартнома дарҷ мегардад.)_",
        'invalid_price': '❗️*Хатогӣ.* Лутфан, маблағро дар шакли рақами бутун ворид кунед (масалан, 250).',
        'ask_docs_after_price': "🎉 *Олӣ!* Пурсишнома ва аксҳои мошин пур карда шуданд.\n\n*Қадами 3/3: Ҳуҷҷатҳо.*\n\nАкнун, мисли пештара, ҳуҷҷатҳоро марҳила ба марҳила бор кунед.",


        'ask_tech_passport_photo': "📸 Қадами 1/4: Акси *паспорти техникии* мошинро (тарафи пеш) ирсол кунед.",
        'ask_tech_passport_back_photo': "📸 Қадами 2/4: Акнун акси *паспорти техникии* мошинро (тарафи қафо) ирсол кунед.",
        'ask_owner_passport_photo': "📸 Қадами 3/4: Акнун акси *шиносномаи соҳибмулк*-ро (тарафи пеш) ирсол кунед.",
        'ask_owner_passport_back_photo': "📸 Қадами 4/4: Ва охирон - акси *шиносномаи соҳибмулк*-ро (тарафи қафо) ирсол кунед.",

        'photo_received_proceed': "✅ Акс қабул шуд. Ба қадами оянда мегузарем...",
        'not_a_photo_error': "❗️Лутфан, маҳз *акс* ирсол кунед, на файл ё матн.",
        'registration_complete': "🎉 *Шумо бомуваффақият ба қайд гирифта шудед!*\nДархости шумо бо ҳамаи ҳуҷҷатҳо барои баррасӣ фиристода шуд. Мо ба наздикӣ бо шумо дар тамос хоҳем шуд.",
        'cancel_button': "❌ Бекор кардан",
        'back_button': "⬅️ Бозгашт",
        'operation_cancelled': "✅ Амалиёт бекор карда шуд.",
        
        # --- НОВЫЙ РАЗДЕЛ ДЛЯ МЕХАНИКА ---
        'staff_help_manager': "\n\n*Фармонҳои менеҷер:*\n/approverental <ID> - Тасдиқи дархост\n/rejectrental <ID> - Рад кардани дархост\n/listpending - Рӯйхати дархостҳои дар интизорӣ буда",
        'staff_help_mechanic': "\n\n*Фармонҳои механик:*\n"
                               "/servicecomplete <ID> - Иҷрошуда қайд кардани ХТ (ТО)\n"
                               "/listmaintenance - Рӯйхати дархостҳо барои ХТ (ТО)\n\n"
                               "*/dailycheck* - Санҷиши ҳаррӯзаи мошин\n"
                               "*/acceptfromowner* - Қабули мошин аз соҳибмулк (чек-лист)\n"
                               "*/givetodriver* - Додан/Қабул кардани мошин аз ронанда\n"
                               "*/edithandover* - Тағйир додани маълумоти қабул/супор",
        'staff_help_accountant': "\n\n*Фармонҳои муҳосиб:*\n/addpayment <ID> <маблағ> - Илова кардани пардохт\n/generatereport - Эҷоди ҳисобот",
        'mechanic_dailycheck_cmd': "Санҷиши ҳаррӯзаи мошин",
        'mechanic_acceptfromowner_cmd': "Қабули мошин аз соҳибмулк (чек-лист)",
        'mechanic_givetodriver_cmd': "Додан/Қабул кардани мошин аз ронанда",


        # Редактирование профиля
        'edit_profile_btn': "✏️ Таҳрири профил",
        'choose_field_to_edit': "✏️ *Кадом маълумотро тағйир додан мехоҳед?*",
        'prompt_new_value': "✍️ *Маълумоти навро барои '{field_name}' ворид кунед:*",
        'profile_updated_successfully': "✅ *Профили шумо бомуваффақият навсозӣ шуд!*",
        'invalid_value': "❗️*Маълумоти нодуруст.* Лутфан, бори дигар кӯшиш кунед.",
        'field_name': "Ному насаб",
        'field_phone': "Рақами телефон",
        'profile_city': '▪️ *Шаҳр:* {city}',
        'profile_address': '▪️ *Суроға:* {address}',
        'profile_age': '▪️ *Санаи таваллуд:* {dob}',
        'field_age': "Санаи таваллуд",
        'field_driving_experience': "Собиқаи ронандагӣ",
        'field_license': "Рақами шаҳодатнома",
        'field_address': "Суроғаи дақиқ", 
        'field_registrator_name': "Ному насаби Бақайдгиранда",
        'field_owner_name': "Ному насаби Соҳибмулк",

        # Огоҳиномаҳо барои Ронанда
        'driver_rent_payment_due': "❗️ *Хотиррасонӣ барои пардохт*\n\nЛутфан, пардохти иҷораи мошини `{car_brand}`-ро фаромӯш накунед.",
        'driver_maintenance_due_dvs': "🔧 *Хизматрасонии техникӣ*\n\nДар мошини `{car_brand}` (`{car_plate}`) иваз кардани равғани муҳаррик зарур аст. Лутфан, бо менеҷер тамос гиред.",
        'driver_maintenance_due_electro': "🔧 *Хизматрасонии техникӣ*\n\nДар электромобили `{car_brand}` (`{car_plate}`) иваз кардани равғани редуктор зарур аст. Лутфан, бо менеҷер тамос гиред.",
        'driver_inspection_due': "📋 *Азназаргузаронии техникӣ*\n\nМӯҳлати азназаргузаронии техникии давлатии мошини `{car_brand}` (`{car_plate}`) `{date}` ба охир мерасад. Лутфан, бо менеҷер тамос гиред.",
        'driver_license_due': "📄 *Иҷозатнома*\n\nМӯҳлати эътибори иҷозатнома барои фаъолияти таксӣ барои мошини `{car_brand}` (`{car_plate}`) `{date}` ба охир мерасад.",
        'driver_car_is_free': "🚙 *Мошини озод*\n\nБа шумо хабар медиҳем, ки мошини `{car_brand}` ҳоло барои иҷора дастрас аст. Барои банд кардан /rent -ро истифода баред.",

        # Огоҳиномаҳо барои Соҳибмулк
        'owner_driver_assigned': "👤 *Ронанда таъин шуд*\n\nБа мошини шумо `{car_brand}` (`{car_plate}`) ронандаи нав таъин шуд: {driver_name} (тел: `{driver_phone}`).",
        'owner_maintenance_completed': "✅ *Хизматрасонии техникӣ анҷом ёфт*\n\nКорҳои хизматрасонии техникии мошини шумо `{car_brand}` (`{car_plate}`) бомуваффақият анҷом ёфтанд.",
        'owner_license_due': "📄 *Иҷозатнома*\n\nДиққат! Мӯҳлати эътибори иҷозатнома барои фаъолияти таксӣ барои мошини шумо `{car_brand}` (`{car_plate}`) `{date}` ба охир мерасад.",
        'owner_payout_received': "💰 *Маблағ ворид шуд*\n\nБа ҳисоби шумо барои иҷораи мошини `{car_brand}` маблағи *{amount} сомонӣ* ворид карда шуд.",
        'owner_inspection_due': "📋 *Азназаргузаронии техникӣ*\n\nДиққат! Мӯҳлати азназаргузаронии техникии давлатии мошини шумо `{car_brand}` (`{car_plate}`) `{date}` ба охир мерасад.",
        
        'choose_rent_type': 'Лутфан, намуди иҷораро интихоб кунед:',
        'short_term': 'Кӯтоҳмуддат',
        'long_term': 'Дарозмуддат',
        'from_6_months': 'Аз 6 моҳ',
        'up_to_1_year': 'То 1 сол',
        'choose_number_of_days': "🔢 Миқдори рӯзҳои иҷораро интихоб кунед:", 
        'invalid_vin': "❗️*Хатогӣ.* VIN-код бояд аз 17 аломат иборат бошад (ҳарфҳои лотинӣ ва рақамҳо, ба ғайр аз I, O, Q). Лутфан, санҷида, дубора ворид кунед.",
        'edit_as_client_btn': "✍️ Ҳамчун Ронанда таҳрир кардан",
        'edit_as_owner_btn': "🚗 Ҳамчун Соҳибмулк таҳрир кардан",
        'not_a_client_error': "❗️ Шумо ҳамчун ронанда ба қайд гирифта нашудаед. Барои бақайдгирӣ /register -ро истифода баред.",
        'not_a_owner_error': "❗️ Шумо ҳамчун соҳибмулк ба қайд гирифта нашудаед. Барои бақайдгирӣ /owner_register -ро истифода баред.",
        'consent_prompt': "Бо пахш намудани тугмаи «Тасдиқ мекунам», ман розигии худро барои коркарди маълумоти шахсӣ ва гирифтани паёмакҳо (SMS) аз ширкати мо изҳор менамоям.\n\nЛутфан, розигии худро тасдиқ кунед.",
        'consent_confirm_btn': "✅ Тасдиқ мекунам",
        
        # --- "Сообщить об ошибке" (TJ) ---
        'admin_btn_report_error': "✉️ Дар бораи хатогӣ хабар додан",
        'admin_prompt_custom_error': "✍️ Матни паёмро барои истифодабаранда (ID: {target_id}) ворид кунед.\n\n_(Паёми шумо аз номи бот ба ӯ фиристода мешавад. Барои бекор кардан /cancel истифода баред)_",
        'admin_custom_error_sent': "✅ Паём ба истифодабарандаи {target_id} фиристода шуд.",
        'admin_select_error_reason': "Сабабро барои огоҳ кардани истифодабаранда интихоб кунед:",
        'error_reason_photo': "Аксҳои норавшани ҳуҷҷатҳо",
        'error_reason_mismatch': "Маълумот дар саволнома ва ҳуҷҷатҳо мувофиқ нест",
        'error_reason_other': "Дигар (шахсан тамос гирифтан)",
        'admin_btn_cancel_error': "Бекор кардан",
        'admin_error_reported': "✅ Огоҳӣ дар бораи хатогӣ ба истифодабаранда фиристода шуд.",
        'user_error_notification_prefix': "❗️ *Огоҳӣ оиди дархости бақайдгирии шумо*",
        'error_message_photo': "Салом! Мо дархости шуморо баррасӣ кардем. Мутаассифона, аксҳои ҳуҷҷатҳои шумо норавшан ё хира баромаданд. Лутфан, дар равшании хуб аксҳои нав ва равшан гирифта, бо мо тавассути /message дубора дар тамос шавед, то мо дархости шуморо нав кунем.",
        'error_message_mismatch': "Салом! Мо дархости шуморо баррасӣ кардем ва номувофиқатиро байни маълумоте, ки шумо дар саволнома ворид кардед (масалан, ННН ё рақами шаҳодатномаи ронандагӣ) ва маълумоти дар аксҳои ҳуҷҷатҳо буда мушоҳида кардем. Лутфан, маълумотро санҷед ва бо мо тавассути /message дар тамос шавед, то мо саволномаи шуморо ислоҳ кунем.",
        'error_message_other': "Салом! Оид ба дархости бақайдгирии шумо якчанд саволҳо пайдо шуданд. Лутфан, барои аниқ кардани тафсилот бо менеҷери мо тамос гиред. Маълумот барои тамос: /contact",
        'registration_rejected_client': "❌ Мутаассифона, дархости шумо рад карда шуд. Лутфан, камбудиҳоро ислоҳ кунед ва аз нав бо истифодаи фармони /register аз қайд гузаред.",
        'registration_rejected_owner': "❌ Мутаассифона, дархости шумо рад карда шуд. Лутфан, камбудиҳоро ислоҳ кунед ва аз нав ҳамчун соҳибмулк бо истифодаи фармони /owner_register аз қайд гузаред.",
        'registration_rejected_owner': "...",
        'city_selected': "✅ Интихоб шуд: {city_name}", 
        'category_selected': "✅ *Категория интихоб шуд: {category}*", 
        'choice_selected': "✅ Интихоб шуд.",
        'cancel_hint': "\n\n_(Барои бекор кардан /cancel-ро ворид кунед)_",
        'already_registered_as_driver': "✅ Шумо аллакай ҳамчун Ронанда ба қайд гирифта шудаед.",
        'transmission_auto': "Автомат", 
        'transmission_manual': "Механика", 
        'transmission_selected': "✅ *Интихоб шуд: {choice_text}*", 
        'main_menu_prompt': "👇 Амалро интихоб кунед:",
        'i_am_the_owner_btn': "👤 Ман худам соҳибмулк ҳастам",
   
    }
}


# --- ГЛАВНЫЕ НАСТРОЙКИ ---
TOKEN = os.environ.get('BOT_TOKEN') or '7820114439:AAFDaaUnkmCfJy7KFr4A1lO2PUXcX1HyH8k'
if not TOKEN:
    raise ValueError("❌ Переменная окружения BOT_TOKEN не найдена. Установите её перед запуском.")
MAIN_ADMIN_CHAT_ID = '5573234528'

# --- ФАЙЛЫ И КОНСТАНТЫ ---
BASE_URL = f'https://api.telegram.org/bot{TOKEN}'
USERS_FILE = 'users.json'
ADMIN_FILE = 'admin.json'
RENTALS_FILE = 'rentals.json'
CARS_FILE = 'cars.json'
OWNER_CARS_FILE = 'owner_cars.json'
STAFF_FILE = 'staff.json'


# Список городов для выбора при регистрации
CITIES = [
    {'key': 'khujand',   'ru': 'Худжанд',      'tj': 'Хуҷанд'},
    {'key': 'ghafurov',  'ru': 'Б. Гафуров',   'tj': 'Б. Ғафуров'},
    {'key': 'guliston',  'ru': 'Гулистан',     'tj': 'Гулистон'},
    {'key': 'istaravshan','ru': 'Истаравшан',   'tj': 'Истаравшан'},
    {'key': 'zafarobod', 'ru': 'Зафарабад',    'tj': 'Зафаробод'},
    {'key': 'asht',      'ru': 'Ашт',          'tj': 'Ашт'},
    {'key': 'rasulov',   'ru': 'Дж. Расулов',  'tj': 'Ҷ. Расулов'},
    {'key': 'konibodom', 'ru': 'Канибадам',    'tj': 'Конибодом'},
    {'key': 'spitamen',  'ru': 'Спитамен',     'tj': 'Спитамен'},
    {'key': 'istiqlol',  'ru': 'Истиклол',     'tj': 'Истиқлол'},
    {'key': 'buston',    'ru': 'Бустон',       'tj': 'Бустон'},
    {'key': 'shahriston','ru': 'Шахристан',    'tj': 'Шаҳристон'},
    {'key': 'devashtich','ru': 'Деваштич',     'tj': 'Деваштич'},
    {'key': 'ayni',      'ru': 'Айни',         'tj': 'Айнӣ'},
]

# Начальные данные для машин
INITIAL_CARS_DATA = {
    # --- Hyundai ---
    "Hyundai Avante": {
        "description": "Надёжный и экономичный седан, идеален для городской езды по Худжанду.",
        "local_image_paths": ["images/hyundai_avante.webp", "images/hyundai_avante.webp", "images/hyundai_avante.webp"],
        "prices": [250, 1500, 5000], "year": 2020, "fuel_type": "Бензин", "engine": "1.6L", "transmission": "Автомат", "seats": 5
    },
    "Hyundai Sonata": {
        "description": "Просторный и комфортабельный бизнес-седан с элегантным дизайном и богатым оснащением.",
        "local_image_paths": ["images/HyundaiSonata.jpg", "images/HyundaiSonata.jpg", "images/HyundaiSonata.jpg"],
        "prices": [380, 2300, 7500], "year": 2021, "fuel_type": "Бензин", "engine": "2.5L", "transmission": "Автомат", "seats": 5
    },
    "Hyundai Elantra": {
        "description": "Стильный и экономичный седан, который отлично подходит как для города, так и для трассы.",
        "local_image_paths": ["images/HyundaiElantra.jpg", "images/HyundaiElantra.jpg", "images/HyundaiElantra.jpg"],
        "prices": [300, 1800, 6000], "year": 2022, "fuel_type": "Бензин", "engine": "1.6L", "transmission": "Автомат", "seats": 5
    },
    # --- BYD ---
    "BYD Song Plus": {
        "description": "Современный электрический кроссовер с большим запасом хода. Отличный выбор для комфортных поездок.",
        "local_image_paths": ["images/BYDSongPlus.webp", "images/BYDSongPlus.webp", "images/BYDSongPlus.webp"],
        "prices": [380, 2280, 7600], "year": 2022, "fuel_type": "Электричество", "engine": "Электромотор 135 кВт", "transmission": "Автомат", "seats": 5
    },
    "BYD Dolphin": {
        "description": "Компактный и юркий городской электромобиль с уникальным дизайном. Идеален для ежедневных поездок.",
        "local_image_paths": ["images/BYDDolphin.jpg", "images/BYDDolphin.jpg", "images/BYDDolphin.jpg"],
        "prices": [320, 1900, 6500], "year": 2023, "fuel_type": "Электричество", "engine": "Электромотор 70 кВт", "transmission": "Автомат", "seats": 5
    },
    # --- Toyota ---
    "Toyota Camry": {
        "description": "Классика надёжности и комфорта. Подходит как для городских условий, так и для дальних поездок.",
        "local_image_paths": ["images/Toyota-Camry.jpg", "images/Toyota-Camry.jpg", "images/Toyota-Camry.jpg"],
        "prices": [360, 2160, 7200], "year": 2021, "fuel_type": "Бензин", "engine": "2.5L", "transmission": "Автомат", "seats": 5
    },
    "Toyota Corolla": {
        "description": "Легендарная надежность в современном исполнении. Комфортный и экономичный седан на все случаи жизни.",
        "local_image_paths": ["images/toyotaCorolla.webp", "images/toyotaCorolla.webp", "images/toyotaCorolla.webp"],
        "prices": [320, 1950, 6800], "year": 2020, "fuel_type": "Бензин", "engine": "1.8L", "transmission": "Автомат", "seats": 5
    },
    "Toyota Land Cruiser Prado": {
        "description": "Мощный и проходимый внедорожник для любых дорог. Сочетает в себе комфорт и впечатляющие возможности.",
        "local_image_paths": ["images/ToyotaPrado.jpg", "images/ToyotaPrado.jpg", "images/ToyotaPrado.jpg"],
        "prices": [700, 4500, 15000], "year": 2019, "fuel_type": "Дизель", "engine": "2.8L", "transmission": "Автомат", "seats": 7
    },
    # --- Opel ---
    "Opel Zafira": {
        "description": "Просторный и практичный минивэн, идеально подходит для семейных поездок или небольших групп.",
        "local_image_paths": ["images/opel.jpg", "images/opel.jpg", "images/opel.jpg"],
        "prices": [280, 1680, 5600], "year": 2019, "fuel_type": "Дизель", "engine": "2.0L", "transmission": "Механика", "seats": 7
    },
     "Opel Astra": {
        "description": "Немецкий хэтчбек с точным управлением и комфортной подвеской. Отличный выбор для активной езды.",
        "local_image_paths": ["images/OpeAstra.jpg", "images/OpeAstra.jpg", "images/OpeAstra.jpg"],
        "prices": [300, 1750, 5800], "year": 2019, "fuel_type": "Бензин", "engine": "1.4L Turbo", "transmission": "Автомат", "seats": 5
    },
    # --- Mercedes-Benz ---
    "Mercedes-Benz E-Class": {
        "description": "Премиальный седан для деловых поездок и особых случаев. Сочетание стиля, комфорта и немецкого качества.",
        "local_image_paths": ["images/MercedesE-Class.webp", "images/MercedesE-Class.webp", "images/MercedesE-Class.webp"],
        "prices": [500, 3200, 11000], "year": 2021, "fuel_type": "Бензин", "engine": "2.0L Turbo", "transmission": "Автомат", "seats": 5
    },
    "Mercedes-Benz C-Class": {
        "description": "Компактный представительский седан, сочетающий в себе роскошь старших моделей и спортивный характер.",
        "local_image_paths": ["images/MercedesC-Class.jpg","images/MercedesC-Class.jpg","images/MercedesC-Class.jpg"],
        "prices": [450, 2900, 10000], "year": 2022, "fuel_type": "Бензин", "engine": "2.0L Turbo", "transmission": "Автомат", "seats": 5
    },
    "Mercedes-Benz GLC": {
        "description": "Универсальный и роскошный кроссовер. Предлагает высокий уровень комфорта как в городе, так и за его пределами.",
        "local_image_paths": ["images/Mercedes-BenzGLC.png", "images/Mercedes-BenzGLC.png", "images/Mercedes-BenzGLC.png"],
        "prices": [650, 4200, 14000], "year": 2021, "fuel_type": "Бензин", "engine": "2.0L Turbo", "transmission": "Автомат", "seats": 5
    },
    # --- BMW ---
     "BMW 5 Series": {
        "description": "Динамичный и престижный седан...",
        "local_image_paths": ["images/BMW5Series.jpg", "images/BMW5Series.jpg", "images/BMW5Series.jpg"],
        "telegram_file_ids": [],
        "prices": [550, 3500, 12000],
        "year": 2020,
        "fuel_type": "Бензин",
        "engine": "2.0L TwinPower Turbo",
        "transmission": "Автомат",
        "seats": 5
    },
    "BMW 3 Series": {
        "description": "Эталон управляемости и спортивного духа в классе седанов. Автомобиль для тех, кто любит водить.",
        "local_image_paths": ["images/BMW3Series.jpg", "images/BMW3Series.jpg", "images/BMW3Series.jpg"],
        "prices": [480, 3100, 10500], "year": 2021, "fuel_type": "Бензин", "engine": "2.0L TwinPower Turbo", "transmission": "Автомат", "seats": 5
    },
    "BMW X5": {
        "description": "Большой и мощный кроссовер, который сочетает в себе спортивный характер BMW и практичность SUV.",
        "local_image_paths": ["images/bmw5.jpg", "images/bmw5.jpg", "images/bmw5.jpg"],
        "prices": [800, 5000, 17000], "year": 2020, "fuel_type": "Дизель", "engine": "3.0L", "transmission": "Автомат", "seats": 5
    },
    # --- Электромобили (Aion, Weltmeister) ---
    "Aion S": {
        "description": "Современный и технологичный электроседан с большим запасом хода. Экономичный и тихий, отличный выбор для города.",
        "local_image_paths": ["images/AionS.jpg", "images/AionS.jpg", "images/AionS.jpg"],
        "prices": [400, 2400, 8000], "year": 2023, "fuel_type": "Электричество", "engine": "Электромотор 150 кВт", "transmission": "Автомат", "seats": 5
    },
    "Aion Y": {
        "description": "Футуристичный и просторный компактвэн на электротяге. Необычный дизайн и огромный внутренний простор.",
        "local_image_paths": ["images/AionY.jpg", "images/AionY.jpg", "images/AionY.jpg"],
        "prices": [420, 2500, 8500], "year": 2023, "fuel_type": "Электричество", "engine": "Электромотор 135 кВт", "transmission": "Автомат", "seats": 5
    },
    "Weltmeister W6": {
        "description": "Инновационный электрический кроссовер с просторным салоном и богатым оснащением. Идеально подходит для семейных поездок.",
        "local_image_paths": ["images/WeltmeisterW6.jpeg", "images/WeltmeisterW6.jpeg", "images/WeltmeisterW6.jpeg"],
        "prices": [450, 2700, 9000], "year": 2022, "fuel_type": "Электричество", "engine": "Электромотор 160 кВт", "transmission": "Автомат", "seats": 5
    },
    "Weltmeister EX5": {
        "description": "Надежный и практичный электрический кроссовер с хорошим запасом хода. Проверенный выбор для семьи.",
        "local_image_paths": ["images/WeltmeisterEX5.jpg", "images/WeltmeisterEX5.jpg", "images/WeltmeisterEX5.jpg"],
        "prices": [430, 2600, 8800], "year": 2021, "fuel_type": "Электричество", "engine": "Электромотор 160 кВт", "transmission": "Автомат", "seats": 5
    },
    # --- Kia ---
    "Kia K5": {
        "description": "Стильный и современный седан с ярким дизайном и комфортным салоном. Отлично подходит для повседневных поездок.",
        "local_image_paths": ["images/KiaK5.jpg", "images/KiaK5.jpg", "images/KiaK5.jpg"],
        "prices": [350, 2100, 7000], "year": 2022, "fuel_type": "Бензин", "engine": "2.0L", "transmission": "Автомат", "seats": 5
    },
    "Kia Sportage": {
        "description": "Популярный кроссовер со смелым дизайном и современными технологиями. Универсальный автомобиль для города и путешествий.",
        "local_image_paths": ["images/KiaSportage.jpg", "images/KiaSportage.jpg", "images/KiaSportage.jpg"],
        "prices": [400, 2400, 8000], "year": 2022, "fuel_type": "Бензин", "engine": "2.0L", "transmission": "Автомат", "seats": 5
    },
    "Kia Rio": {
        "description": "Компактный и надежный седан, один из лидеров в своем классе. Низкий расход топлива и доступная цена аренды.",
        "local_image_paths": ["images/KiaRio.webp", "images/KiaRio.webp", "images/KiaRio.webp"],
        "prices": [280, 1600, 5500], "year": 2021, "fuel_type": "Бензин", "engine": "1.6L", "transmission": "Автомат", "seats": 5
    },
    # --- Lexus ---
    "Lexus RX 350": {
        "description": "Надежный и комфортабельный премиум-кроссовер. Плавность хода и высокое качество материалов делают каждую поездку приятной.",
        "local_image_paths": ["images/LexusRX350.jpg", "images/LexusRX350.jpg", "images/LexusRX350.jpg"],
        "prices": [600, 3800, 13000], "year": 2020, "fuel_type": "Бензин", "engine": "3.5L V6", "transmission": "Автомат", "seats": 5
    },
    "Lexus ES 350": {
        "description": "Роскошный седан, известный своим непревзойденным комфортом и плавностью хода. Идеален для дальних поездок.",
        "local_image_paths": ["images/LexusES350.webp", "images/LexusES350.webp", "images/LexusES350.webp"],
        "prices": [580, 3700, 12500], "year": 2021, "fuel_type": "Бензин", "engine": "3.5L V6", "transmission": "Автомат", "seats": 5
    },
    # --- Volkswagen ---
    "Volkswagen Passat": {
        "description": "Практичный и вместительный немецкий седан. Отличный баланс между комфортом, управляемостью и экономичностью.",
        "local_image_paths": ["images/VolkswagenPassat.jpg", "images/VolkswagenPassat.jpg", "images/VolkswagenPassat.jpg"],
        "prices": [340, 2000, 6800], "year": 2019, "fuel_type": "Бензин", "engine": "1.8L TSI", "transmission": "Автомат", "seats": 5
    },
    "Volkswagen Jetta": {
        "description": "Надежный и практичный седан с просторным салоном и большим багажником. Немецкое качество по доступной цене.",
        "local_image_paths": ["images/VolkswagenJetta.avif", "images/VolkswagenJetta.avif", "images/VolkswagenJetta.avif"],
        "prices": [320, 1900, 6500], "year": 2020, "fuel_type": "Бензин", "engine": "1.4L TSI", "transmission": "Автомат", "seats": 5
    },
    "Volkswagen Tiguan": {
        "description": "Сбалансированный и технологичный кроссовер. Отличное сочетание комфорта, практичности и управляемости.",
        "local_image_paths": ["images/VolkswagenTiguan.webp", "images/VolkswagenTiguan.webp", "images/VolkswagenTiguan.webp"],
        "prices": [450, 2800, 9500], "year": 2020, "fuel_type": "Бензин", "engine": "2.0L TSI", "transmission": "Автомат", "seats": 5
    },
    # --- Audi ---
    "Audi A6": {
        "description": "Элегантный бизнес-седан с передовыми технологиями и безупречным интерьером. Подчеркнет ваш статус и обеспечит комфорт.",
        "local_image_paths": ["images/AudiA6.webp", "images/AudiA6.webp", "images/AudiA6.webp"],
        "prices": [520, 3300, 11500], "year": 2020, "fuel_type": "Бензин", "engine": "2.0L TFSI", "transmission": "Автомат", "seats": 5
    },
    "Audi A4": {
        "description": "Премиальный седан с выверенным дизайном, высококачественным интерьером и отличной управляемостью.",
        "local_image_paths": ["images/AudiA4.avif", "images/AudiA4.avif", "images/AudiA4.avif"],
        "prices": [490, 3150, 10800], "year": 2021, "fuel_type": "Бензин", "engine": "2.0L TFSI", "transmission": "Автомат", "seats": 5
    },
    "Audi Q5": {
        "description": "Стильный и комфортабельный кроссовер премиум-класса. Идеальный баланс между размером, мощностью и технологиями.",
        "local_image_paths": ["images/AudiQ5.jpg", "images/AudiQ5.jpg", "images/AudiQ5.jpg"],
        "prices": [680, 4300, 14500], "year": 2020, "fuel_type": "Бензин", "engine": "2.0L TFSI", "transmission": "Автомат", "seats": 5
    },
    # --- Daewoo ---
    "Daewoo Gentra": {
        "description": "Простой, надежный и очень доступный седан. Идеальный 'рабочий' автомобиль, неприхотливый в обслуживании.",
        "local_image_paths": ["images/DaewooGentra.webp", "images/DaewooGentra.webp", "images/DaewooGentra.webp"],
        "prices": [200, 1200, 4000], "year": 2018, "fuel_type": "Бензин", "engine": "1.5L", "transmission": "Механика", "seats": 5
    },
    "Daewoo Nexia R3": {
        "description": "Обновленная версия популярного бюджетного седана. Простой, недорогой и экономичный автомобиль для работы.",
        "local_image_paths": ["images/DaewooNexiaR3.jpg", "images/DaewooNexiaR3.jpg", "images/DaewooNexiaR3.jpg"],
        "prices": [220, 1300, 4200], "year": 2019, "fuel_type": "Бензин", "engine": "1.5L", "transmission": "Автомат", "seats": 5
    },
}

class State(Enum):
    # Состояния для клиента
    AWAITING_NAME = 1
    AWAITING_PHONE = 2
    AWAITING_CITY = 3
    AWAITING_AGE = 4
    AWAITING_DRIVING_EXPERIENCE = 5
    AWAITING_LICENSE = 6
    AWAITING_LICENSE_CATEGORY = 7
    AWAITING_PREFERRED_CAR_TYPE = 8
    AWAITING_TAXI_EXPERIENCE = 9
    AWAITING_DRIVER_ID = 10
    AWAITING_DEPOSIT = 11
    AWAITING_PASSPORT_PHOTO = 12

    AWAITING_RENT_CAR = 13
    AWAITING_RENT_PERIOD = 14
    
    # Состояния для админа
    AWAITING_BROADCAST_CONFIRMATION = 15
    AWAITING_NEW_CAR_NAME = 16
    AWAITING_NEW_CAR_DESCRIPTION = 17
    AWAITING_NEW_CAR_PHOTO = 18
    AWAITING_NEW_CAR_PRICES = 19
    AWAITING_EDIT_CAR_SELECT = 20
    AWAITING_EDIT_CAR_FIELD = 21
    AWAITING_EDIT_CAR_VALUE = 22

    # Состояния для регистрации собственника
    AWAITING_OWNER_REGISTRATOR_NAME = 30
    AWAITING_OWNER_NAME = 31
    AWAITING_OWNER_PHONE = 32
    AWAITING_OWNER_CAR_BRAND = 33
    AWAITING_OWNER_CAR_PLATE = 34
    AWAITING_OWNER_CAR_VIN = 35
    AWAITING_OWNER_CAR_YEAR = 36
    AWAITING_OWNER_CAR_MILEAGE = 37
    AWAITING_OWNER_CAR_TRANSMISSION = 38
    AWAITING_OWNER_CAR_INSURANCE = 39
    AWAITING_OWNER_GPS_CAMERA = 40
    AWAITING_OWNER_CAR_COLOR = 41
    AWAITING_OWNER_CAR_TYPE = 42
    AWAITING_SERVICE_REQUEST = 43

    # Состояния для редактирования профиля
    AWAITING_PROFILE_EDIT_CHOICE = 44
    AWAITING_PROFILE_EDIT_VALUE = 45
    AWAITING_ADDRESS = 46
    AWAITING_PROFILE_EDIT_CITY = 47

    AWAITING_LICENSE_FRONT_PHOTO = 50
    AWAITING_LICENSE_BACK_PHOTO = 51

    #состояния для фото собственника
    AWAITING_TECH_PASSPORT_PHOTO = 52
    AWAITING_OWNER_PASSPORT_PHOTO = 53
    
    AWAITING_CONSENT = 54 
    AWAITING_PASSPORT_BACK_PHOTO = 55 #состояние для обратной стороны паспорта
    
    # --- СОСТОЯНИЯ ДЛЯ СОБСТВЕННИКА ---
    AWAITING_TECH_PASSPORT_BACK_PHOTO = 56
    AWAITING_OWNER_PASSPORT_BACK_PHOTO = 57
    AWAITING_RENT_TYPE = 58
    
    # --- СОСТОЯНИЕ ДЛЯ ЧЕК-ЛИСТОВ ---
    AWAITING_CHECKLIST_INPUT = 59
    
    AWAITING_OWNER_CAR_PHOTOS = 60
    AWAITING_OWNER_DESIRED_PRICE = 61
    
    AWAITING_CUSTOM_ERROR_MESSAGE = 62 
    IN_CHAT_WITH_MANAGER = 63
    ADMIN_IN_CHAT_WITH_USER = 64
    
    
def load_from_json(filename):
    if not os.path.exists(filename): return {}
    try:
        with open(filename, 'r', encoding='utf-8') as f: return json.load(f)
    except (json.JSONDecodeError, FileNotFoundError):
        print(f"Ошибка чтения или декодирования {filename}. Возвращаем пустой словарь.")
        return {}

def save_to_json(data, filename):
    try:
        with open(filename, 'w', encoding='utf-8') as f: json.dump(data, f, ensure_ascii=False, indent=4)
    except IOError as e: print(f"Ошибка записи в файл {filename}: {e}")

def send_request(method, payload, files=None):
    url = f'{BASE_URL}/{method}'
    try:
        if files:
            # Увеличиваем время ожидания до 60 секунд для загрузки файлов
            response = requests.post(url, data=payload, files=files, timeout=60)
        else:
            # Для обычных запросов оставляем 20 секунд
            response = requests.post(url, json=payload, timeout=20)
        
        # Проверяем статус ответа и пытаемся вернуть JSON в любом случае
        json_response = response.json()
        if not json_response.get('ok'):
             print(f"Telegram API Error ({method}): {json_response.get('description')}")
        return json_response

    except requests.exceptions.RequestException as e:
        print(f"Network/HTTP Error ({method}): {e}")
        # Если есть ответ с ошибкой, пытаемся вернуть его тело
        if e.response is not None:
            try:
                return e.response.json()
            except json.JSONDecodeError:
                pass
        return None

def send_message(chat_id, text, reply_markup=None, parse_mode='Markdown', disable_web_page_preview=False):
    payload = {'chat_id': chat_id, 'text': text, 'parse_mode': parse_mode, 'disable_web_page_preview': disable_web_page_preview}
    if reply_markup: payload['reply_markup'] = reply_markup
    return send_request('sendMessage', payload)

def send_photo(chat_id, photo, caption, reply_markup=None, parse_mode='Markdown'):
    payload = {'chat_id': chat_id, 'caption': caption, 'parse_mode': parse_mode}
    if reply_markup:
        payload['reply_markup'] = json.dumps(reply_markup)

    # Попытаемся отправить до 3 раз в случае сетевой ошибки
    for attempt in range(3):
        files = None
        photo_file = None
        try:
            # Если это путь к локальному файлу
            if os.path.exists(str(photo)):
                photo_file = open(photo, 'rb')
                files = {'photo': photo_file}
                # Отправляем запрос
                response = send_request('sendPhoto', payload, files=files)
            # Если это file_id
            else:
                payload['photo'] = photo
                response = send_request('sendPhoto', payload)

            # Если ответ успешный, выходим из цикла
            if response and response.get('ok'):
                return response
            
            # Если неуспешно, выводим ошибку и ждем перед новой попыткой
            print(f"Попытка {attempt + 1} отправить фото не удалась. Ошибка: {response}")
            time.sleep(2) # Пауза 2 секунды перед повтором

        except Exception as e:
            print(f"Критическая ошибка на попытке {attempt + 1} при отправке фото: {e}")
            time.sleep(2) # Пауза 2 секунды

        finally:
            # Гарантированно закрываем файл после каждой попытки
            if photo_file:
                photo_file.close()

    # Если все попытки провалились
    print(f"Не удалось отправить фото в чат {chat_id} после 3 попыток.")
    # Как запасной вариант, отправляем только текст
    return send_message(chat_id, caption, parse_mode=parse_mode, reply_markup=reply_markup)


def send_video(chat_id, video_file_id, caption=None, parse_mode='Markdown'):
    """Отправляет видео по его file_id."""
    payload = {
        'chat_id': chat_id,
        'video': video_file_id,
        'caption': caption,
        'parse_mode': parse_mode
    }
    return send_request('sendVideo', payload)

def send_voice(chat_id, voice_file_id, caption=None, parse_mode='Markdown'):
    """Отправляет голосовое сообщение по его file_id."""
    payload = {'chat_id': chat_id, 'voice': voice_file_id, 'caption': caption, 'parse_mode': parse_mode}
    return send_request('sendVoice', payload)

def send_document(chat_id, document_file_id, caption=None, parse_mode='Markdown'):
    """Отправляет документ по его file_id."""
    payload = {'chat_id': chat_id, 'document': document_file_id, 'caption': caption, 'parse_mode': parse_mode}
    return send_request('sendDocument', payload)

def send_animation(chat_id, animation_file_id, caption=None, parse_mode='Markdown'):
    """Отправляет GIF (анимацию) по его file_id."""
    payload = {'chat_id': chat_id, 'animation': animation_file_id, 'caption': caption, 'parse_mode': parse_mode}
    return send_request('sendAnimation', payload)

def send_sticker(chat_id, sticker_file_id):
    """Отправляет стикер по его file_id."""
    payload = {'chat_id': chat_id, 'sticker': sticker_file_id}
    return send_request('sendSticker', payload)


import json 
def send_media_group(chat_id, photo_paths, caption=None):
    """Отправляет группу фотографий как один медиа-альбом с 3 попытками."""
    
    # Попытаемся отправить до 3 раз
    for attempt in range(3):
        media_payload = []
        files_to_upload = {}
        photo_paths_to_send = photo_paths[:10] # Ограничение Telegram

        try:
            # Подготовка файлов и JSON-полезной нагрузки
            for i, path in enumerate(photo_paths_to_send):
                if not path or not os.path.exists(path):
                    print(f"Файл не найден, пропускаем: {path}")
                    continue
                
                attachment_name = f'photo{i}'
                media_item = {'type': 'photo', 'media': f'attach://{attachment_name}'}

                if not media_payload and caption: # Добавляем подпись к первому фото
                    media_item['caption'] = caption
                    media_item['parse_mode'] = 'Markdown'
                
                media_payload.append(media_item)
                files_to_upload[attachment_name] = open(path, 'rb')

            if not files_to_upload:
                print("Не найдено файлов для медиа-группы.")
                if caption: send_message(chat_id, caption)
                return None

            payload = {'chat_id': str(chat_id), 'media': json.dumps(media_payload)}
            
            # Отправка запроса
            response = send_request('sendMediaGroup', payload, files=files_to_upload)

            # Если успешно, выходим из цикла
            if response and response.get('ok'):
                return response
            
            print(f"Попытка {attempt + 1} отправить медиагруппу не удалась. Ошибка: {response}")
            time.sleep(2) # Пауза перед повтором

        except Exception as e:
            print(f"Критическая ошибка на попытке {attempt + 1} при отправке медиагруппы: {e}")
            time.sleep(2)

        finally:
            # Гарантированно закрываем все открытые файлы
            for f in files_to_upload.values():
                f.close()

    # Если все попытки провалились, отправляем только текст в качестве запасного варианта
    print(f"Не удалось отправить медиагруппу в чат {chat_id} после 3 попыток.")
    if caption:
        return send_message(chat_id, caption)
    return None

def send_media_group_mixed(chat_id, media_items, caption=None):
    """
    Отправляет смешанную группу медиа, обрабатывая и file_id, и локальные пути.
    Возвращает список полученных file_id в случае успеха.
    """
    for attempt in range(3): # Добавляем 3 попытки
        media_payload = []
        files_to_upload = {}
        
        try:
            # Ограничение Telegram - не больше 10 медиа в группе
            items_to_send = media_items[:10]

            for i, item in enumerate(items_to_send):
                if not item: continue

                media_spec = {'type': 'photo'}
                
                # Если это локальный путь, готовим его к загрузке
                if isinstance(item, str) and os.path.exists(item):
                    attachment_name = f'photo{i}'
                    media_spec['media'] = f'attach://{attachment_name}'
                    files_to_upload[attachment_name] = open(item, 'rb')
                # Иначе считаем, что это file_id
                else:
                    media_spec['media'] = item

                if i == 0 and caption:
                    media_spec['caption'] = caption
                    media_spec['parse_mode'] = 'Markdown'

                media_payload.append(media_spec)

            if not media_payload:
                print("В медиа-группе нет элементов для отправки.")
                if caption: send_message(chat_id, caption)
                return None

            payload = {'chat_id': str(chat_id), 'media': json.dumps(media_payload)}
            response = send_request('sendMediaGroup', payload, files=files_to_upload)

            # Если отправка успешна, извлекаем и возвращаем file_id
            if response and response.get('ok'):
                sent_messages = response.get('result', [])
                file_ids = [msg.get('photo', [{}])[-1].get('file_id') for msg in sent_messages if msg.get('photo')]
                return file_ids
            
            print(f"Попытка {attempt + 1} не удалась. Ошибка: {response}")
            time.sleep(2)

        except Exception as e:
            print(f"Критическая ошибка на попытке {attempt + 1} при отправке медиагруппы: {e}")
            time.sleep(2)
        finally:
            for f in files_to_upload.values():
                f.close()
    
    # Если все попытки провалились
    print(f"Не удалось отправить медиагруппу в чат {chat_id} после 3 попыток.")
    if caption:
        return send_message(chat_id, caption)
    return None


def edit_message_text(chat_id, message_id, text, reply_markup=None, parse_mode='Markdown'):
    payload = {'chat_id': chat_id, 'message_id': message_id, 'text': text, 'parse_mode': parse_mode}
    if reply_markup: payload['reply_markup'] = reply_markup
    return send_request('editMessageText', payload)

def delete_message(chat_id, message_id):
    payload = {'chat_id': chat_id, 'message_id': message_id}
    return send_request('deleteMessage', payload)

def create_keyboard(buttons, one_time=True, inline=False):
    if inline:
        processed_buttons = []
        for row in buttons:
            processed_row = []
            for btn in row:
                if isinstance(btn, dict):
                    processed_row.append(btn)
                else:
                    processed_row.append({'text': btn, 'callback_data': btn})
            processed_buttons.append(processed_row)
        return {'inline_keyboard': processed_buttons}

    return {'keyboard': buttons, 'one_time_keyboard': one_time, 'resize_keyboard': True}


def get_main_menu_keyboard(chat_id):
    """Возвращает клавиатуру главного меню в зависимости от языка пользователя."""
    buttons = [
        [get_text('menu_rent', chat_id)],
        [get_text('menu_register_client', chat_id), get_text('menu_register_owner', chat_id)],
        [get_text('menu_list_cars', chat_id), get_text('menu_my_rentals', chat_id)],
        # --- СТРОКА НИЖЕ ИЗМЕНЕНА ---
        [get_text('menu_profile', chat_id), get_text('menu_language', chat_id)],
        [get_text('menu_chat_manager', chat_id), get_text('menu_contact', chat_id)],
        [get_text('menu_help', chat_id)]
    ]
    return create_keyboard(buttons, one_time=False)

def get_cancel_keyboard(chat_id):
    """Создает клавиатуру с кнопкой отмены."""
    return create_keyboard(
        [[get_text('cancel_button', chat_id)]],
        one_time=False,
        inline=False
    )

def get_admins():
    admins_data = load_from_json(ADMIN_FILE)
    return [str(chat_id) for chat_id in admins_data.values()]

def get_staff():
    staff_data = load_from_json(STAFF_FILE)
    return staff_data

def find_user_by_identifier(identifier):
    users = load_from_json(USERS_FILE)
    if identifier.isdigit() and identifier in users: return identifier, users[identifier]
    clean_username = identifier.lstrip('@')
    for chat_id, user_data in users.items():
        if user_data.get('username', '').lower() == clean_username.lower(): return chat_id, user_data
    return None, None

def get_available_cars(): return load_from_json(CARS_FILE)

def get_user_lang(chat_id):
    users = load_from_json(USERS_FILE)
    return users.get(str(chat_id), {}).get('lang', 'ru')

def get_text(key, chat_id, **kwargs):
    lang = get_user_lang(str(chat_id))
    text_template = TEXTS.get(lang, TEXTS['ru']).get(key)
    if text_template is None:
        text_template = TEXTS['ru'].get(key, f"_{key}_ not found")
    return text_template.format(**kwargs)

def escape_markdown(text: str) -> str:
    """Экранирует специальные символы для Telegram Markdown."""
    if not isinstance(text, str):
        text = str(text)
    escape_chars = r'_*`['
    for char in escape_chars:
        text = text.replace(char, f'\\{char}')
    return text


class NotificationManager:
    """
    Класс для управления и отправки push-уведомлений водителям и собственникам.
    """
    def send_rental_payment_due(self, driver_chat_id, car_info):
        message = get_text('driver_rent_payment_due', driver_chat_id, car_brand=car_info.get('car_brand', 'N/A'))
        send_message(driver_chat_id, message)
        print(f"Отправлено уведомление об оплате водителю {driver_chat_id}")

    def send_maintenance_due(self, driver_chat_id, car_info):
        is_electro = car_info.get('car_type', '').lower() == 'электромобиль'
        key = 'driver_maintenance_due_electro' if is_electro else 'driver_maintenance_due_dvs'
        message = get_text(key, driver_chat_id, 
                           car_brand=car_info.get('car_brand', 'N/A'),
                           car_plate=car_info.get('car_plate', 'N/A'))
        send_message(driver_chat_id, message)
        print(f"Отправлено уведомление о ТО водителю {driver_chat_id}")
        
    def send_inspection_due(self, chat_id, car_info, role='driver'):
        key = 'driver_inspection_due' if role == 'driver' else 'owner_inspection_due'
        expiry_date = car_info.get('inspection_expiry_date', '??.??.????')
        message = get_text(key, chat_id,
                           car_brand=car_info.get('car_brand', 'N/A'),
                           car_plate=car_info.get('car_plate', 'N/A'),
                           date=expiry_date)
        send_message(chat_id, message)
        print(f"Отправлено уведомление о техосмотре {role} {chat_id}")

    def send_license_due(self, chat_id, car_info, role='driver'):
        key = 'driver_license_due' if role == 'driver' else 'owner_license_due'
        expiry_date = car_info.get('license_expiry_date', '??.??.????')
        message = get_text(key, chat_id,
                           car_brand=car_info.get('car_brand', 'N/A'),
                           car_plate=car_info.get('car_plate', 'N/A'),
                           date=expiry_date)
        send_message(chat_id, message)
        print(f"Отправлено уведомление о лицензии {role} {chat_id}")
        
    def notify_all_drivers_about_free_car(self, car_info):
        all_users = load_from_json(USERS_FILE)
        driver_ids = [uid for uid, udata in all_users.items() if udata.get('role') == 'client' and udata.get('status') == 'active']
        
        for driver_id in driver_ids:
            message = get_text('driver_car_is_free', driver_id, car_brand=car_info.get('car_brand', 'N/A'))
            send_message(driver_id, message)
            time.sleep(0.1)
        print(f"Отправлено уведомление о свободной машине {car_info.get('car_brand')} {len(driver_ids)} водителям.")

    def notify_owner_of_driver_assignment(self, owner_chat_id, driver_info, car_info):
        message = get_text('owner_driver_assigned', owner_chat_id,
                           car_brand=car_info.get('car_brand', 'N/A'),
                           car_plate=car_info.get('car_plate', 'N/A'),
                           driver_name=driver_info.get('name', 'N/A'),
                           driver_phone=driver_info.get('phone', 'N/A'))
        send_message(owner_chat_id, message)
        print(f"Отправлено уведомление о назначении водителя собственнику {owner_chat_id}")

    def notify_owner_of_maintenance_completion(self, owner_chat_id, car_info):
        message = get_text('owner_maintenance_completed', owner_chat_id,
                           car_brand=car_info.get('car_brand', 'N/A'),
                           car_plate=car_info.get('car_plate', 'N/A'))
        send_message(owner_chat_id, message)
        print(f"Отправлено уведомление о завершении ТО собственнику {owner_chat_id}")

    def notify_owner_of_payout(self, owner_chat_id, car_info, amount):
        message = get_text('owner_payout_received', owner_chat_id,
                           car_brand=car_info.get('car_brand', 'N/A'),
                           amount=amount)
        send_message(owner_chat_id, message)
        print(f"Отправлено уведомление о выплате {amount} сомони собственнику {owner_chat_id}")

def daily_scheduler_check():
    """
    Проверяет даты и отправляет нужные уведомления.
    """
    print(f"\n[{datetime.now()}] --- Запуск ежедневной проверки по расписанию ---")
    notifications = NotificationManager()
    owner_cars = load_from_json(OWNER_CARS_FILE)
    
    today = datetime.now().date()
    
    for owner_id, car_data in owner_cars.items():
        if 'inspection_expiry_date' in car_data:
            try:
                expiry_date = datetime.strptime(car_data['inspection_expiry_date'], '%Y-%m-%d').date()
                days_left = (expiry_date - today).days
                if days_left in [15, 3]:
                    notifications.send_inspection_due(owner_id, car_data, role='owner')
                    if car_data.get('current_driver_id'):
                        notifications.send_inspection_due(car_data['current_driver_id'], car_data, role='driver')
            except (ValueError, TypeError):
                pass 

        if 'license_expiry_date' in car_data:
            try:
                expiry_date = datetime.strptime(car_data['license_expiry_date'], '%Y-%m-%d').date()
                days_left = (expiry_date - today).days
                if days_left in [30, 7, 3]:
                    notifications.send_license_due(owner_id, car_data, role='owner')
                    if car_data.get('current_driver_id'):
                        notifications.send_license_due(car_data['current_driver_id'], car_data, role='driver')
            except (ValueError, TypeError):
                pass
    
    print(f"[{datetime.now()}] --- Ежедневная проверка завершена ---\n")


class CarRentalBot:
    
    def get_back_cancel_keyboard(self, chat_id):
        """Создает клавиатуру с кнопками 'Назад' и 'Отмена'."""
        return create_keyboard(
            [
                [get_text('back_button', chat_id), get_text('cancel_button', chat_id)]
            ],
            one_time=False,
            inline=False
        )

    def _go_back(self, chat_id):
        """Возвращает пользователя к предыдущему шагу регистрации."""
        data = self.user_data.get(chat_id, {})
        previous_states = data.get('state_history', [])

        if not previous_states:
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            send_message(chat_id, get_text('operation_cancelled', chat_id),
                         reply_markup=get_main_menu_keyboard(chat_id))
            return

        previous_states.pop()
        if not previous_states:
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            send_message(chat_id, get_text('operation_cancelled', chat_id),
                         reply_markup=get_main_menu_keyboard(chat_id))
            return

        prev_state_name = previous_states[-1]
        self.user_states[chat_id] = State[prev_state_name]

        state_actions = {
            State.AWAITING_NAME: (get_text('start_registration', chat_id), get_cancel_keyboard(chat_id)),
            State.AWAITING_PHONE: (get_text('ask_phone', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_CITY: (get_text('ask_city', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_ADDRESS: (get_text('ask_address', chat_id, city=data.get('city', '')), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_AGE: (get_text('ask_age', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_DRIVING_EXPERIENCE: (get_text('ask_driving_experience', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_LICENSE: (get_text('ask_license', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_DRIVER_ID: (get_text('ask_driver_id', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_PASSPORT_PHOTO: (get_text('ask_passport_photo', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_REGISTRATOR_NAME: (get_text('owner_start_registration', chat_id), get_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_NAME: (get_text('owner_ask_owner_name', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_PHONE: (get_text('ask_phone', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_CAR_BRAND: (get_text('owner_ask_car_brand', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_CAR_PLATE: (get_text('owner_ask_car_plate', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_CAR_VIN: (get_text('owner_ask_car_vin', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_CAR_YEAR: (get_text('owner_ask_car_year', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_CAR_MILEAGE: (get_text('owner_ask_car_mileage', chat_id), self.get_back_cancel_keyboard(chat_id)),
            State.AWAITING_OWNER_CAR_COLOR: (get_text('owner_ask_car_color', chat_id), self.get_back_cancel_keyboard(chat_id)),
        }
        
        action = state_actions.get(State[prev_state_name])
        if action:
            send_message(chat_id, action[0], reply_markup=action[1])
        else:
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            send_message(chat_id, get_text('operation_cancelled', chat_id),
                         reply_markup=get_main_menu_keyboard(chat_id))

    def _set_next_state(self, chat_id, next_state):
        """Устанавливает следующее состояние и сохраняет историю."""
        self.user_states[chat_id] = next_state
        data = self.user_data.get(chat_id, {})
        if 'state_history' not in data:
            data['state_history'] = []
        
        data['state_history'].append(next_state.name)
        self.user_data[chat_id] = data
    
    def __init__(self):
        self.user_states = {}
        self.user_data = {}
        self.notifications = NotificationManager()
        self._initialize_files()
        
    def _get_user_display_name(self, user_info):
        """Находит и возвращает лучшее имя для пользователя из его профиля."""
        if not user_info:
            return "Пользователь"
        
        if 'client_profile' in user_info and user_info['client_profile'].get('name'):
            return user_info['client_profile']['name']
            
        if 'owner_profile' in user_info and user_info['owner_profile'].get('owner_name'):
            return user_info['owner_profile']['owner_name']
        if 'owner_profile' in user_info and user_info['owner_profile'].get('registrator_name'):
            return user_info['owner_profile']['registrator_name']

        if user_info.get('name'):
            return user_info['name']
            
        return "Пользователь" 

    def _initialize_files(self):
        if not os.path.exists(ADMIN_FILE):
            save_to_json({'main_admin': MAIN_ADMIN_CHAT_ID}, ADMIN_FILE)
        if not os.path.exists(STAFF_FILE):
            save_to_json({}, STAFF_FILE)
        if not os.path.exists(CARS_FILE) or not load_from_json(CARS_FILE):
            if not os.path.exists('images'): os.makedirs('images')
            for car_name, car_info in INITIAL_CARS_DATA.items():
                image_paths = car_info.get('local_image_paths', [])

                for image_path in image_paths:
                    if not image_path:
                        continue

                    img_dir = os.path.dirname(image_path)
                    
                    if img_dir and not os.path.exists(img_dir):
                        os.makedirs(img_dir)

                    if not os.path.exists(image_path):
                        try:
                            with open(image_path, 'w') as f:
                                f.write(f"Placeholder for {car_name}")
                        except IOError as e:
                            print(f"Не удалось создать файл-заглушку {image_path}: {e}")
            
            save_to_json(INITIAL_CARS_DATA, CARS_FILE)
        for filename in [USERS_FILE, RENTALS_FILE, OWNER_CARS_FILE]:
            if not os.path.exists(filename): save_to_json({}, filename)

    def process_update(self, update):
        if 'inline_query' in update:
            self._handle_inline_query(update['inline_query'])
            return

        if 'callback_query' in update: 
            self._process_callback_query(update['callback_query'])
            return
            
        if 'message' not in update: return
        
        message = update['message']
        chat_id = str(message['chat']['id'])
        user = message['from']
        
        text = message.get('text', '').strip()
        state = self.user_states.get(chat_id)

        # --- НАЧАЛО ИСПРАВЛЕНИЯ ---
        
        # Собираем все возможные тексты кнопок, которые должны работать как команды
        command_button_texts = [
            get_text('menu_rent', chat_id),
            get_text('menu_list_cars', chat_id),
            get_text('menu_my_rentals', chat_id),
            get_text('menu_profile', chat_id),
            get_text('menu_contact', chat_id),
            get_text('menu_help', chat_id),
            get_text('menu_register_client', chat_id),
            get_text('menu_register_owner', chat_id),
            get_text('menu_chat_manager', chat_id),
            #get_text('end_chat_button', chat_id),
            get_text('cancel_button', chat_id)
        ]
        
        # ПРОВЕРКА: является ли сообщение командой ИЛИ нажатием на одну из командных кнопок
        is_command_or_button = text.startswith('/') or text in command_button_texts
        if is_command_or_button:
            # Если во время активного состояния приходит команда, старое состояние нужно прервать.
            if state:
                # --- НАЧАЛО ИСПРАВЛЕНИЙ ---
                data = self.user_data.get(chat_id, {})
                edit_menu_message_id = data.get('edit_menu_message_id')

                # ПРОВЕРЯЕМ: если мы были в состоянии редактирования профиля и у нас есть ID сообщения с кнопками
                if state in [State.AWAITING_PROFILE_EDIT_CHOICE, State.AWAITING_PROFILE_EDIT_VALUE] and edit_menu_message_id:
                    # Редактируем то самое сообщение, чтобы убрать клавиатуру
                    edit_message_text(
                        chat_id=chat_id,
                        message_id=edit_menu_message_id,
                        text=get_text('choose_field_to_edit', chat_id) + f"\n\n*{get_text('operation_cancelled', chat_id)}*",
                        reply_markup=None  # Это убирает клавиатуру
                    )
                else:
                    # Для всех других состояний (не редактирование профиля) работает старая логика отмены
                    is_cancel_command = (text == get_text('cancel_button', chat_id)) or text == '/cancel'
                    if not is_cancel_command:
                        send_message(chat_id, get_text('operation_cancelled', chat_id))

                # В любом случае очищаем состояние и временные данные
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                # --- КОНЕЦ ИСПРАВЛЕНИЙ ---
                
            # Вызываем главный обработчик команд (например, чтобы показать контакты после нажатия на кнопку)
            self._handle_commands(chat_id, text, user)
            return
        
        # Если это не команда, а у пользователя есть активное состояние, обрабатываем его
        if state:
            self._handle_stateful_message(message)
            return
        
        # Если состояний нет, и это не команда, считаем это неизвестной командой
        self._handle_commands(chat_id, text, user)
        
    def _handle_inline_query(self, inline_query):
        """
        Обрабатывает входящие инлайн-запросы для поиска автомобилей.
        """
        query_id = inline_query['id']
        query_text = inline_query.get('query', '').lower().strip()
        
        available_cars = get_available_cars()
        results = []
        
        if not query_text:
            cars_to_show = list(available_cars.items())[:5]
        else:
            search_words = query_text.split()
            cars_to_show = [
                (name, details) for name, details in available_cars.items()
                if all(word in name.lower() for word in search_words)
            ]

        for i, (name, details) in enumerate(cars_to_show):
            description = (f"Год: {details.get('year', 'N/A')}, "
                           f"Топливо: {details.get('fuel_type', 'N/A')}, "
                           f"Цена за день: {details.get('prices', [0])[0]} сомони")
            
            message_text = f"/rentcar {name}" 

            result_item = {
                'type': 'article',
                'id': str(i),
                'title': name,
                'description': description,
                'input_message_content': {
                    'message_text': f"/selectcar {name}"
                },

            }
            results.append(result_item)

        send_request('answerInlineQuery', {'inline_query_id': query_id, 'results': results, 'cache_time': 5})

    def _handle_client_photo_step(self, chat_id, file_id, current_state):
        """Обрабатывает пошаговую загрузку фото клиента."""
        data = self.user_data[chat_id]
        
        if current_state == State.AWAITING_PASSPORT_PHOTO:
            data['passport_photo_id'] = file_id
            self.user_states[chat_id] = State.AWAITING_PASSPORT_BACK_PHOTO 
            send_message(chat_id, get_text('ask_passport_back_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))
        
        elif current_state == State.AWAITING_PASSPORT_BACK_PHOTO:
            data['passport_back_photo_id'] = file_id
            self.user_states[chat_id] = State.AWAITING_LICENSE_FRONT_PHOTO 
            send_message(chat_id, get_text('ask_license_front_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))

        elif current_state == State.AWAITING_LICENSE_FRONT_PHOTO:
            data['license_front_photo_id'] = file_id
            self.user_states[chat_id] = State.AWAITING_LICENSE_BACK_PHOTO
            send_message(chat_id, get_text('ask_license_back_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))

        elif current_state == State.AWAITING_LICENSE_BACK_PHOTO:
            data['license_back_photo_id'] = file_id
            self._finalize_client_registration(chat_id)
            
            
    def _handle_owner_car_photo_upload(self, chat_id, file_id):
        """Обрабатывает загрузку фотографий автомобиля от собственника."""
        data = self.user_data.get(chat_id, {})
    
        if 'car_photo_ids' not in data:
            data['car_photo_ids'] = []
    
        # Проверяем лимит (например, 5 фото)
        if len(data['car_photo_ids']) < 5:
            data['car_photo_ids'].append(file_id)
            photo_count = len(data['car_photo_ids'])
    
            if photo_count == 5:
                self.user_states[chat_id] = State.AWAITING_OWNER_DESIRED_PRICE
                send_message(chat_id, get_text('max_photos_reached', chat_id), reply_markup={'remove_keyboard': True})
                send_message(chat_id, get_text('owner_ask_desired_price', chat_id))
            else:
                send_message(chat_id, get_text('owner_photo_received', chat_id, count=photo_count))
        else:
             send_message(chat_id, get_text('max_photos_reached', chat_id), reply_markup={'remove_keyboard': True})
    

    def _handle_owner_photo_step(self, chat_id, file_id, current_state):
        """Обрабатывает пошаговую загрузку фото собственника."""
        data = self.user_data[chat_id]

        if current_state == State.AWAITING_TECH_PASSPORT_PHOTO:
            data['tech_passport_photo_id'] = file_id
            self.user_states[chat_id] = State.AWAITING_TECH_PASSPORT_BACK_PHOTO
            send_message(chat_id, get_text('ask_tech_passport_back_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))

        elif current_state == State.AWAITING_TECH_PASSPORT_BACK_PHOTO:
            data['tech_passport_back_photo_id'] = file_id
            self.user_states[chat_id] = State.AWAITING_OWNER_PASSPORT_PHOTO
            send_message(chat_id, get_text('ask_owner_passport_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))

        elif current_state == State.AWAITING_OWNER_PASSPORT_PHOTO:
            data['owner_passport_photo_id'] = file_id
            self.user_states[chat_id] = State.AWAITING_OWNER_PASSPORT_BACK_PHOTO
            send_message(chat_id, get_text('ask_owner_passport_back_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))

        elif current_state == State.AWAITING_OWNER_PASSPORT_BACK_PHOTO:
            data['owner_passport_back_photo_id'] = file_id
            self._finalize_owner_registration(chat_id)

    def _finalize_client_registration(self, chat_id):
        """Завершает регистрацию клиента, добавляя или обновляя его профиль."""
        new_client_data = self.user_data[chat_id]
        users = load_from_json(USERS_FILE)

        user_record = users.get(str(chat_id), {})

        user_record['username'] = new_client_data.get('username')
        user_record['first_name'] = new_client_data.get('first_name')
        user_record['last_name'] = new_client_data.get('last_name')

        if 'active_roles' not in user_record:
            user_record['active_roles'] = []
        if 'client' not in user_record['active_roles']:
            user_record['active_roles'].append('client')

        user_record.pop('role', None)

        user_record['client_profile'] = new_client_data
        user_record['status'] = 'pending' 

        users[str(chat_id)] = user_record
        save_to_json(users, USERS_FILE)

        self._notify_admins_new_user(chat_id, user_record)
        send_message(chat_id, get_text('registration_complete', chat_id), reply_markup=get_main_menu_keyboard(chat_id))

        self.user_states.pop(chat_id, None)
        self.user_data.pop(chat_id, None)

    def _finalize_owner_registration(self, chat_id):
        """Завершает регистрацию собственника, добавляя или обновляя его профиль."""
        users = load_from_json(USERS_FILE)
        owner_reg_data = self.user_data[chat_id]

        user_record = users.get(str(chat_id), {})

        user_record['username'] = owner_reg_data.get('username')

        if 'active_roles' not in user_record:
            user_record['active_roles'] = []
        if 'owner' not in user_record['active_roles']:
            user_record['active_roles'].append('owner')

        user_record.pop('role', None)

        owner_profile_data = {k: v for k, v in owner_reg_data.items() if not k.startswith('car_')}
        car_data = {k: v for k, v in owner_reg_data.items() if k.startswith('car_')}
        
        if 'car_photo_ids' in owner_reg_data:
            car_data['car_photo_ids'] = owner_reg_data['car_photo_ids']
        if 'desired_price_day' in owner_reg_data:
            car_data['desired_price_day'] = owner_reg_data['desired_price_day']

        user_record['owner_profile'] = owner_profile_data
        user_record['status'] = 'pending'

        users[str(chat_id)] = user_record
        save_to_json(users, USERS_FILE)

        owner_cars = load_from_json(OWNER_CARS_FILE)
        owner_cars[str(chat_id)] = car_data
        save_to_json(owner_cars, OWNER_CARS_FILE)

        self._notify_admins_new_owner_car(chat_id, user_record)

        send_message(chat_id, get_text('registration_complete', chat_id), reply_markup=get_main_menu_keyboard(chat_id))

        self.user_states.pop(chat_id, None)
        self.user_data.pop(chat_id, None)
        
    def _handle_checklist_media(self, chat_id, message):
        """Обрабатывает полученные фото/видео во время прохождения чек-листа."""
        session = self.user_data.get(chat_id)
        if not session or 'checklist_type' not in session:
            return

        template = self._get_checklist_template(session['checklist_type'])
        prev_step_index = session.get('step', 0) - 1

        if prev_step_index < 0 or prev_step_index >= len(template):
            return 

        prev_step_data = template[prev_step_index]
        expected_type = prev_step_data.get('type')

        if expected_type != 'media':
            send_message(chat_id, "❗️ На этом шаге не ожидается фото или видео.")
            return

        media_type = 'photo' if 'photo' in message else 'video'
        file_id = ""
        if media_type == 'photo':
            file_id = message['photo'][-1]['file_id']
        else:
            file_id = message['video']['file_id']

        session['answers'][prev_step_data['key']] = f"<{media_type.capitalize()} получено>"

        if 'media_files' not in session:
            session['media_files'] = []
        
        session['media_files'].append({
            'type': media_type,
            'id': file_id,
            'caption': prev_step_data['key']
        })

        self._process_checklist_step(chat_id, message)
        
        
    def _handle_checklist_media(self, chat_id, message):
        """Обрабатывает полученные фото/видео во время прохождения чек-листа."""
        session = self.user_data.get(chat_id)
        if not session or 'checklist_type' not in session:
            return 

        template = self._get_checklist_template(session['checklist_type'])
        prev_step_index = session.get('step', 0) - 1

        if prev_step_index < 0 or prev_step_index >= len(template):
            return 

        prev_step_data = template[prev_step_index]
        expected_type = prev_step_data.get('type')

        if expected_type != 'media':
            send_message(chat_id, "❗️ На этом шаге не ожидается фото или видео.")
            return

        media_type = 'photo' if 'photo' in message else 'video'
        file_id = ""
        if media_type == 'photo':
            file_id = message['photo'][-1]['file_id']
        else:
            file_id = message['video']['file_id']

        session['answers'][prev_step_data['key']] = f"<{media_type.capitalize()} получено>"

        if 'media_files' not in session:
            session['media_files'] = []
        
        session['media_files'].append({
            'type': media_type,
            'id': file_id,
            'caption': prev_step_data['key']
        })

        self._process_checklist_step(chat_id, message)
    

    def _start_profile_edit(self, chat_id, role_to_edit):
        """Начинает процесс редактирования профиля для конкретной роли."""
        users = load_from_json(USERS_FILE)
        if chat_id not in users:
            send_message(chat_id, get_text('not_registered', chat_id))
            return

        buttons = []
        # --- ИЗМЕНЕНИЕ: Сохраняем роль и инициализируем данные ---
        self.user_data[chat_id] = {'role_to_edit': role_to_edit}

        if role_to_edit == 'owner':
            buttons.append([{'text': get_text('field_registrator_name', chat_id), 'callback_data': 'edit_field_registrator_name'}])
            buttons.append([{'text': get_text('field_owner_name', chat_id), 'callback_data': 'edit_field_owner_name'}])
            buttons.append([{'text': get_text('field_phone', chat_id), 'callback_data': 'edit_field_phone'}])

        elif role_to_edit == 'client':
            buttons.extend([
                [{'text': get_text('field_name', chat_id), 'callback_data': 'edit_field_name'}],
                [{'text': get_text('field_phone', chat_id), 'callback_data': 'edit_field_phone'}],
                [{'text': get_text('field_city', chat_id), 'callback_data': 'edit_field_city'}],
                [{'text': get_text('field_address', chat_id), 'callback_data': 'edit_field_address'}],
                [{'text': get_text('field_age', chat_id), 'callback_data': 'edit_field_age'}],
                [{'text': get_text('field_driving_experience', chat_id), 'callback_data': 'edit_field_driving_experience'}],
                [{'text': get_text('field_license', chat_id), 'callback_data': 'edit_field_license'}]
            ])

        keyboard = create_keyboard(buttons, inline=True)
        
        # --- ИЗМЕНЕНИЕ: Сохраняем ID сообщения с кнопками ---
        response = send_message(chat_id, get_text('choose_field_to_edit', chat_id), reply_markup=keyboard)
        if response and response.get('ok'):
            # Сохраняем ID для последующего редактирования/удаления кнопок
            self.user_data[chat_id]['edit_menu_message_id'] = response['result']['message_id']
        
        self.user_states[chat_id] = State.AWAITING_PROFILE_EDIT_CHOICE


    def _handle_stateful_message(self, message):
        chat_id = str(message['chat']['id'])
        text = message.get('text', '').strip()
        photo = message.get('photo')
        video = message.get('video')
        voice = message.get('voice')
        document = message.get('document') 
        animation = message.get('animation') 
        sticker = message.get('sticker') 
        
        state = self.user_states.get(chat_id)
        data = self.user_data.get(chat_id, {})

        cancel_btn_text = get_text('cancel_button', chat_id)
        back_btn_text = get_text('back_button', chat_id)

        if text == '/cancel' or text == cancel_btn_text:
            # --- НАЧАЛО ИЗМЕНЕНИЙ ---
            edit_menu_message_id = data.get('edit_menu_message_id')

            # Если мы находились в процессе редактирования профиля, то редактируем старое сообщение, убирая кнопки
            if state in [State.AWAITING_PROFILE_EDIT_CHOICE, State.AWAITING_PROFILE_EDIT_VALUE] and edit_menu_message_id:
                edit_message_text(
                    chat_id=chat_id,
                    message_id=edit_menu_message_id,
                    text=get_text('choose_field_to_edit', chat_id) + f"\n\n*{get_text('operation_cancelled', chat_id)}*",
                    reply_markup=None  # Передача None убирает inline-клавиатуру
                )
                # После этого можно дополнительно показать главное меню
                send_message(chat_id, get_text('main_menu_prompt', chat_id), reply_markup=get_main_menu_keyboard(chat_id))
            else:
                # Стандартное поведение для всех остальных операций
                send_message(chat_id, get_text('operation_cancelled', chat_id), 
                           reply_markup=get_main_menu_keyboard(chat_id))

            # Очищаем состояние и временные данные в любом случае
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            return
            # --- КОНЕЦ ИЗМЕНЕНИЙ ---

        if text == back_btn_text:
            self._go_back(chat_id)
            return

        finish_btn_text = get_text('finish_photo_upload_btn', chat_id)
        if state == State.AWAITING_OWNER_CAR_PHOTOS and text == finish_btn_text:
            if not data.get('car_photo_ids'):
                send_message(chat_id, "❗️ Пожалуйста, отправьте хотя бы одну фотографию вашего автомобиля перед тем, как нажать 'Готово'.")
                return
            
            self._set_next_state(chat_id, State.AWAITING_OWNER_DESIRED_PRICE)
            send_message(chat_id, get_text('owner_ask_desired_price', chat_id), reply_markup={'remove_keyboard': True})
            return
        
        if (photo or video) and state == State.AWAITING_CHECKLIST_INPUT:
            self._handle_checklist_media(chat_id, message)
            return
        
        if photo: 
            file_id = photo[-1]['file_id'] 

            client_photo_states = [
                State.AWAITING_PASSPORT_PHOTO, State.AWAITING_PASSPORT_BACK_PHOTO,
                State.AWAITING_LICENSE_FRONT_PHOTO, State.AWAITING_LICENSE_BACK_PHOTO
            ]
            owner_doc_photo_states = [
                State.AWAITING_TECH_PASSPORT_PHOTO, State.AWAITING_TECH_PASSPORT_BACK_PHOTO,
                State.AWAITING_OWNER_PASSPORT_PHOTO, State.AWAITING_OWNER_PASSPORT_BACK_PHOTO
            ]

            if state in client_photo_states:
                self._handle_client_photo_step(chat_id, file_id, state)
                return 
            
            if state in owner_doc_photo_states:
                self._handle_owner_photo_step(chat_id, file_id, state)
                return

            if state == State.AWAITING_OWNER_CAR_PHOTOS:
                self._handle_owner_car_photo_upload(chat_id, file_id)
                return

            if state == State.AWAITING_NEW_CAR_PHOTO:
                self._handle_new_car_photo(chat_id, file_id)
                return
        
            if (photo or video) and state == State.AWAITING_CHECKLIST_INPUT:
                self._handle_checklist_media(chat_id, message)
                return
        
        state = self.user_states.get(chat_id)
        
        if state == State.IN_CHAT_WITH_MANAGER:
            if text == '/endchat' or text == get_text('end_chat_button', chat_id):
                admins = get_admins()
                if admins:
                    users = load_from_json(USERS_FILE)
                    user_info = users.get(chat_id, {})
                    user_name = self._get_user_display_name(user_info)
                    for admin_id in admins:
                        admin_lang_text = get_text('admin_user_ended_chat', admin_id, user_name=user_name, user_id=chat_id)
                        send_message(admin_id, admin_lang_text)
                
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                send_message(chat_id, get_text('exit_chat_mode', chat_id), reply_markup=get_main_menu_keyboard(chat_id))
                return

            admins = get_admins()
            if not admins:
                send_message(chat_id, get_text('no_admins_online', chat_id))
                return

            users = load_from_json(USERS_FILE)
            user_info = users.get(chat_id, {})
            user_name = self._get_user_display_name(user_info)
            username_mention = f" (@{user_info.get('username')})" if user_info.get('username') else ""
            
            caption = (f"💬 *Сообщение в чате от {user_name}{username_mention}*\n"
                       f"*ID для ответа:* `{chat_id}`")

            reply_keyboard = create_keyboard(
                [[{'text': get_text('admin_btn_reply', chat_id), 'callback_data': f'admin_reply_{chat_id}'}]],
                inline=True
            )

            for admin_id in admins:
                if photo:
                    send_photo(admin_id, photo[-1]['file_id'], caption=caption, reply_markup=reply_keyboard)
                elif video:
                    send_video(admin_id, video['file_id'], caption=caption, reply_markup=reply_keyboard)
                elif voice:
                    send_voice(admin_id, voice['file_id'], caption=caption, reply_markup=reply_keyboard)
                elif document:
                    send_document(admin_id, document['file_id'], caption=caption, reply_markup=reply_keyboard)
                elif animation:
                    send_animation(admin_id, animation['file_id'], caption=caption, reply_markup=reply_keyboard)
                elif sticker:
                    send_message(admin_id, caption, reply_markup=reply_keyboard)
                    send_sticker(admin_id, sticker['file_id'])
                elif text:
                    send_message(admin_id, f"{caption}\n\n*{text}*", reply_markup=reply_keyboard)
            return
   
        elif state == State.ADMIN_IN_CHAT_WITH_USER:
            target_info = data
            target_user_name = target_info.get('target_user_name', '')
            end_chat_btn_text = get_text('admin_end_chat_button', chat_id, user_name=target_user_name)

            if text == end_chat_btn_text:
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                send_message(chat_id, get_text('admin_exit_chat_session', chat_id, user_name=target_user_name), reply_markup=get_main_menu_keyboard(chat_id))
                return

            target_user_id = target_info.get('target_user_id')
            if not target_user_id:
                send_message(chat_id, "❗️ Произошла ошибка сессии. Начните заново.")
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                return

            media_caption = message.get('caption', '')
            
            prefix = get_text('admin_reply_prefix', target_user_id, text='')
            full_caption = f"{prefix}\n{media_caption}"

            response = None 

            if photo:
                response = send_photo(target_user_id, photo[-1]['file_id'], caption=full_caption)
            elif video:
                response = send_video(target_user_id, video['file_id'], caption=full_caption)
            elif voice:
                response = send_voice(target_user_id, voice['file_id'], caption=full_caption)
            elif document:
                response = send_document(target_user_id, document['file_id'], caption=full_caption)
            elif animation:
                response = send_animation(target_user_id, animation['file_id'], caption=full_caption)
            elif sticker:
                response = send_sticker(target_user_id, sticker['file_id'])
            elif text:
                response = send_message(target_user_id, get_text('admin_reply_prefix', target_user_id, text=text))

            if response and not response.get('ok') and response.get('error_code') == 403:
                send_message(
                    chat_id, 
                    f"❗️*Сообщение не доставлено.* Пользователь *{target_user_name}* (ID: `{target_user_id}`) заблокировал бота."
                )
                
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                send_message(
                    chat_id, 
                    get_text('admin_exit_chat_session', chat_id, user_name=target_user_name), 
                    reply_markup=get_main_menu_keyboard(chat_id)
                )

            return

        
        if state == State.AWAITING_CUSTOM_ERROR_MESSAGE:
            target_user_id = data.get('target_user_id')
            if not target_user_id:
                send_message(chat_id, "❗️ Произошла ошибка сессии. Не удалось найти ID пользователя. Операция отменена.")
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                return

            custom_message_text = text
            escaped_custom_text = escape_markdown(custom_message_text)

            all_users = load_from_json(USERS_FILE)
            target_user_data = all_users.get(target_user_id, {})
            user_roles = target_user_data.get('active_roles', [])

            if 'owner' in user_roles:
                retry_instruction = get_text('registration_rejected_owner', target_user_id)
            else:
                retry_instruction = get_text('registration_rejected_client', target_user_id)
            
            escaped_retry_instruction = escape_markdown(retry_instruction)
            
            user_message_full = (f"{get_text('user_error_notification_prefix', target_user_id)}\n\n"
                                 f"{escaped_custom_text}\n\n"
                                 f"{escaped_retry_instruction}")
            
            send_message(target_user_id, user_message_full)

            if target_user_id in all_users:
                all_users[target_user_id]['status'] = 'rejected'
                save_to_json(all_users, USERS_FILE)

            send_message(chat_id, get_text('admin_custom_error_sent', chat_id, target_id=target_user_id))

            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            return
        
        if state == State.AWAITING_CHECKLIST_INPUT:
            session = data
            template = self._get_checklist_template(session['checklist_type'])
            prev_step_index = session.get('step', 0) - 1

            if prev_step_index < 0:
                return

            prev_step_data = template[prev_step_index]
            expected_type = prev_step_data.get('type')

            if expected_type == 'media':
                valid_skip_words = ['ок', 'ok', 'нет', 'no']
                if text.lower() in valid_skip_words:
                    session['answers'][prev_step_data['key']] = text
                    self._process_checklist_step(chat_id, {'text': text})
                else:
                    send_message(chat_id, "❗️*Ошибка:* На этом шаге нужно отправить фото или видео, а не текст. Пожалуйста, попробуйте снова.")
                return

            elif expected_type == 'buttons':
                send_message(chat_id, "❗️*Ошибка:* Пожалуйста, используйте кнопки для ответа на этот вопрос.")
                return

            elif expected_type == 'text':
                session['answers'][prev_step_data['key']] = text
                self._process_checklist_step(chat_id, {'text': text})
                return
    
        if state == State.AWAITING_NAME:
            data['name'] = text
            self._set_next_state(chat_id, State.AWAITING_PHONE)
            send_message(chat_id, get_text('ask_phone', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
        
        elif state == State.AWAITING_PHONE:
            if len(text) == 9 and text.isdigit():
                data['phone'] = f"+992{text}"
                self._set_next_state(chat_id, State.AWAITING_CITY)
                lang = get_user_lang(chat_id)
                buttons_in_row = 2
                keyboard_buttons = []
                for i in range(0, len(CITIES), buttons_in_row):
                    row = []
                    for j in range(i, min(i + buttons_in_row, len(CITIES))):
                         row.append({'text': CITIES[j][lang], 'callback_data': f"city_{CITIES[j]['key']}"})
                    keyboard_buttons.append(row)

                city_keyboard = create_keyboard(keyboard_buttons, inline=True)
                send_message(chat_id, get_text('ask_city', chat_id), reply_markup=city_keyboard)

            else:
                send_message(chat_id, get_text('invalid_phone', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))

        elif state == State.AWAITING_CITY:
            send_message(chat_id, get_text('select_city_from_list', chat_id))

        elif state == State.AWAITING_ADDRESS:
            data['address'] = text
            self._set_next_state(chat_id, State.AWAITING_AGE)
            send_message(chat_id, get_text('ask_age', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))

        elif state == State.AWAITING_AGE:
            try:
                user_dob = datetime.strptime(text, '%d.%m.%Y')
                today = datetime.now()
        
                age = today.year - user_dob.year - ((today.month, today.day) < (user_dob.month, user_dob.day))
        
                if 18 <= age <= 80:
                    data['dob'] = text 
                    data.pop('age', None)
            
                    self._set_next_state(chat_id, State.AWAITING_DRIVING_EXPERIENCE)
                    send_message(chat_id, get_text('ask_driving_experience', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
                else:
                    send_message(chat_id, get_text('invalid_age', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
            
            except ValueError:
                send_message(chat_id, get_text('invalid_age', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
    
        elif state == State.AWAITING_DRIVING_EXPERIENCE:
            if text.isdigit() and 0 <= int(text) <= 60:
                data['driving_experience'] = text
                self._set_next_state(chat_id, State.AWAITING_LICENSE)
                send_message(chat_id, get_text('ask_license', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
            else:
                send_message(chat_id, get_text('invalid_driving_experience', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
    
        elif state == State.AWAITING_LICENSE:
            if re.fullmatch(r'[A-Za-z]{2}\d{7}', text):
                data['license'] = text.upper()
                self._set_next_state(chat_id, State.AWAITING_LICENSE_CATEGORY)
                categories_keyboard = create_keyboard([[{'text': 'A', 'callback_data': 'cat_A'}, {'text': 'B', 'callback_data': 'cat_B'}, {'text': 'C', 'callback_data': 'cat_C'}, {'text': 'D', 'callback_data': 'cat_D'}],[{'text': 'BE', 'callback_data': 'cat_BE'}, {'text': 'CE', 'callback_data': 'cat_CE'}, {'text': 'DE', 'callback_data': 'cat_DE'}]], inline=True)
                send_message(chat_id, get_text('ask_license_category', chat_id) + get_text('cancel_hint', chat_id), reply_markup=categories_keyboard)
            else:
                send_message(chat_id, get_text('invalid_license', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))

        elif state == State.AWAITING_DRIVER_ID:
            data['driver_id'] = text
            self._set_next_state(chat_id, State.AWAITING_DEPOSIT)
            yes_no_keyboard = create_keyboard([[{'text': get_text('yes', chat_id), 'callback_data': 'reg_deposit_yes'},{'text': get_text('no', chat_id), 'callback_data': 'reg_deposit_no'}]], inline=True)
            send_message(chat_id, get_text('ask_deposit', chat_id) + get_text('cancel_hint', chat_id), reply_markup=yes_no_keyboard)

        elif state == State.AWAITING_OWNER_REGISTRATOR_NAME:
            data['registrator_name'] = text
            self._set_next_state(chat_id, State.AWAITING_OWNER_NAME)

            # Создаем клавиатуру с новой кнопкой "Я и есть собственник"
            i_am_owner_keyboard = create_keyboard([
                [get_text('i_am_the_owner_btn', chat_id)],  # Новая кнопка в отдельном ряду
                [get_text('back_button', chat_id), get_text('cancel_button', chat_id)] # Стандартные кнопки "Назад" и "Отмена"
            ], one_time=False, inline=False)

            send_message(chat_id, get_text('owner_ask_owner_name', chat_id), reply_markup=i_am_owner_keyboard)
        
        elif state == State.AWAITING_OWNER_NAME:
            # Проверяем, была ли нажата новая кнопка
            if text == get_text('i_am_the_owner_btn', chat_id):
                # Если да, копируем ФИО регистратора в ФИО собственника
                data['owner_name'] = data.get('registrator_name')
            else:
                # В противном случае, используем введенный текст как ФИО собственника
                data['owner_name'] = text
        
            # Переходим к следующему шагу в любом случае
            self._set_next_state(chat_id, State.AWAITING_OWNER_PHONE)
            send_message(chat_id, get_text('ask_phone', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
        
        elif state == State.AWAITING_OWNER_PHONE:
            if len(text) == 9 and text.isdigit():
                data['phone'] = f"+992{text}"
                self._set_next_state(chat_id, State.AWAITING_OWNER_CAR_BRAND)
                send_message(chat_id, get_text('owner_ask_car_brand', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
            else:
                send_message(chat_id, get_text('invalid_phone', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
            
        elif state == State.AWAITING_OWNER_CAR_BRAND:
            data['car_brand'] = text
            self._set_next_state(chat_id, State.AWAITING_OWNER_CAR_PLATE)
            send_message(chat_id, get_text('owner_ask_car_plate', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
        
        elif state == State.AWAITING_OWNER_CAR_PLATE:
            plate_number = text.upper()
            pattern = r'^\d{3,4}[A-Z]{2}\d{2}$'
            if re.fullmatch(pattern, plate_number):
                data['car_plate'] = plate_number
                self._set_next_state(chat_id, State.AWAITING_OWNER_CAR_VIN)
                send_message(chat_id, get_text('owner_ask_car_vin', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
            else:
                send_message(chat_id, get_text('invalid_plate_format_tj', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
        
        elif state == State.AWAITING_OWNER_CAR_VIN:
            vin_code = text.upper()

            if len(vin_code) == 17 and vin_code.isalnum() and all(char not in 'IOQ' for char in vin_code):
                data['car_vin'] = vin_code
                self._set_next_state(chat_id, State.AWAITING_OWNER_CAR_YEAR)
                send_message(chat_id, get_text('owner_ask_car_year', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
            else:
                send_message(chat_id, get_text('invalid_vin', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
        
        elif state == State.AWAITING_OWNER_CAR_YEAR:
            current_year = datetime.now().year
            if text.isdigit() and len(text) == 4 and 1950 <= int(text) <= current_year + 1:
                data['car_year'] = text
                self._set_next_state(chat_id, State.AWAITING_OWNER_CAR_MILEAGE)
                send_message(chat_id, get_text('owner_ask_car_mileage', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
            else:
                send_message(chat_id, get_text('invalid_year', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))
    
        elif state == State.AWAITING_OWNER_CAR_MILEAGE:
            if text.isdigit() and int(text) >= 0:
                data['car_mileage'] = text
                self._set_next_state(chat_id, State.AWAITING_OWNER_CAR_TRANSMISSION)
                transmission_keyboard = create_keyboard([[{'text': "Автомат", 'callback_data': 'owner_trans_auto'},{'text': "Механика", 'callback_data': 'owner_trans_manual'}]], inline=True)
                send_message(chat_id, get_text('owner_ask_car_transmission', chat_id) + get_text('cancel_hint', chat_id), reply_markup=transmission_keyboard)
            else:
                send_message(chat_id, get_text('invalid_mileage', chat_id), reply_markup=self.get_back_cancel_keyboard(chat_id))

        elif state == State.AWAITING_OWNER_CAR_COLOR:
            data['car_color'] = text
            self._set_next_state(chat_id, State.AWAITING_OWNER_CAR_TYPE)
            car_type_keyboard = create_keyboard([[{'text': "ДВС", 'callback_data': 'owner_cartype_ice'},{'text': "Электромобиль", 'callback_data': 'owner_cartype_electric'},{'text': "Гибрид", 'callback_data': 'owner_cartype_hybrid'}]], inline=True)
            send_message(chat_id, get_text('owner_ask_car_type', chat_id) + get_text('cancel_hint', chat_id), reply_markup=car_type_keyboard)
            
        elif state == State.AWAITING_OWNER_DESIRED_PRICE:
            if text.isdigit() and int(text) > 0:
                data['desired_price_day'] = int(text)
                self._set_next_state(chat_id, State.AWAITING_CONSENT)

                consent_keyboard = create_keyboard([
                    [{'text': get_text('consent_confirm_btn', chat_id), 'callback_data': 'consent_confirm_owner'}]
                ], inline=True)

                send_message(chat_id, get_text('ask_docs_after_price', chat_id))

                send_message(chat_id, get_text('consent_prompt', chat_id), reply_markup=consent_keyboard)
            else:
                send_message(chat_id, get_text('invalid_price', chat_id))

        elif state == State.AWAITING_PROFILE_EDIT_VALUE:
            edit_data = self.user_data.get(chat_id, {})
            field_to_edit = edit_data.get('edit_field')
            new_value = text
            is_valid = False
            error_message = None

            if not field_to_edit:
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                return

            if field_to_edit == 'phone':
                if len(new_value) == 9 and new_value.isdigit():
                    new_value = f"+992{new_value}"
                    is_valid = True
                else:
                    error_message = get_text('invalid_phone', chat_id)
            elif field_to_edit == 'age':
                try:
                    user_dob = datetime.strptime(new_value, '%d.%m.%Y')
                    today = datetime.now()
                    age = today.year - user_dob.year - ((today.month, today.day) < (user_dob.month, user_dob.day))
        
                    if 18 <= age <= 80:
                        is_valid = True
                        users[chat_id]['dob'] = new_value
                        users[chat_id].pop('age', None)
                    else:
                        error_message = get_text('invalid_age', chat_id)
                except ValueError:
                    error_message = get_text('invalid_age', chat_id)
                if is_valid:
                    save_to_json(users, USERS_FILE)
                    send_message(chat_id, get_text('profile_updated_successfully', chat_id))
                    self.user_states.pop(chat_id, None)
                    self.user_data.pop(chat_id, None)
                    self._handle_commands(chat_id, '/profile', {'from': {'id': chat_id}})
                else:
                    send_message(chat_id, error_message)
                return
            
            elif field_to_edit == 'driving_experience':
                if new_value.isdigit() and 0 <= int(new_value) <= 60:
                    is_valid = True
                else:
                    error_message = get_text('invalid_driving_experience', chat_id)
            elif field_to_edit == 'license':
                if re.fullmatch(r'[А-Яа-я]{2}\d{7}', new_value):
                    new_value = new_value.upper()
                    is_valid = True
                else:
                    error_message = get_text('invalid_license', chat_id)

            elif field_to_edit == 'address':
                if new_value:
                    is_valid = True
                else:
                    error_message = get_text('invalid_value', chat_id)

            elif field_to_edit in ['name', 'city', 'registrator_name', 'owner_name']:
                if new_value:
                    is_valid = True
                else:
                    error_message = get_text('invalid_value', chat_id)

            if is_valid:
                users = load_from_json(USERS_FILE)
                if chat_id in users:
                    role_being_edited = self.user_data.get(chat_id, {}).get('role_to_edit')
                    if role_being_edited:
                        profile_key = f"{role_being_edited}_profile"
                        if profile_key in users[chat_id]:
                            users[chat_id][profile_key][field_to_edit] = new_value
                        else:
                            users[chat_id][profile_key] = {field_to_edit: new_value}
                    else:
                        users[chat_id][field_to_edit] = new_value
                    save_to_json(users, USERS_FILE)
                    
                    send_message(chat_id, get_text('profile_updated_successfully', chat_id))
                    
                    self.user_states.pop(chat_id, None)
                    self.user_data.pop(chat_id, None)
                    
                    self._handle_commands(chat_id, '/profile', {'from': {'id': chat_id}})
                else:
                    send_message(chat_id, get_text('not_registered', chat_id))
            else:
                if error_message:
                    send_message(chat_id, error_message)
                else:
                    send_message(chat_id, get_text('invalid_value', chat_id))
            
        elif state == State.AWAITING_SERVICE_REQUEST:
            request_text = text
            admins = get_admins()
            user_info = load_from_json(USERS_FILE).get(chat_id, {})
            car_info = load_from_json(OWNER_CARS_FILE).get(chat_id, {})
            
            admin_message = (f"⚙️ *Новая заявка на ТО от собственника!*\n\n"
                             f"👤 *Имя:* {user_info.get('name', 'N/A')}\n"
                             f"📞 *Телефон:* `{user_info.get('phone', 'N/A')}`\n"
                             f"🆔 *Chat ID:* `{chat_id}`\n\n"
                             f"🚗 *Автомобиль:* {car_info.get('car_brand', 'N/A')} ({car_info.get('car_plate', 'N/A')})\n\n"
                             f"📝 *Описание проблемы:*\n{request_text}")
            for admin_id in admins:
                send_message(admin_id, admin_message)
            
            send_message(chat_id, get_text('service_request_sent', chat_id))
            self.user_states.pop(chat_id, None)
            
        elif state == State.AWAITING_RENT_CAR:
            available_cars = get_available_cars()
            if text in available_cars:
                data['car'] = text
                self._set_next_state(chat_id, State.AWAITING_RENT_PERIOD)
                car_info = available_cars[text]

                short_caption = f"🚗 *{text}*\n\n📜 *Описание:* {car_info['description']}"
                if car_info.get('local_image_path') and os.path.exists(car_info['local_image_path']):
                    send_photo(chat_id, car_info['local_image_path'], caption=short_caption)
                else:
                    send_message(chat_id, short_caption)
                
                details_text = (f"*{get_text('car_details_price', chat_id)}*\n"
                                f"{get_text('price_day', chat_id, price=car_info['prices'][0])}\n"
                                f"{get_text('price_week', chat_id, price=car_info['prices'][1])}\n"
                                f"{get_text('price_month', chat_id, price=car_info['prices'][2])}\n\n"
                                f"*{get_text('car_specs', chat_id)}*\n"
                                f"{get_text('car_year', chat_id, year=car_info.get('year', 'N/A'))}\n"
                                f"{get_text('car_fuel', chat_id, fuel_type=car_info.get('fuel_type', 'N/A'))}\n"
                                f"{get_text('car_engine', chat_id, engine=car_info.get('engine', 'N/A'))}\n"
                                f"{get_text('car_transmission', chat_id, transmission=car_info.get('transmission', 'N/A'))}\n"
                                f"{get_text('car_seats', chat_id, seats=car_info.get('seats', 'N/A'))}")
                send_message(chat_id, details_text)
                time.sleep(0.3)

                periods_keyboard = create_keyboard([
                    [get_text('day', chat_id)], 
                    [get_text('week', chat_id)], 
                    [get_text('month', chat_id)]
                ], inline=True)
                send_message(chat_id, get_text('choose_rent_period', chat_id), reply_markup=periods_keyboard)
            else:
                send_message(chat_id, get_text('select_car_from_list', chat_id))
                
        elif state == State.AWAITING_BROADCAST_CONFIRMATION:
            if text.lower() in ['да', 'yes']:
                broadcast_message = data['broadcast_message']
                users = load_from_json(USERS_FILE)
                active_users = [uid for uid, udata in users.items() if udata.get('status') == 'active']
                count = 0
                send_message(chat_id, get_text('admin_broadcast_start', chat_id, count=len(active_users)))
                for user_id in active_users:
                    user_lang_text = get_text('admin_reply_prefix', user_id, text=broadcast_message)
                    if send_message(user_id, user_lang_text): count += 1
                    time.sleep(0.1)
                send_message(chat_id, get_text('admin_broadcast_finish', chat_id, count=count))
            else:
                send_message(chat_id, get_text('admin_broadcast_cancelled', chat_id))
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            
        elif state == State.AWAITING_NEW_CAR_NAME:
            car_name = text.strip()
            if car_name in get_available_cars():
                send_message(chat_id, "❗️Автомобиль с таким названием уже существует. Введите другое или /cancel.")
                return
            data['car_name'] = car_name
            self._set_next_state(chat_id, State.AWAITING_NEW_CAR_DESCRIPTION)
            send_message(chat_id, "📝 Введите описание для нового автомобиля:")
            
        elif state == State.AWAITING_NEW_CAR_DESCRIPTION:
            data['description'] = text.strip()
            self._set_next_state(chat_id, State.AWAITING_NEW_CAR_PHOTO)
            send_message(chat_id, "📸 Отправьте фотографию для нового автомобиля:")
            
        elif state == State.AWAITING_NEW_CAR_PRICES:
            try:
                prices = tuple(map(int, text.split()))
                if len(prices) == 3 and all(p > 0 for p in prices):
                    data['prices'] = prices
                    available_cars = get_available_cars()
                    available_cars[data['car_name']] = {
                        "description": data['description'],
                        "local_image_path": data['local_image_path'],
                        "prices": data['prices'],
                        "year": data.get('year', 2023),
                        "fuel_type": data.get('fuel_type', 'Бензин'),
                        "engine": data.get('engine', 'N/A'),
                        "transmission": data.get('transmission', 'Автомат'),
                        "seats": data.get('seats', 5)
                    }
                    save_to_json(available_cars, CARS_FILE)
                    send_message(chat_id, f"✅ Автомобиль *{data['car_name']}* успешно добавлен.")
                    self.user_states.pop(chat_id, None)
                    self.user_data.pop(chat_id, None)
                else:
                    raise ValueError()
            except ValueError:
                send_message(chat_id, "❗️Неверный формат. Введите 3 положительных числа через пробел (например, 360 2160 7200).")
                
        elif state == State.AWAITING_EDIT_CAR_SELECT:
            selected_car_name = text.strip()
            if selected_car_name in get_available_cars():
                self.user_data[chat_id]['selected_car'] = selected_car_name
                self._set_next_state(chat_id, State.AWAITING_EDIT_CAR_FIELD)
                keyboard = create_keyboard([['Описание', 'Фото'], ['Цены', 'Отмена']], one_time=True)
                send_message(chat_id, f"Что вы хотите изменить для *{selected_car_name}*?", reply_markup=keyboard)
            else:
                send_message(chat_id, "❗️Автомобиль не найден. Выберите из списка или /cancel.")
                
        elif state == State.AWAITING_EDIT_CAR_FIELD:
            field = text.strip().lower()
            if field == 'отмена':
                send_message(chat_id, "❌ Редактирование отменено.", reply_markup={'remove_keyboard': True})
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                return
                
            if field in ['описание', 'фото', 'цены']:
                self.user_data[chat_id]['edit_field'] = field
                if field == 'фото':
                    self._set_next_state(chat_id, State.AWAITING_NEW_CAR_PHOTO)
                    send_message(chat_id, "📸 Отправьте новую фотографию:")
                elif field == 'цены':
                    self._set_next_state(chat_id, State.AWAITING_EDIT_CAR_VALUE)
                    send_message(chat_id, "🔢 Введите новые цены (3 числа через пробел):")
                elif field == 'описание':
                    self._set_next_state(chat_id, State.AWAITING_EDIT_CAR_VALUE)
                    send_message(chat_id, "📝 Введите новое описание:")
            else:
                send_message(chat_id, "❗️Неверное поле. Выберите 'Описание', 'Фото' или 'Цены'.")
                
        elif state == State.AWAITING_EDIT_CAR_VALUE:
            selected_car = self.user_data[chat_id]['selected_car']
            edit_field = self.user_data[chat_id]['edit_field']
            available_cars = get_available_cars()
            
            if selected_car not in available_cars:
                send_message(chat_id, "❗️Ошибка: Автомобиль не найден. /cancel.")
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                return
                
            car_data = available_cars[selected_car]
            
            if edit_field == 'описание':
                car_data['description'] = text.strip()
                save_to_json(available_cars, CARS_FILE)
                send_message(chat_id, f"✅ Описание для *{selected_car}* обновлено.")
                
            elif edit_field == 'цены':
                try:
                    prices = tuple(map(int, text.split()))
                    if len(prices) == 3 and all(p >= 0 for p in prices):
                        car_data['prices'] = prices
                        save_to_json(available_cars, CARS_FILE)
                        send_message(chat_id, f"✅ Цены для *{selected_car}* обновлены.")
                    else:
                        send_message(chat_id, "❗️Неверный формат. Введите 3 неотрицательных числа через пробел.")
                        return
                except ValueError:
                    send_message(chat_id, "❗️Неверный формат. Введите 3 числа через пробел.")
                    return
                    
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)

    def _get_checklist_template(self, checklist_type):
        """Возвращает шаблон с вопросами для указанного типа чек-листа."""
        templates = {
            'dailycheck': [
                {'q': 'Пробег (введите число)', 'type': 'text', 'key': 'Пробег'},
                {'q': '📸 Пожалуйста, отправьте фото спидометра', 'type': 'media', 'key': 'Фото спидометра'},
                {'q': 'Уровень топлива (%)', 'type': 'buttons', 'options': ['25', '50', '75', '100'], 'key': 'Уровень топлива'},
                {'q': 'Давление в шинах', 'type': 'buttons', 'options': ['Норма', 'Требует подкачки'], 'key': 'Давление в шинах'},
                {'q': 'Световые приборы (фары, стоп-сигналы)', 'type': 'buttons', 'options': ['В порядке', 'Неисправность'], 'key': 'Световые приборы'},
                {'q': '📹 Отправьте фото или видео внешнего вида авто. Если есть повреждения, покажите их. Если всё в порядке, напишите "ок".', 'type': 'media', 'key': 'Внешний вид'},
                {'q': 'Состояние салона', 'type': 'buttons', 'options': ['Чисто', 'Требуется уборка'], 'key': 'Состояние салона'},
                {'q': 'Наличие документов (техпаспорт, страховка)', 'type': 'buttons', 'options': ['Комплект', 'Отсутствуют'], 'key': 'Наличие документов'}
            ],
            'acceptfromowner': [
                {'q': 'Пробег при приемке (введите число)', 'type': 'text', 'key': 'Пробег при приемке'},
                {'q': 'Состояние кузова', 'type': 'buttons', 'options': ['Без повреждений', 'Есть царапины', 'Есть вмятины'], 'key': 'Состояние кузова'},
                {'q': '📸 Отправьте фото/видео любых имеющихся повреждений. Если их нет, напишите "нет".', 'type': 'media', 'key': 'Фото повреждений'},
                {'q': 'Состояние шин', 'type': 'buttons', 'options': ['Новые', 'Износ < 50%', 'Износ > 50%'], 'key': 'Состояние шин'},
                {'q': 'Комментарий (если есть проблемы, опишите их)', 'type': 'text', 'key': 'Комментарий'}
            ],
            'givetodriver': [
                {'q': 'ID или телефон водителя', 'type': 'text', 'key': 'driver_id'},
                {'q': 'Пробег при выдаче (введите число)', 'type': 'text', 'key': 'mileage_handover'},
                {'q': '📸 Сделайте фото автомобиля перед выдачей', 'type': 'media', 'key': 'Фото перед выдачей'},
                {'q': 'Состояние авто перед выдачей', 'type': 'buttons', 'options': ['Чистое, без повреждений', 'Требует мойки', 'Есть замечания'], 'key': 'condition_handover'},
                {'q': 'Водитель с условиями ознакомлен?', 'type': 'buttons', 'options': ['Да, ознакомлен', 'Нет'], 'key': 'driver_agreed'}
            ]
        }
        return templates.get(checklist_type, [])

    def _process_checklist_step(self, chat_id, message, is_callback=False, callback_data=None):
        """Задает следующий вопрос чек-листа или завершает его."""
        session = self.user_data.get(chat_id)
        if not session: return

        template = self._get_checklist_template(session['checklist_type'])
        current_step_index = session.get('step', 0)

        if current_step_index >= len(template):
            if is_callback:
                delete_message(chat_id, message['message_id'])

            answers_text = "\n".join([f" - *{key}*: {val}" for key, val in session.get('answers', {}).items()])
            final_report = (f"✅ *Чек-лист завершен!*\n\n"
                            f"*Тип:* `{session['checklist_type']}`\n"
                            f"*Автомобиль:* `{session.get('car_plate', 'Не выбран')}`\n"
                            f"*Механик ID:* `{chat_id}`\n"
                            f"*Дата:* {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\n\n"
                            f"*Результаты:*\n{answers_text}")
            send_message(chat_id, final_report)

            if session.get('media_files'):
                send_message(chat_id, "🗂️ *Отправка медиафайлов отчета...*")
                for media in session['media_files']:
                    caption = f"Отчет: `{session['checklist_type']}` | Авто: `{session.get('car_plate')}`\nПункт: `{media['caption']}`"
                    if media['type'] == 'photo':
                        send_photo(chat_id, media['id'], caption=caption)
                    elif media['type'] == 'video':
                        send_video(chat_id, media['id'], caption=caption)
                    time.sleep(0.5)

            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            return

        step_data = template[current_step_index]
        prompt_text = f"*{step_data['q']}* ({current_step_index + 1}/{len(template)})"

        keyboard = None
        if step_data['type'] == 'buttons':
            buttons = [[{'text': opt, 'callback_data': f"checklist_answer_{opt}"}] for opt in step_data['options']]
            keyboard = create_keyboard(buttons, inline=True)

        if is_callback:
            edit_message_text(chat_id, message['message_id'], prompt_text, reply_markup=keyboard)
        else:
            send_message(chat_id, prompt_text, reply_markup=keyboard)

        session['step'] += 1

            
    def _handle_rent_type_selection(self, chat_id, data, message, query_id):
        """Обрабатывает выбор пользователя между краткосрочной и долгосрочной арендой."""
        send_request('answerCallbackQuery', {'callback_query_id': query_id})

        if data == 'rent_type_short':
            self.user_states[chat_id] = State.AWAITING_RENT_PERIOD
            day_picker_keyboard = self.create_day_picker_keyboard()
            edit_message_text(chat_id, message['message_id'],
                              get_text('choose_number_of_days', chat_id),
                              reply_markup=day_picker_keyboard)

        elif data == 'rent_type_long':
            self.user_states[chat_id] = State.AWAITING_RENT_PERIOD
            periods_keyboard = create_keyboard([
                [{'text': get_text('from_6_months', chat_id), 'callback_data': 'long_from_6_months'}],
                [{'text': get_text('up_to_1_year', chat_id), 'callback_data': 'long_up_to_1_year'}],
                [{'text': "От 1 года и более", 'callback_data': 'long_over_1_year'}], 
                [{'text': '❌ Отмена', 'callback_data': 'days_cancel'}]
            ], inline=True)
            
            edit_message_text(chat_id, message['message_id'], get_text('choose_rent_period', chat_id), reply_markup=periods_keyboard)
    
    def create_day_picker_keyboard(self, page=0, max_days=30):
        """Создает клавиатуру для выбора количества дней с пагинацией."""
        keyboard = []
        days_per_page = 10
        start_day = page * days_per_page + 1
        end_day = start_day + days_per_page

        days = list(range(start_day, min(end_day, max_days + 1)))
        for i in range(0, len(days), 5):
            row_days = days[i:i+5]
            if not row_days: continue
            keyboard.append([{'text': str(d), 'callback_data': f'days_select_{d}'} for d in row_days])

        nav_row = []
        if page > 0:
            nav_row.append({'text': '⬅️', 'callback_data': f'days_nav_{page - 1}'})
        else:
            nav_row.append({'text': ' ', 'callback_data': 'ignore'})

        # Кнопка отмены в центре
        nav_row.append({'text': '❌', 'callback_data': 'days_cancel'})

        # Стрелка вправо
        if end_day <= max_days:
            nav_row.append({'text': '➡️', 'callback_data': f'days_nav_{page + 1}'})
        else:
            nav_row.append({'text': ' ', 'callback_data': 'ignore'})

        keyboard.append(nav_row)
        return {'inline_keyboard': keyboard}

    def _handle_day_picker_callback(self, chat_id, data, message, query_id):
        """Обрабатывает все нажатия на кнопки выбора дней."""
        send_request('answerCallbackQuery', {'callback_query_id': query_id})

        if data.startswith('days_nav_'):
            page = int(data.split('_')[-1]) 
            day_picker_keyboard = self.create_day_picker_keyboard(page=page)
            edit_message_text(chat_id, message['message_id'],
                              text=get_text('choose_number_of_days', chat_id),
                              reply_markup=day_picker_keyboard)
            return

        if data == 'days_cancel':
            delete_message(chat_id, message['message_id'])
            send_message(chat_id, get_text('operation_cancelled', chat_id),
                       reply_markup=get_main_menu_keyboard(chat_id))
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            return

        if data.startswith('days_select_'):
            num_days = int(data.split('_')[-1])
            user_session_data = self.user_data.get(chat_id, {})
            car_name = user_session_data.get('car')
            car_info = get_available_cars().get(car_name, {})
            
            if not car_info or not car_info.get('prices'):
                send_message(chat_id, "❗️Критическая ошибка: информация о ценах не найдена. Начните заново: /rent")
                delete_message(chat_id, message['message_id'])
                return
            
            price_per_day = car_info['prices'][0]
            total_price = price_per_day * num_days
            
            period_text = f"{num_days} дн."
            price_text = f"{total_price} сомони"
            
            final_user_message = (f"*{get_text('choose_number_of_days', chat_id)}*\n\n"
                                  f"✅ Выбрано: *{period_text}*\n"
                                  f"💰 Итоговая стоимость: *{price_text}*")
            edit_message_text(chat_id, message['message_id'], final_user_message, reply_markup=None)
            
            self._finalize_rental_request(chat_id, period_text, price_text)
            return

    def _finalize_rental_request(self, chat_id, period_text, price_text):
        """
        Универсальная функция для завершения процесса аренды.
        Сохраняет заявку, уведомляет пользователя и админов, очищает состояние.
        """
        user_session_data = self.user_data.get(chat_id, {})
        car_name = user_session_data.get('car')

        if not car_name:
            send_message(chat_id, "❗️ Произошла ошибка сессии. Пожалуйста, начните выбор автомобиля заново: /rent")
            return

        all_users = load_from_json(USERS_FILE)
        user_info = all_users.get(chat_id, {})
        client_profile = user_info.get('client_profile', {})
        
        user_session_data['client_name'] = client_profile.get('name', 'Не указан')
        user_session_data['client_phone'] = client_profile.get('phone', 'Не указан')
        user_session_data['client_license'] = client_profile.get('license', 'Не указан')

        user_session_data['period'] = period_text
        user_session_data['price'] = price_text
        user_session_data['status'] = 'pending'
        user_session_data['timestamp'] = datetime.now().isoformat()

        rentals = load_from_json(RENTALS_FILE)
        rental_id = str(int(time.time()))
        rentals[rental_id] = user_session_data
        save_to_json(rentals, RENTALS_FILE)

        send_message(chat_id, get_text('rental_request_accepted', chat_id, id=rental_id),
                     reply_markup=get_main_menu_keyboard(chat_id))
        self._notify_admins_new_rental(rental_id, user_session_data)

        self.user_states.pop(chat_id, None)
        self.user_data.pop(chat_id, None)
    
    
    def _process_callback_query(self, callback_query):
        query_id, from_user, data, message = callback_query['id'], callback_query['from'], callback_query['data'], callback_query['message']
        chat_id = str(from_user['id'])
        state = self.user_states.get(chat_id)
        
        if data == 'show_main_menu':
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            # Удаляем сообщение с выбором роли
            delete_message(chat_id, message['message_id'])
            # Показываем главное меню
            send_message(chat_id, get_text('welcome', chat_id, name=from_user.get('first_name', get_text('guest', chat_id))), reply_markup=get_main_menu_keyboard(chat_id))
            return
        
        if data.startswith('admin_reply_'):
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            
            target_user_id = data.split('_')[-1]
            users = load_from_json(USERS_FILE)
            target_user_info = users.get(target_user_id, {})
            target_user_name = self._get_user_display_name(target_user_info)

            self.user_states[chat_id] = State.ADMIN_IN_CHAT_WITH_USER
            self.user_data[chat_id] = {
                'target_user_id': target_user_id,
                'target_user_name': target_user_name
            }

            end_chat_btn_text = get_text('admin_end_chat_button', chat_id, user_name=target_user_name)
            end_chat_keyboard = create_keyboard([[end_chat_btn_text]], one_time=False)

            original_text = message.get('caption', message.get('text', ''))

            edit_message_text(chat_id, message['message_id'], original_text, reply_markup=None)
            send_message(chat_id, get_text('admin_enter_chat_session', chat_id, user_name=target_user_name), reply_markup=end_chat_keyboard)
            return

        if data.startswith('user_error_'):
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            target_id = data.split('_')[-1]

            self.user_states[chat_id] = State.AWAITING_CUSTOM_ERROR_MESSAGE
            self.user_data[chat_id] = {'target_user_id': target_id}

            prompt_text = get_text('admin_prompt_custom_error', chat_id, target_id=target_id)
            edit_message_text(chat_id, message['message_id'], prompt_text, reply_markup=None)
            return 
        
        if data.startswith('checklist_') and state == State.AWAITING_CHECKLIST_INPUT:
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            session = self.user_data.get(chat_id)
            
            if data.startswith('checklist_car_'):
                session['car_plate'] = data.replace('checklist_car_', '')
                self._process_checklist_step(chat_id, message, is_callback=True)
            elif data.startswith('checklist_answer_'):
                self._process_checklist_step(chat_id, message, is_callback=True, callback_data=data)
            return

        if (chat_id in get_admins() or self._is_staff(chat_id)) and '_' in data:
            parts = data.split('_', 2)
            if len(parts) > 1:
                action_type = parts[0]
                if action_type in ['rental', 'user', 'car']:
                    if action_type == 'rental':
                        self._handle_rental_callback(chat_id, message, parts[1], parts[2], query_id)
                    elif action_type == 'user':
                        self._handle_user_approval_callback(chat_id, message, parts[1], parts[2], query_id)
                    elif action_type == 'car':
                        self._handle_car_management_callback(chat_id, message, parts[1], parts[2], query_id)
                    return 
                
        if data.startswith('rent_type_'):
            self._handle_rent_type_selection(chat_id, data, message, query_id)
            return
        if data.startswith('days_'):
            self._handle_day_picker_callback(chat_id, data, message, query_id)
            return
        
        elif data.startswith('long_') and state == State.AWAITING_RENT_PERIOD:
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            
            period_text = ""
            if data == 'long_from_6_months':
                period_text = get_text('from_6_months', chat_id)
            elif data == 'long_up_to_1_year':
                period_text = get_text('up_to_1_year', chat_id)
            # --- НАЧАЛО ИСПРАВЛЕНИЯ ---
            elif data == 'long_over_1_year':
                period_text = "От 1 года и более" # Добавлена обработка этого варианта
            # --- КОНЕЦ ИСПРАВЛЕНИЯ ---
            
            price_text = "Договорная"

            edit_message_text(chat_id, message['message_id'], f"{get_text('choose_rent_period', chat_id)}\n\n✅ Выбрано: *{period_text}*", reply_markup=None)
            
            self._finalize_rental_request(chat_id, period_text, price_text)
            return


        if data == 'start_as_client':
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            role_name = get_text('role_client', chat_id)
            edit_message_text(chat_id, message['message_id'], f"{message.get('text', '')}\n\n{get_text('role_selection_made', chat_id, role_name=role_name)}")
            self.user_states[chat_id] = State.AWAITING_NAME
            self.user_data[chat_id] = {
                'username': from_user.get('username'),
                'first_name': from_user.get('first_name'),
                'last_name': from_user.get('last_name')
            }
            send_message(chat_id, get_text('start_registration', chat_id), reply_markup=get_cancel_keyboard(chat_id))
            return

        elif data == 'start_as_owner':
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            role_name = get_text('role_owner', chat_id)
            edit_message_text(chat_id, message['message_id'], f"{message.get('text', '')}\n\n{get_text('role_selection_made', chat_id, role_name=role_name)}")
            self.user_states[chat_id] = State.AWAITING_OWNER_REGISTRATOR_NAME
            self.user_data[chat_id] = {'username': from_user.get('username')}
            send_message(chat_id, get_text('owner_start_registration', chat_id), reply_markup=get_cancel_keyboard(chat_id))
            return

        elif data == 'profile_edit_as_client':
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            users = load_from_json(USERS_FILE)
            user_info = users.get(chat_id, {})

            if 'client' in user_info.get('active_roles', []):
                delete_message(chat_id, message['message_id'])
                self._start_profile_edit(chat_id, 'client') 
            else:
                edit_message_text(chat_id, message['message_id'], message['text'])
                send_message(chat_id, get_text('not_a_client_error', chat_id))
            return

        elif data == 'profile_edit_as_owner':
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            users = load_from_json(USERS_FILE)
            user_info = users.get(chat_id, {})

            if 'owner' in user_info.get('active_roles', []):
                delete_message(chat_id, message['message_id'])
                self._start_profile_edit(chat_id, 'owner')
            else:
                edit_message_text(chat_id, message['message_id'], message['text'])
                send_message(chat_id, get_text('not_a_owner_error', chat_id))
            return

        elif data.startswith('edit_field_') and state == State.AWAITING_PROFILE_EDIT_CHOICE:
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            field_key = data.replace('edit_field_', '')
            
            if field_key == 'city':
                return

            field_name_text_key = f'field_{field_key}'
            user_friendly_field_name = get_text(field_name_text_key, chat_id, default_key=field_key)
            self.user_data[chat_id]['edit_field'] = field_key
            self.user_states[chat_id] = State.AWAITING_PROFILE_EDIT_VALUE
            edit_message_text(chat_id, message['message_id'], get_text('prompt_new_value', chat_id, field_name=user_friendly_field_name))
            return
            
        elif data.startswith('city_edit_') and state == State.AWAITING_PROFILE_EDIT_CITY:
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            new_city = data.replace('city_edit_', '')
            users = load_from_json(USERS_FILE)
            if chat_id in users:
                users[chat_id]['city'] = new_city
                save_to_json(users, USERS_FILE)
                delete_message(chat_id, message['message_id'])
                send_message(chat_id, get_text('profile_updated_successfully', chat_id))
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                self._handle_commands(chat_id, '/profile', {'from': {'id': chat_id}})
            return

        elif data.startswith('city_') and state == State.AWAITING_CITY:
            selected_city_key = data.replace('city_', '')
            lang = get_user_lang(chat_id)
            selected_city_name = "N/A"
            for city in CITIES:
                if city['key'] == selected_city_key:
                    selected_city_name = city[lang]
                    break

            self.user_data[chat_id]['city'] = selected_city_name
            self.user_states[chat_id] = State.AWAITING_ADDRESS
            send_request('answerCallbackQuery', {'callback_query_id': query_id})

            confirmation_text = get_text('city_selected', chat_id, city_name=selected_city_name)
            edit_message_text(chat_id, message['message_id'], confirmation_text, reply_markup=None)

            send_message(chat_id, get_text('ask_address', chat_id, city=selected_city_name), reply_markup=get_cancel_keyboard(chat_id))
            return

        elif data.startswith('cat_') and state == State.AWAITING_LICENSE_CATEGORY:
            category = data.split('_')[1]
            self.user_data[chat_id]['license_category'] = category
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            edit_message_text(chat_id, message['message_id'], f"{message.get('text', '').splitlines()[0]}\n\n{get_text('category_selected', chat_id, category=category)}")
            self.user_states[chat_id] = State.AWAITING_PREFERRED_CAR_TYPE
            keyboard = create_keyboard([[{'text': get_text('ice_car', chat_id), 'callback_data': 'reg_car_type_ice'},{'text': get_text('electric_car', chat_id), 'callback_data': 'reg_car_type_electric'}]], inline=True)
            send_message(chat_id, get_text('ask_preferred_car_type', chat_id), reply_markup=keyboard)
            return
        
        if data.startswith('reg_'):
            current_message_text = message.get('text', '').splitlines()[0]
            edit_message_text(chat_id, message['message_id'], f"{current_message_text}\n\n{get_text('choice_selected', chat_id)}")
            
            if state == State.AWAITING_PREFERRED_CAR_TYPE:
                self.user_data[chat_id]['preferred_car_type'] = get_text('ice_car', chat_id) if data == 'reg_car_type_ice' else get_text('electric_car', chat_id)
                self.user_states[chat_id] = State.AWAITING_TAXI_EXPERIENCE
                yes_no_keyboard = create_keyboard([[{'text': get_text('yes', chat_id), 'callback_data': 'reg_experience_yes'},{'text': get_text('no', chat_id), 'callback_data': 'reg_experience_no'}]], inline=True)
                send_message(chat_id, get_text('ask_taxi_experience', chat_id), reply_markup=yes_no_keyboard)
            
            elif state == State.AWAITING_TAXI_EXPERIENCE:
                has_experience = data == 'reg_experience_yes'
                self.user_data[chat_id]['taxi_experience'] = get_text('yes', chat_id) if has_experience else get_text('no', chat_id)
                if has_experience:
                    self.user_states[chat_id] = State.AWAITING_DRIVER_ID
                    send_message(chat_id, get_text('ask_driver_id', chat_id), reply_markup=get_cancel_keyboard(chat_id))
                else: 
                    self.user_data[chat_id]['driver_id'] = 'нет'
                    self.user_states[chat_id] = State.AWAITING_DEPOSIT
                    yes_no_keyboard = create_keyboard([[{'text': get_text('yes', chat_id), 'callback_data': 'reg_deposit_yes'},{'text': get_text('no', chat_id), 'callback_data': 'reg_deposit_no'}]], inline=True)
                    send_message(chat_id, get_text('ask_deposit', chat_id), reply_markup=yes_no_keyboard)

            elif state == State.AWAITING_DEPOSIT:
                self.user_data[chat_id]['deposit'] = get_text('yes', chat_id) if data == 'reg_deposit_yes' else get_text('no', chat_id)
                self.user_states[chat_id] = State.AWAITING_CONSENT 
                consent_keyboard = create_keyboard([
                    [{'text': get_text('consent_confirm_btn', chat_id), 'callback_data': 'consent_confirm_client'}]
                ], inline=True)
                send_message(chat_id, get_text('consent_prompt', chat_id), reply_markup=consent_keyboard)

            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            return
            
        elif data.startswith('setlang_'):
            lang_code = data.split('_')[1]
            users = load_from_json(USERS_FILE)
            
            # Получаем информацию о пользователе или создаем пустую запись
            user_info = users.get(chat_id, {})
            user_info['lang'] = lang_code
            users[chat_id] = user_info
            save_to_json(users, USERS_FILE)

            # Отвечаем на callback, чтобы убрать "загрузку" с кнопки
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            
            # Редактируем сообщение с выбором языка, показывая подтверждение
            edit_message_text(chat_id, message['message_id'], get_text('language_set', chat_id))
            
            # Определяем, зарегистрирован ли пользователь
            is_registered = user_info.get('status') or user_info.get('active_roles')
            is_admin = chat_id in get_admins()
            
            # Если пользователь уже зарегистрирован (или является админом), показываем главное меню
            if is_registered or is_admin:
                send_message(chat_id, get_text('main_menu_prompt', chat_id), 
                               reply_markup=get_main_menu_keyboard(chat_id))
                if is_admin:
                    send_message(chat_id, "👑 *Вы вошли как администратор.* Используйте /adminhelp для списка команд.")
            # Если это новый пользователь, показываем выбор роли
            else:
                user_name = from_user.get('first_name') or from_user.get('username') or get_text('guest', chat_id)
                welcome_text = get_text('welcome', chat_id, name=user_name)
                prompt_text = get_text('choose_your_role', chat_id)

                role_selection_keyboard = create_keyboard([
                    [
                        {'text': get_text('start_btn_client', chat_id), 'callback_data': 'start_as_client'},
                        {'text': get_text('start_btn_owner', chat_id), 'callback_data': 'start_as_owner'}
                    ],
                    [
                        {'text': get_text('go_to_main_menu', chat_id), 'callback_data': 'show_main_menu'}
                    ]
                ], inline=True)
                
                send_message(chat_id, f"{welcome_text}\n\n{prompt_text}", reply_markup=role_selection_keyboard)
            
            return
        
        elif data.startswith('owner_trans_') and state == State.AWAITING_OWNER_CAR_TRANSMISSION:
            choice_key = 'transmission_auto' if data == 'owner_trans_auto' else 'transmission_manual'
            choice_text = get_text(choice_key, chat_id)

            self.user_data[chat_id]['car_transmission'] = choice_text
            send_request('answerCallbackQuery', {'callback_query_id': query_id})

            confirmation_msg = get_text('transmission_selected', chat_id, choice_text=choice_text)
            edit_message_text(chat_id, message['message_id'], f"{message.get('text').splitlines()[0]}\n\n{confirmation_msg}")

            self.user_states[chat_id] = State.AWAITING_OWNER_CAR_INSURANCE
            yes_no_keyboard = create_keyboard([
                [{'text': get_text('yes', chat_id), 'callback_data': 'owner_ins_yes'},
                 {'text': get_text('no', chat_id), 'callback_data': 'owner_ins_no'}]
            ], inline=True)
            send_message(chat_id, get_text('owner_ask_car_insurance', chat_id), reply_markup=yes_no_keyboard)
            return

        elif data.startswith('owner_cartype_') and state == State.AWAITING_OWNER_CAR_TYPE:
            type_map = {'owner_cartype_ice': 'ДВС','owner_cartype_electric': 'Электромобиль','owner_cartype_hybrid': 'Гибрид'}
            choice_text = type_map.get(data, "Не определено")
            self.user_data[chat_id]['car_type'] = choice_text
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            edit_message_text(chat_id, message['message_id'], f"{message.get('text').splitlines()[0]}\n\n✅ *Выбрано: {choice_text}*")

            self.user_states[chat_id] = State.AWAITING_OWNER_CAR_PHOTOS
            finish_photo_keyboard = create_keyboard([[get_text('finish_photo_upload_btn', chat_id)]], one_time=False)
            send_message(chat_id, get_text('owner_ask_car_photos', chat_id), reply_markup=finish_photo_keyboard)
            return
        
        elif data.startswith('consent_confirm_') and state == State.AWAITING_CONSENT:
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            delete_message(chat_id, message['message_id'])

            if data == 'consent_confirm_client':
                self.user_states[chat_id] = State.AWAITING_PASSPORT_PHOTO
                send_message(chat_id, get_text('ask_passport_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))
            elif data == 'consent_confirm_owner':
                self.user_states[chat_id] = State.AWAITING_TECH_PASSPORT_PHOTO
                send_message(chat_id, get_text('ask_tech_passport_photo', chat_id), reply_markup=get_cancel_keyboard(chat_id))
            return

        elif data.startswith('owner_ins_') and state == State.AWAITING_OWNER_CAR_INSURANCE:
            choice = (data == 'owner_ins_yes')
            self.user_data[chat_id]['car_insurance'] = get_text('yes', chat_id) if choice else get_text('no', chat_id)
            
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            edit_message_text(chat_id, message['message_id'], f"{message.get('text').splitlines()[0]}\n\n{get_text('choice_selected', chat_id)}")

            self.user_states[chat_id] = State.AWAITING_OWNER_GPS_CAMERA
            yes_no_keyboard = create_keyboard([
                [{'text': get_text('yes', chat_id), 'callback_data': 'owner_gps_yes'},
                 {'text': get_text('no', chat_id), 'callback_data': 'owner_gps_no'}]
            ], inline=True)
            send_message(chat_id, get_text('owner_ask_gps_camera', chat_id), reply_markup=yes_no_keyboard)
            return

        elif data.startswith('owner_gps_') and state == State.AWAITING_OWNER_GPS_CAMERA:
            choice = (data == 'owner_gps_yes')
            self.user_data[chat_id]['gps_camera_ready'] = get_text('yes', chat_id) if choice else get_text('no', chat_id)

            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            edit_message_text(chat_id, message['message_id'], f"{message.get('text').splitlines()[0]}\n\n{get_text('choice_selected', chat_id)}")

            self.user_states[chat_id] = State.AWAITING_OWNER_CAR_COLOR
            send_message(chat_id, get_text('owner_ask_car_color', chat_id), reply_markup=get_cancel_keyboard(chat_id))
            return
                

        elif data.startswith('user_error_'):
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            target_id = data.split('_')[-1]

            self.user_states[chat_id] = State.AWAITING_CUSTOM_ERROR_MESSAGE
            self.user_data[chat_id] = {'target_user_id': target_id}

            prompt_text = get_text('admin_prompt_custom_error', chat_id, target_id=target_id)
            edit_message_text(chat_id, message['message_id'], prompt_text, reply_markup=None)
            return

        elif data.startswith('user_'):
            try:
                action_type, action, target_id = data.split('_', 2)
                if action_type == 'user':
                    self._handle_user_approval_callback(chat_id, message, action, target_id, query_id)
            except ValueError:
                send_request('answerCallbackQuery', {'callback_query_id': query_id, 'text': '❗️Некорректные данные.'})
            return
        
            
        if data.startswith('listcars_brand_'):
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            
            brand_name = data.split('_')[-1]
            edit_message_text(chat_id, message['message_id'], f"✅ *Выбрана марка:* {brand_name}", reply_markup=None)

            selected_brand = data.replace('listcars_brand_', '')
            available_cars = get_available_cars()

            cars_of_brand = {
                name: details for name, details in available_cars.items()
                if name.lower().startswith(selected_brand.lower())
            }

            if not cars_of_brand:
                send_message(chat_id, f"❗️Машины марки *{selected_brand}* не найдены.")
                return
            
            send_message(chat_id, get_text('loading_car_list', chat_id))
            
            for name, details in cars_of_brand.items():
                caption = (f"*{name}*\n"
                           f"{details.get('description', '')}\n\n"
                           f"💰 *Прайс-лист:*\n"
                           f"• День: *{details['prices'][0]} сомони*\n"
                           f"• Неделя: *{details['prices'][1]} сомони*\n"
                           f"• Месяц: *{details['prices'][2]} сомони*\n\n"
                           f"🔧 *Характеристики:*\n"
                           f"• Год выпуска: {details.get('year', 'N/A')}\n"
                           f"• Тип топлива: {details.get('fuel_type', 'N/A')}\n"
                           f"• Двигатель: {details.get('engine', 'N/A')}\n"
                           f"• Коробка передач: {details.get('transmission', 'N/A')}\n"
                           f"• Места: {details.get('seats', 'N/A')}")

                image_paths = details.get('local_image_paths', [])
                
                if image_paths:
                    send_media_group(chat_id, image_paths, caption=caption)
                else:
                    send_message(chat_id, caption)
                    
                time.sleep(0.8) # Немного увеличим задержку для отправки альбомов
                
            send_message(chat_id, get_text('full_car_list_end', chat_id))
            return 
            
        elif chat_id in get_admins() or self._is_staff(chat_id):
             
            if data.startswith('listcars_brand_'):
                send_request('answerCallbackQuery', {'callback_query_id': query_id})
            
            edit_message_text(chat_id, message['message_id'], f"✅ *Выбрана марка:* {data.split('_')[-1]}", reply_markup=None)

            selected_brand = data.replace('listcars_brand_', '')
            available_cars = get_available_cars()

            cars_of_brand = {
                name: details for name, details in available_cars.items()
                if name.lower().startswith(selected_brand.lower())
            }

            if not cars_of_brand:
                send_message(chat_id, f"❗️Машины марки *{selected_brand}* не найдены.")
                return
            
            send_message(chat_id, get_text('loading_car_list', chat_id))
            
            for name, details in cars_of_brand.items():
                caption = (f"*{name}*\n_{details['description']}_\n\n"
                           f"*{get_text('car_details_price', chat_id)}*\n"
                           f"{get_text('price_day', chat_id, price=details['prices'][0])}\n"
                           f"{get_text('price_week', chat_id, price=details['prices'][1])}\n"
                           f"{get_text('price_month', chat_id, price=details['prices'][2])}\n\n"
                           f"*{get_text('car_specs', chat_id)}*\n"
                           f"{get_text('car_year', chat_id, year=details.get('year', 'N/A'))}\n"
                           f"{get_text('car_fuel', chat_id, fuel_type=details.get('fuel_type', 'N/A'))}\n"
                           f"{get_text('car_engine', chat_id, engine=details.get('engine', 'N/A'))}\n"
                           f"{get_text('car_transmission', chat_id, transmission=details.get('transmission', 'N/A'))}\n"
                           f"{get_text('car_seats', chat_id, seats=details.get('seats', 'N/A'))}")
                           
                if details.get('local_image_path') and os.path.exists(details['local_image_path']):
                    send_photo(chat_id, details['local_image_path'], caption=caption)
                else:
                    send_message(chat_id, caption)
                    
                time.sleep(0.5)
                
            send_message(chat_id, get_text('full_car_list_end', chat_id))
            return 
             
            try:
                action_type, action, target_id = data.split('_', 2)
            except ValueError:
                send_request('answerCallbackQuery', {'callback_query_id': query_id, 'text': '❗️Некорректные данные.'})
                return
                
            if action_type == 'rental':
                self._handle_rental_callback(chat_id, message, action, target_id, query_id)
            elif action_type == 'car':
                self._handle_car_management_callback(chat_id, message, action, target_id, query_id)
                
        else:
            send_request('answerCallbackQuery', {'callback_query_id': query_id})

    def _is_staff(self, chat_id):
        staff = get_staff()
        return str(chat_id) in staff

    def _handle_rental_callback(self, admin_id, message, action, rental_id, query_id):
        rentals = load_from_json(RENTALS_FILE)
        if rental_id not in rentals:
            edit_message_text(admin_id, message['message_id'], 
                            f"{message.get('caption', message.get('text', ''))}\n\n*❗️ Заявка №{rental_id} не найдена.*")
            return
            
        rental_info = rentals[rental_id]
        if rental_info['status'] != 'pending':
            send_request('answerCallbackQuery', {'callback_query_id': query_id, 
                                               'text': f'Заявка уже обработана. Статус: {rental_info["status"]}'})
            return
            
        new_status = 'approved' if action == 'approve' else 'rejected'
        rentals[rental_id]['status'] = new_status
        save_to_json(rentals, RENTALS_FILE)
        
        status_emoji = '✅' if new_status == 'approved' else '❌'
        admin_name = message['from'].get('first_name', 'Админ')
        original_caption = message.get('caption', message.get('text', ''))
        edit_message_text(admin_id, message['message_id'], 
                         f"{original_caption}\n\n*{status_emoji} Статус '{new_status}' назначен админом {admin_name}.*", 
                         reply_markup=None)
        
        user_id = rental_info['chat_id']
        user_message = get_text('rental_status_changed', user_id, id=rental_id, car=rental_info['car'], 
                              emoji=status_emoji, status=new_status.capitalize())
        user_message += get_text('status_approved_details', user_id) if new_status == 'approved' else get_text('status_rejected_details', user_id)
        send_message(user_id, user_message)

        if new_status == 'approved':
            owner_cars = load_from_json(OWNER_CARS_FILE)
            users = load_from_json(USERS_FILE)
            driver_info = users.get(user_id)
            
            owner_id_found = None
            car_info_found = None
            for owner_id, owner_car_data in owner_cars.items():
                if owner_car_data.get('car_brand') == rental_info['car']:
                    owner_id_found = owner_id
                    car_info_found = owner_car_data
                    owner_cars[owner_id]['current_driver_id'] = user_id
                    save_to_json(owner_cars, OWNER_CARS_FILE)
                    break
            
            if owner_id_found and driver_info and car_info_found:
                self.notifications.notify_owner_of_driver_assignment(owner_id_found, driver_info, car_info_found)
        
        elif new_status == 'rejected':
            car_info = get_available_cars().get(rental_info['car'], {})
            if car_info:
                self.notifications.notify_all_drivers_about_free_car(car_info)

    def _handle_car_management_callback(self, admin_id, message, action, car_name_encoded, query_id):
        car_name = car_name_encoded.replace('%20', ' ')
        cars = get_available_cars()
        
        if car_name not in cars and action != 'cancel':
            edit_message_text(admin_id, message['message_id'], 
                            f"{message['text']}\n\n*❗️Машина '{car_name}' не найдена.*", 
                            reply_markup=None)
            return
            
        if action == 'delete':
            keyboard = {
                'inline_keyboard': [
                    [
                        {'text': '✅ Да, удалить', 'callback_data': f'car_confirmdelete_{car_name_encoded}'},
                        {'text': '❌ Отмена', 'callback_data': f'car_cancel_0'}
                    ]
                ]
            }
            edit_message_text(admin_id, message['message_id'], 
                            f"Вы уверены, что хотите удалить *{car_name}*?", 
                            reply_markup=keyboard)
        elif action == 'confirmdelete':
            if car_name in cars:
                del cars[car_name]
                save_to_json(cars, CARS_FILE)
                edit_message_text(admin_id, message['message_id'], 
                                f"✅ Автомобиль *{car_name}* удален.", 
                                reply_markup=None)
            else:
                edit_message_text(admin_id, message['message_id'], 
                                f"❗️ Автомобиль *{car_name}* уже был удален.", 
                                reply_markup=None)
        elif action == 'cancel':
            edit_message_text(admin_id, message['message_id'], "❌ Действие отменено.", reply_markup=None)

    def _handle_user_approval_callback(self, admin_id, message, action, target_id, query_id):
        users = load_from_json(USERS_FILE)
        
        if target_id not in users:
            edit_message_text(admin_id, message['message_id'],
                              f"{message.get('caption', message.get('text', ''))}\n\n*❗️ Пользователь не найден.*")
            send_request('answerCallbackQuery', {'callback_query_id': query_id})
            return

        user_data = users[target_id]
        if user_data.get('status') != 'pending':
            send_request('answerCallbackQuery', {'callback_query_id': query_id,
                                               'text': get_text('user_already_processed', admin_id), 'show_alert': True})
            return

        new_status = 'active' if action == 'approve' else 'rejected'
        users[target_id]['status'] = new_status
        save_to_json(users, USERS_FILE)

        admin_user_info = load_from_json(USERS_FILE).get(admin_id, {})
        admin_name = admin_user_info.get('name', 'Администратор')

        if new_status == 'active':
            send_message(target_id, get_text('registration_approved', target_id))
            new_admin_text = get_text('user_approved_by_admin', admin_id, admin_name=admin_name)
        else:
            user_roles = user_data.get('active_roles', [])
            if 'owner' in user_roles:
                rejection_key = 'registration_rejected_owner'
            else:
                rejection_key = 'registration_rejected_client'

            send_message(target_id, get_text(rejection_key, target_id), parse_mode=None)

            new_admin_text = get_text('user_rejected_by_admin', admin_id, admin_name=admin_name)

        original_caption = message.get('caption', message.get('text', ''))
        edit_message_text(admin_id, message['message_id'],
                         f"{original_caption}\n\n*--- ОБРАБОТАНО ---\n{new_admin_text}*",
                         reply_markup=None)

        send_request('answerCallbackQuery', {'callback_query_id': query_id})       

    def _handle_new_car_photo(self, chat_id, file_id):
        data = self.user_data.get(chat_id, {})
        file_info_response = requests.get(f'{BASE_URL}/getFile?file_id={file_id}').json()
        
        if not file_info_response or not file_info_response['ok']:
            send_message(chat_id, "❗️Не удалось получить информацию о файле. Попробуйте снова.")
            return
            
        file_path = file_info_response['result']['file_path']
        download_url = f'https://api.telegram.org/file/bot{TOKEN}/{file_path}'
        
        is_editing = self.user_data.get(chat_id, {}).get('edit_field') == 'фото'
        car_name_for_file = data.get('selected_car') if is_editing else data.get('car_name', 'new_car')
        unique_filename = f"images/{car_name_for_file.replace(' ', '_').lower()}_{int(time.time())}.jpg"
        
        try:
            image_response = requests.get(download_url, stream=True)
            image_response.raise_for_status()
            
            with open(unique_filename, 'wb') as f:
                for chunk in image_response.iter_content(chunk_size=8192):
                    f.write(chunk)
                    
            data['local_image_path'] = unique_filename
            
            if is_editing:
                available_cars = get_available_cars()
                selected_car = data['selected_car']
                if selected_car in available_cars:
                    available_cars[selected_car]['local_image_path'] = unique_filename
                    save_to_json(available_cars, CARS_FILE)
                    send_message(chat_id, f"✅ Фото для *{selected_car}* обновлено.")
                    self.user_states.pop(chat_id, None)
                    self.user_data.pop(chat_id, None)
            else:
                self.user_states[chat_id] = State.AWAITING_NEW_CAR_PRICES
                send_message(chat_id, "🔢 Введите цены (день, неделя, месяц) через пробел (например: `360 2160 7200`):")
                
        except requests.exceptions.RequestException as e:
            send_message(chat_id, f"❗️Ошибка при загрузке фото: {e}. Попробуйте снова.")
        except IOError as e:
            send_message(chat_id, f"❗️Ошибка при сохранении фото: {e}. Попробуйте снова.")

    def _notify_admins_new_user(self, user_id, user_data):
        admins = get_admins()
        if not admins: return

        client_profile = user_data.get('client_profile', {})
        if not client_profile:
            print(f"ОШИБКА: client_profile не найден для пользователя {user_id} при отправке админам.")
            return

        user_name = escape_markdown(client_profile.get('name', 'N/A'))
        username_str = f"(@{escape_markdown(user_data.get('username'))})" if user_data.get('username') else ""
        phone = escape_markdown(client_profile.get('phone', 'N/A'))
        city = escape_markdown(client_profile.get('city', 'N/A'))
        address = escape_markdown(client_profile.get('address', 'N/A'))
        dob_str = client_profile.get('dob', 'N/A')
        age_display = dob_str
        if dob_str != 'N/A':
            try:
                user_dob = datetime.strptime(dob_str, '%d.%m.%Y')
                today = datetime.now()
                calculated_age = today.year - user_dob.year - ((today.month, today.day) < (user_dob.month, user_dob.day))
                age_display = f"{dob_str} ({calculated_age} лет)"
            except ValueError:
                age_display = dob_str
        age_display = escape_markdown(age_display)
        experience = escape_markdown(client_profile.get('driving_experience', 'N/A'))
        license_num = escape_markdown(client_profile.get('license', 'N/A'))
        license_cat = escape_markdown(client_profile.get('license_category', 'N/A'))
        pref_car = escape_markdown(client_profile.get('preferred_car_type', 'N/A'))
        taxi_exp = escape_markdown(client_profile.get('taxi_experience', 'N/A'))
        driver_id = escape_markdown(client_profile.get('driver_id', 'N/A'))
        deposit = escape_markdown(client_profile.get('deposit', 'N/A'))

        message_text = (f"✅ *Зарегистрирован новый КЛИЕНТ! (Ожидает одобрения)*\n\n"
                        f"👤 *ФИО:* {user_name} {username_str}\n"
                        f"🆔 *Chat ID:* `{user_id}`\n\n"
                        f"--- *АНКЕТА* ---\n"
                        f"📞 *Телефон:* `{phone}`\n"
                        f"📍 *Город:* {city}\n"
                        f"🏠 *Адрес:* {address}\n"
                        f"🎂 *Дата рождения (возраст):* {age_display}\n"
                        f"짬 *Стаж вождения:* {experience} лет\n"
                        f"💳 *Номер В/У:* `{license_num}`\n"
                        f"📋 *Категория В/У:* {license_cat}\n\n"
                        f"--- *ПРЕДПОЧТЕНИЯ* ---\n"
                        f"🚗 *Предпочитаемый авто:* {pref_car}\n"
                        f"🚕 *Опыт в такси:* {taxi_exp}\n"
                        f"🆔 *ID Водителя:* {driver_id}\n"
                        f"💰 *Готов к залогу:* {deposit}")

        keyboard = {
            'inline_keyboard': [
                [
                    {'text': '✅ Одобрить', 'callback_data': f'user_approve_{user_id}'},
                    {'text': '❌ Отклонить', 'callback_data': f'user_reject_{user_id}'}
                ],
                [
                    {'text': get_text('admin_btn_report_error', user_id), 'callback_data': f'user_error_{user_id}'}
                ]
            ]
        }

        for admin_id in admins:
            send_message(admin_id, message_text)

            if client_profile.get('passport_photo_id'):
                send_photo(admin_id, client_profile['passport_photo_id'], caption=f"📄 Паспорт, лицевая сторона (ID: {user_id})")

            if client_profile.get('passport_back_photo_id'):
                send_photo(admin_id, client_profile['passport_back_photo_id'], caption=f"📄 Паспорт, обратная сторона (ID: {user_id})")

            if client_profile.get('license_front_photo_id'):
                send_photo(admin_id, client_profile['license_front_photo_id'], caption=f"💳 В/У, лицевая сторона (ID: {user_id})")
            if client_profile.get('license_back_photo_id'):
                send_photo(admin_id, client_profile['license_back_photo_id'], caption=f"💳 В/У, обратная сторона (ID: {user_id})")

            send_message(admin_id, f"Принять решение по пользователю {user_name} (ID: `{user_id}`)?", reply_markup=keyboard)

    def _notify_admins_new_owner_car(self, chat_id, user_data):
        admins = get_admins()
        if not admins: return

        owner_profile = user_data.get('owner_profile', {})
        car_info = load_from_json(OWNER_CARS_FILE).get(chat_id, {})
        if not owner_profile:
            print(f"ОШИБКА: owner_profile не найден для пользователя {chat_id} при отправке админам.")
            return

        registrator_name = escape_markdown(owner_profile.get('registrator_name', 'N/A'))
        owner_name = escape_markdown(owner_profile.get('owner_name', 'N/A'))
        username_str = f"(@{escape_markdown(user_data.get('username'))})" if user_data.get('username') else ""
        phone = escape_markdown(owner_profile.get('phone', 'N/A'))
        car_brand = escape_markdown(car_info.get('car_brand', 'N/A'))
        car_plate = escape_markdown(car_info.get('car_plate', 'N/A'))
        car_color = escape_markdown(car_info.get('car_color', 'N/A'))
        car_year = escape_markdown(car_info.get('car_year', 'N/A'))
        car_mileage = escape_markdown(car_info.get('car_mileage', 'N/A'))
        car_transmission = escape_markdown(car_info.get('car_transmission', 'N/A'))
        car_type = escape_markdown(car_info.get('car_type', 'N/A'))
        car_vin = escape_markdown(car_info.get('car_vin', 'N/A'))
        car_insurance = escape_markdown(owner_profile.get('car_insurance', 'N/A'))
        gps_camera_ready = escape_markdown(owner_profile.get('gps_camera_ready', 'N/A'))

        desired_price_text = f"💰 *Желаемая цена (день):* {car_info.get('desired_price_day', 'Не указана')} сомони\n\n"
        message_text = (f"🆕 *Новая заявка от СОБСТВЕННИКА авто!*\n\n"             
                        f"👤 *ФИО Регистратора:* {registrator_name}{username_str}\n"
                        f"👤 *ФИО Собственника:* {owner_name}\n"
                        f"📞 *Телефон:* `{phone}`\n"
                        f"🆔 *Chat ID:* `{chat_id}`\n\n"
                        f"--- *Данные по автомобилю* ---\n"
                        f"🚗 *Марка/модель:* {car_brand}\n"
                        f"🔢 *Гос. номер:* `{car_plate}`\n"
                        f"🎨 *Цвет:* {car_color}\n"
                        f"📅 *Год выпуска:* {car_year}\n"
                        f"🛣️ *Пробег:* {car_mileage} км\n"
                        f"⚙️ *Коробка:* {car_transmission}\n"
                        f"⛽ *Тип:* {car_type}\n"
                        f"🔍 *VIN:* `{car_info.get('car_vin', 'N/A')}`\n\n"
                        f"{desired_price_text}"
                        f"🛡️ *Страховка:* {car_insurance}\n"
                        f"🛰️ *Готов к GPS/Камере:* {gps_camera_ready}\n\n"
                        f"Заявка ожидает рассмотрения.")
        

        keyboard = {
            'inline_keyboard': [
                [
                    {'text': '✅ Одобрить', 'callback_data': f'user_approve_{chat_id}'},
                    {'text': '❌ Отклонить', 'callback_data': f'user_reject_{chat_id}'}
                ],
                [
                    {'text': get_text('admin_btn_report_error', chat_id), 'callback_data': f'user_error_{chat_id}'}
                ]
            ]
        }

        for admin_id in admins:
            send_message(admin_id, message_text)
            time.sleep(0.5)

            car_photo_ids = car_info.get('car_photo_ids', [])
            if car_photo_ids:
                send_media_group_mixed(admin_id, car_photo_ids, caption=f"📸 Фото авто (Собственник ID: {chat_id})")
                time.sleep(1)

            if owner_profile.get('tech_passport_photo_id'):
                send_photo(admin_id, owner_profile['tech_passport_photo_id'], caption=f"🚗 Тех. паспорт, лицевая сторона (ID: {chat_id})")
                time.sleep(0.5)
            
            if owner_profile.get('tech_passport_back_photo_id'):
                send_photo(admin_id, owner_profile['tech_passport_back_photo_id'], caption=f"🚗 Тех. паспорт, обратная сторона (ID: {chat_id})")
                time.sleep(0.5)

            if owner_profile.get('owner_passport_photo_id'):
                send_photo(admin_id, owner_profile['owner_passport_photo_id'], caption=f"👤 Паспорт собственника, лицевая сторона (ID: {chat_id})")
                time.sleep(0.5)
            
            if owner_profile.get('owner_passport_back_photo_id'):
                send_photo(admin_id, owner_profile['owner_passport_back_photo_id'], caption=f"👤 Паспорт собственника, обратная сторона (ID: {chat_id})")
                time.sleep(0.5)

            send_message(admin_id, f"Принять решение по собственнику {registrator_name} (ID: `{chat_id}`)?", reply_markup=keyboard)


    def _notify_admins_new_rental(self, rental_id, rental_data):
        admins = get_admins()
        if not admins: return

        users = load_from_json(USERS_FILE)
        user_info = users.get(rental_data['chat_id'], {})
        if not user_info:
            print(f"Критическая ошибка: не найден пользователь {rental_data['chat_id']} для заявки {rental_id}")
            return

        username_str = f" (@{user_info.get('username')})" if user_info.get('username') else ""
    
        main_caption = (f"🆕 *Новая заявка на аренду №{rental_id}*\n\n"
                        f"👤 *Клиент:* {rental_data.get('client_name', 'Не указан')}{username_str}\n"
                        f"📞 *Телефон:* `{rental_data.get('client_phone', 'Не указан')}`\n"
                        f"💳 *Вод. удостоверение:* `{rental_data.get('client_license', 'Не указан')}`\n\n"
                        f"🚗 *Авто:* {rental_data.get('car', 'N/A')}\n"
                        f"⏳ *Период:* {rental_data.get('period', 'N/A')}\n" 
                        f"💰 *Цена:* {rental_data.get('price', 'N/A')}\n\n"
                        f"*(Дата: {datetime.fromisoformat(rental_data['timestamp']).strftime('%d.%m.%Y %H:%M')})*")

        car_info = get_available_cars().get(rental_data.get('car', ''), {})
        car_photo_paths = car_info.get('local_image_paths', [])

        client_profile = user_info.get('client_profile', {})
        document_photo_ids = []
        if client_profile.get('passport_photo_id'):
            document_photo_ids.append(client_profile['passport_photo_id'])
        if client_profile.get('passport_back_photo_id'):
            document_photo_ids.append(client_profile['passport_back_photo_id'])
        if client_profile.get('license_front_photo_id'):
            document_photo_ids.append(client_profile['license_front_photo_id'])
        if client_profile.get('license_back_photo_id'):
            document_photo_ids.append(client_profile['license_back_photo_id'])
        
        keyboard = {
            'inline_keyboard': [[
                {'text': '✅ Одобрить', 'callback_data': f'rental_approve_{rental_id}'},
                {'text': '❌ Отклонить', 'callback_data': f'rental_reject_{rental_id}'}
            ]]
        }

        for admin_id in admins:
            if car_photo_paths:
                send_media_group_mixed(admin_id, car_photo_paths, caption=main_caption)
            else:
                send_message(admin_id, main_caption)
        
            time.sleep(0.5)

            if document_photo_ids:
                user_id_for_caption = rental_data['chat_id']
                docs_caption = f"📄 Документы клиента (ID: {user_id_for_caption})"
                send_media_group_mixed(admin_id, document_photo_ids, caption=docs_caption)
        
            time.sleep(0.5)

            send_message(admin_id, f"Принять решение по заявке №{rental_id}?", reply_markup=keyboard)

    def _handle_commands(self, chat_id, text, user):
        # Сначала создаем карту соответствия текста кнопок и команд для языка пользователя
        command_map = {
            get_text('menu_rent', chat_id): '/rent',
            get_text('menu_list_cars', chat_id): '/listcars',
            get_text('menu_my_rentals', chat_id): '/myrentals',
            get_text('menu_profile', chat_id): '/profile',
            get_text('menu_contact', chat_id): '/contact',
            get_text('menu_help', chat_id): '/help',
            get_text('menu_register_client', chat_id): '/register',
            get_text('menu_register_owner', chat_id): '/owner_register',
            get_text('menu_chat_manager', chat_id): '/chat',
            get_text('end_chat_button', chat_id): '/endchat',
            get_text('menu_language', chat_id): '/language' # <--- ДОБАВЛЕНА ЭТА СТРОКА
        }

        # Если текст от пользователя - это одна из кнопок, получаем соответствующую команду
        command_text = command_map.get(text, text)

        parts = command_text.split(maxsplit=1)
        command = parts[0]
        args = parts[1] if len(parts) > 1 else ""

        is_admin = chat_id in get_admins()
        is_staff = self._is_staff(chat_id)
        users = load_from_json(USERS_FILE)
        user_info = users.get(chat_id, {})
        user_role = user_info.get('role')

        if self.user_states.get(chat_id) == State.ADMIN_IN_CHAT_WITH_USER:
            target_info = self.user_data.get(chat_id, {})
            target_user_name = target_info.get('target_user_name', '')
            end_chat_btn_text = get_text('admin_end_chat_button', chat_id, user_name=target_user_name)
            if text == end_chat_btn_text:
                command = '/endchat'

        if command == '/start':
            self.user_states.pop(chat_id, None)
            self.user_data.pop(chat_id, None)
            user_name = user.get('first_name') or user.get('username') or get_text('guest', chat_id)

            if is_admin:
                send_message(chat_id, get_text('welcome', chat_id, name=user_name), 
                               reply_markup=get_main_menu_keyboard(chat_id))
                send_message(chat_id, "👑 *Вы вошли как администратор.* Используйте /adminhelp для списка команд.")
                return

            if 'lang' not in user_info:
                keyboard = {
                    'inline_keyboard': [
                        [
                            {'text': 'Русский 🇷🇺', 'callback_data': 'setlang_ru'},
                            {'text': 'Тоҷикӣ 🇹🇯', 'callback_data': 'setlang_tj'}
                        ]
                    ]
                }
                send_message(chat_id, get_text('choose_language', chat_id), reply_markup=keyboard)
            
            elif user_info.get('role') in ['client', 'owner']:
                 send_message(chat_id, get_text('welcome', chat_id, name=user_name), 
                               reply_markup=get_main_menu_keyboard(chat_id))
            
            else:
                role_selection_keyboard = create_keyboard([
                    [
                        {'text': get_text('start_btn_client', chat_id), 'callback_data': 'start_as_client'},
                        {'text': get_text('start_btn_owner', chat_id), 'callback_data': 'start_as_owner'}
                    ],
                    [
                        {'text': get_text('go_to_main_menu', chat_id), 'callback_data': 'show_main_menu'}
                    ]
                ], inline=True)

                welcome_text = get_text('welcome', chat_id, name=user_name)
                prompt_text = get_text('choose_your_role', chat_id)
                
                send_message(chat_id, f"{welcome_text}\n\n{prompt_text}", reply_markup=role_selection_keyboard)
                
        elif command == '/chat':
            self.user_states[chat_id] = State.IN_CHAT_WITH_MANAGER
            chat_keyboard = create_keyboard([[get_text('end_chat_button', chat_id)]], one_time=False)
            send_message(chat_id, get_text('enter_chat_mode', chat_id), reply_markup=chat_keyboard)
            return

        elif command == '/endchat':
            current_state = self.user_states.get(chat_id)
            
            if current_state == State.IN_CHAT_WITH_MANAGER:
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                send_message(chat_id, get_text('exit_chat_mode', chat_id), reply_markup=get_main_menu_keyboard(chat_id))
            
            elif current_state == State.ADMIN_IN_CHAT_WITH_USER:
                target_info = self.user_data.get(chat_id, {})
                target_user_name = target_info.get('target_user_name', '')
                
                self.user_states.pop(chat_id, None)
                self.user_data.pop(chat_id, None)
                send_message(chat_id, get_text('admin_exit_chat_session', chat_id, user_name=target_user_name), reply_markup=get_main_menu_keyboard(chat_id))
            
            return
        
        elif command == '/help':
            help_text_parts = [
                get_text('help_intro', chat_id),
                get_text('register_cmd', chat_id),
                get_text('owner_register_cmd', chat_id),
                "\n" + get_text('rent_cmd', chat_id),
                get_text('listcars_cmd', chat_id),
                get_text('myrentals_cmd', chat_id),
                get_text('profile_cmd', chat_id),
                get_text('edit_profile_cmd', chat_id),
                get_text('message_cmd', chat_id),
                get_text('contact_cmd', chat_id),
                get_text('faq_cmd', chat_id),
                get_text('lang_cmd', chat_id)
            ]
            
            if 'owner' in user_info.get('active_roles', []):
                help_text_parts.extend([
                    get_text('owner_help_intro', chat_id),
                    get_text('owner_payouts_cmd', chat_id),
                    get_text('owner_drivers_cmd', chat_id),
                    get_text('owner_service_cmd', chat_id),
                    get_text('owner_calendar_cmd', chat_id),
                    get_text('owner_contract_cmd', chat_id)
                ])

            if is_admin:
                help_text_parts.append(get_text('admin_help_cmd', chat_id))
            elif is_staff:
                staff_role = get_staff().get(str(chat_id), {}).get('role')
                if staff_role == 'manager':
                    help_text_parts.append(get_text('staff_help_manager', chat_id))
                elif staff_role == 'mechanic':
                    help_text_parts.append(get_text('staff_help_mechanic', chat_id))
                elif staff_role == 'accountant':
                    help_text_parts.append(get_text('staff_help_accountant', chat_id))
                
            send_message(chat_id, "\n".join(help_text_parts))

        elif command == '/register':
            user_info = users.get(chat_id, {})

            if 'client' in user_info.get('active_roles', []):
                send_message(chat_id, get_text('already_registered_as_driver', chat_id))
                return
            
            if user_info.get('status') == 'pending':
                send_message(chat_id, get_text('registration_pending', chat_id))
                return
                
            self.user_data[chat_id] = {
                'username': user.get('username'),
                'first_name': user.get('first_name'),
                'last_name': user.get('last_name'),
                'state_history': []
            }
            self._set_next_state(chat_id, State.AWAITING_NAME)
            send_message(chat_id, get_text('start_registration', chat_id), reply_markup=get_cancel_keyboard(chat_id))

        elif command == '/owner_register':
            user_info = users.get(chat_id, {})

            if 'owner' in user_info.get('active_roles', []):
                send_message(chat_id, "✅ Вы уже зарегистрированы как Собственник.")
                return
            
            if user_info.get('status') == 'pending':
                send_message(chat_id, get_text('registration_pending', chat_id))
                return
                
            self.user_data[chat_id] = {
                'username': user.get('username'),
                'state_history': []
            }
            self._set_next_state(chat_id, State.AWAITING_OWNER_REGISTRATOR_NAME)
            send_message(chat_id, get_text('owner_start_registration', chat_id), reply_markup=get_cancel_keyboard(chat_id))
        
        elif command == '/payouts':
            if 'owner' not in user_info.get('active_roles', []):
                send_message(chat_id, get_text('must_be_owner', chat_id))
                return
            send_message(chat_id, get_text('payouts_info', chat_id))
        
        elif command == '/drivers':
            if 'owner' not in user_info.get('active_roles', []):
                send_message(chat_id, get_text('must_be_owner', chat_id))
                return
                
            owner_car = load_from_json(OWNER_CARS_FILE).get(chat_id, {})
            if not owner_car:
                send_message(chat_id, "❗️Информация о вашем авто не найдена.")
                return
                
            if int(chat_id) % 2 == 0:
                send_message(chat_id, get_text('drivers_info_assigned', chat_id, 
                                             car_brand=owner_car.get('car_brand'), 
                                             car_plate=owner_car.get('car_plate')))
            else:
                send_message(chat_id, get_text('drivers_info_free', chat_id, 
                                             car_brand=owner_car.get('car_brand'), 
                                             car_plate=owner_car.get('car_plate')))

        elif command == '/service':
            if 'owner' not in user_info.get('active_roles', []):
                send_message(chat_id, get_text('must_be_owner', chat_id))
                return
                
            self.user_states[chat_id] = State.AWAITING_SERVICE_REQUEST
            send_message(chat_id, get_text('service_request_prompt', chat_id), reply_markup={'remove_keyboard': True})

        elif command == '/calendar':
            if 'owner' not in user_info.get('active_roles', []):
                send_message(chat_id, get_text('must_be_owner', chat_id))
                return
            send_message(chat_id, get_text('calendar_info', chat_id))

        elif command == '/contract':
            if 'owner' not in user_info.get('active_roles', []):
                send_message(chat_id, get_text('must_be_owner', chat_id))
                return
            send_message(chat_id, get_text('contract_info', chat_id))

        elif command == '/editprofile':
            users = load_from_json(USERS_FILE)
            user_info = users.get(chat_id)

            if not user_info:
                send_message(chat_id, get_text('not_registered', chat_id))
                return

            active_roles = user_info.get('active_roles', [])
            
            # Если у пользователя обе роли, даем ему выбор
            if 'client' in active_roles and 'owner' in active_roles:
                edit_keyboard = create_keyboard([
                    [{'text': get_text('edit_as_client_btn', chat_id), 'callback_data': 'profile_edit_as_client'}],
                    [{'text': get_text('edit_as_owner_btn', chat_id), 'callback_data': 'profile_edit_as_owner'}]
                ], inline=True)
                # --- НАЧАЛО ИЗМЕНЕНИЙ ---
                response = send_message(chat_id, get_text('choose_field_to_edit', chat_id), reply_markup=edit_keyboard)
                if response and response.get('ok'):
                    # Сохраняем ID сообщения с кнопками
                    if chat_id not in self.user_data:
                        self.user_data[chat_id] = {}
                    self.user_data[chat_id]['edit_menu_message_id'] = response['result']['message_id']
                # --- КОНЕЦ ИЗМЕНЕНИЙ ---
            
            # Если только роль клиента, сразу открываем редактирование профиля клиента
            elif 'client' in active_roles:
                self._start_profile_edit(chat_id, 'client')
                
            # Если только роль собственника, сразу открываем редактирование профиля собственника
            elif 'owner' in active_roles:
                self._start_profile_edit(chat_id, 'owner')
                
            # Если ролей нет, сообщаем, что нужно зарегистрироваться
            else:
                send_message(chat_id, get_text('not_registered', chat_id))

        elif command == '/rent':
            if chat_id not in users:
                send_message(chat_id, get_text('must_register_first', chat_id))
                return

            status = users[chat_id].get('status')
            
            if status == 'pending':
                send_message(chat_id, get_text('cant_rent_pending', chat_id))
                return
            
            if status == 'blocked':
                send_message(chat_id, get_text('you_are_blocked', chat_id))
                return

            available_cars = get_available_cars()
            if not available_cars:
                send_message(chat_id, get_text('no_cars_available', chat_id))
                return
            
            user_info_for_rent = users.get(chat_id, {})
            self.user_data[chat_id] = {
                'chat_id': chat_id, 
                'user_name': self._get_user_display_name(user_info_for_rent), 
                'username': user.get('username')
            }

            search_button = {
                'text': get_text('menu_search', chat_id),
                'switch_inline_query_current_chat': '' 
            }
            
            keyboard = {
                'inline_keyboard': [
                    [search_button]
                ]
            }
            
            send_message(
                chat_id, 
                get_text('choose_car_for_rent', chat_id) + "\n\n" + "Нажмите на кнопку ниже, чтобы начать поиск.",
                reply_markup=keyboard
            )
        
        elif command == '/listcars':
            available_cars = get_available_cars()
            if not available_cars:
                send_message(chat_id, get_text('no_cars_available', chat_id))
                return

            brands = sorted(list(set([name.split(' ')[0] for name in available_cars.keys()])))
            
            if not brands:
                send_message(chat_id, get_text('no_cars_available', chat_id))
                return

            buttons = [
                {'text': brand, 'callback_data': f'listcars_brand_{brand}'}
                for brand in brands
            ]

            keyboard = create_keyboard([buttons[i:i + 3] for i in range(0, len(buttons), 3)], inline=True)

            send_message(chat_id, "🗂️ *Пожалуйста, выберите марку автомобиля, чтобы посмотреть доступные модели:*", reply_markup=keyboard)
            
        elif command == '/profile':
            if chat_id in users:
                u = users[chat_id]
                profile_text_parts = [get_text('profile_title', chat_id)]
                
                active_roles = u.get('active_roles', [])
                
                if 'client' in active_roles and 'client_profile' in u:
                    cp = u['client_profile']
                    profile_text_parts.append("\n*--- Профиль Водителя ---*")
                    client_details = [
                        get_text('profile_name', chat_id, name=cp.get('name', 'N/A')),
                        get_text('profile_phone', chat_id, phone=cp.get('phone', 'N/A')),
                        get_text('profile_city', chat_id, city=cp.get('city', 'N/A')),
                        get_text('profile_address', chat_id, address=cp.get('address', 'N/A')),
                        get_text('profile_age', chat_id, dob=cp.get('dob', 'N/A')),
                        get_text('profile_driving_experience', chat_id, experience=cp.get('driving_experience', 'N/A')),
                        get_text('profile_license', chat_id, license=cp.get('license', 'N/A'))
                    ]
                    profile_text_parts.extend(client_details)

                if 'owner' in active_roles and 'owner_profile' in u:
                    op = u['owner_profile']
                    car_info = load_from_json(OWNER_CARS_FILE).get(chat_id, {})
                    profile_text_parts.append("\n*--- Профиль Собственника ---*")
                    
                    owner_details = []

                    if op.get('registrator_name'):
                        owner_details.append(f"▪️ *{get_text('field_registrator_name', chat_id)}:* {op['registrator_name']}")
                    
                    if op.get('owner_name'):
                        owner_details.append(f"▪️ *{get_text('field_owner_name', chat_id)}:* {op['owner_name']}")

                    owner_details.extend([
                        get_text('profile_phone', chat_id, phone=op.get('phone', 'N/A')),
                        f"▪️ *Автомобиль:* {car_info.get('car_brand', 'N/A')}",
                        f"▪️ *Гос. номер:* `{car_info.get('car_plate', 'N/A')}`"
                    ])

                    profile_text_parts.extend(owner_details)

                profile_text_parts.append(f"\n*Статус аккаунта:* {u.get('status', 'N/A')}")
                
                edit_keyboard = create_keyboard([
                    [{'text': get_text('edit_as_client_btn', chat_id), 'callback_data': 'profile_edit_as_client'}],
                    [{'text': get_text('edit_as_owner_btn', chat_id), 'callback_data': 'profile_edit_as_owner'}]
                ], inline=True)

                send_message(chat_id, "\n".join(profile_text_parts), reply_markup=edit_keyboard)

            else:
                send_message(chat_id, get_text('not_registered', chat_id))  
        
        elif command == '/myrentals':
            rentals = load_from_json(RENTALS_FILE)
            user_rentals = [
                get_text('rental_list_item', chat_id, 
                         id=rid, car=r['car'], status=r['status'], 
                         date=datetime.fromisoformat(r['timestamp']).strftime('%d.%m.%Y'))
                for rid, r in rentals.items() 
                if r.get('chat_id') == chat_id
            ]
            
            send_message(chat_id, f"{get_text('your_rentals_title', chat_id)}\n\n" + 
                       ("\n".join(user_rentals) if user_rentals else get_text('no_rentals_yet', chat_id)))
        
        elif command == '/message':
            if not args:
                send_message(chat_id, get_text('message_to_admin_usage', chat_id))
                return
                
            admins = get_admins()
            if not admins:
                send_message(chat_id, get_text('no_admins_online', chat_id))
                return
                
            user_name = users.get(chat_id, {}).get('name', user.get('first_name', 'Пользователь'))
            user_phone = users.get(chat_id, {}).get('phone', 'N/A')
            username_mention = f" (@{user['username']})" if user.get('username') else ""
            
            admin_message = (f"📩 *Новое сообщение от {user_name}{username_mention}*\n"
                            f"*ID:* `{chat_id}`\n*Тел:* `{user_phone}`\n\n"
                            f"*Текст:*\n{args}")
                            
            for admin_id in admins:
                send_message(admin_id, admin_message)
                
            send_message(chat_id, get_text('message_sent_to_admin', chat_id))
        
        elif command == '/contact':
            send_message(chat_id, f"{get_text('contact_info_title', chat_id)}\n\n{get_text('contact_info_details', chat_id)}", disable_web_page_preview=True)
            
            send_request('sendLocation', {
                'chat_id': chat_id,
                'latitude': 40.264263,
                'longitude': 69.655816
            })
        
        elif command == '/faq':
            send_message(chat_id, f"{get_text('faq_title', chat_id)}\n\n{get_text('faq_content', chat_id)}")
        
        elif command == '/language':
            keyboard = {
                'inline_keyboard': [
                    [
                        {'text': 'Русский 🇷🇺', 'callback_data': 'setlang_ru'},
                        {'text': 'Тоҷикӣ 🇹🇯', 'callback_data': 'setlang_tj'}
                    ]
                ]
            }
            send_message(chat_id, get_text('choose_language', chat_id), reply_markup=keyboard)

        elif command == '/selectcar':
            car_name = args
            # Загружаем машины один раз в начале
            available_cars = get_available_cars()

            if car_name in available_cars:
                if chat_id not in users or users[chat_id].get('status') != 'active':
                    send_message(chat_id, get_text('must_register_first', chat_id))
                    return

                # Сообщаем пользователю, что начали работу
                send_message(chat_id, "⏳ Загружаю информацию об автомобиле...")

                self.user_data[chat_id] = {
                    'chat_id': chat_id, 'user_name': self._get_user_display_name(user_info),
                    'username': user.get('username'), 'car': car_name
                }
                car_info = available_cars[car_name]
                full_caption = (
                    f"📜 *Описание:* {car_info.get('description', 'N/A')}\n\n"
                    f"*{get_text('car_details_price', chat_id)}*\n"
                    f"{get_text('price_day', chat_id, price=car_info['prices'][0])}\n"
                    f"{get_text('price_week', chat_id, price=car_info['prices'][1])}\n"
                    f"{get_text('price_month', chat_id, price=car_info['prices'][2])}\n\n"
                    f"*{get_text('car_specs', chat_id)}*\n"
                    f"{get_text('car_year', chat_id, year=car_info.get('year', 'N/A'))}\n"
                    f"{get_text('car_fuel', chat_id, fuel_type=car_info.get('fuel_type', 'N/A'))}\n"
                    f"{get_text('car_engine', chat_id, engine=car_info.get('engine', 'N/A'))}\n"
                    f"{get_text('car_transmission', chat_id, transmission=car_info.get('transmission', 'N/A'))}\n"
                    f"{get_text('car_seats', chat_id, seats=car_info.get('seats', 'N/A'))}"
                )
                
                # --- ОСНОВНАЯ ЛОГИКА КЭШИРОВАНИЯ ---
                
                # 1. Проверяем, есть ли уже сохраненные file_id
                existing_file_ids = car_info.get('telegram_file_ids', [])
                
                # 2. Выбираем, что отправлять: file_id (быстро) или пути к файлам (медленно)
                media_to_send = existing_file_ids or car_info.get('local_image_paths', [])
                
                if not media_to_send:
                    # Если нет ни фото, ни file_id, отправляем только текст
                    send_message(chat_id, full_caption)
                else:
                    # 3. Отправляем медиа и получаем в ответ новые file_id
                    new_file_ids = send_media_group_mixed(chat_id, media_to_send, caption=full_caption)
                    
                    # 4. Если мы отправляли файлы (а не id) и получили новые id, СОХРАНЯЕМ их
                    if not existing_file_ids and new_file_ids:
                        print(f"Сохраняем новые file_id для '{car_name}': {new_file_ids}")
                        available_cars[car_name]['telegram_file_ids'] = new_file_ids
                        save_to_json(available_cars, CARS_FILE)

                # --- КОНЕЦ ЛОГИКИ КЭШИРОВАНИЯ ---
                
                time.sleep(0.3)
                self.user_states[chat_id] = State.AWAITING_RENT_TYPE
                rent_type_keyboard = create_keyboard([
                    [{'text': get_text('short_term', chat_id), 'callback_data': 'rent_type_short'},
                     {'text': get_text('long_term', chat_id), 'callback_data': 'rent_type_long'}]
                ], inline=True)
                send_message(chat_id, get_text('choose_rent_type', chat_id), reply_markup=rent_type_keyboard)
            
            else:
                send_message(chat_id, "❗️ Извините, информация об этой машине не найдена.")
        
        elif is_admin or is_staff:
            self._handle_admin_commands(chat_id, command, args, user)
        
        elif command.startswith('/'):
            send_message(chat_id, get_text('unknown_command', chat_id))

    def _handle_admin_commands(self, chat_id, command, args, user):
        main_admin_id = load_from_json(ADMIN_FILE).get('main_admin')
        is_admin = chat_id in get_admins()
        is_staff = self._is_staff(chat_id)
        staff_role = get_staff().get(str(chat_id), {}).get('role') if is_staff else None
        users = load_from_json(USERS_FILE)
        
        if command == '/adminhelp' and is_admin:
            help_text = ("👑 *Команды администратора*\n\n"
                         "*/listusers* - Показать всех пользователей.\n"
                         "*/userinfo <ID|@user>* - Полная инфо о пользователе.\n"
                         "*/block <ID|@user>* - Заблокировать пользователя.\n"
                         "*/unblock <ID|@user>* - Разблокировать.\n"
                         "*/deleteuser <ID|@user>* - Удалить пользователя.\n"
                         "*/broadcast <текст>* - Сообщение всем активным.\n"
                         "*/reply <ID|@user> <текст>* - Ответить пользователю.\n"
                         "*/listallrentals* - Показать все заявки на аренду.\n\n"
                         "*Управление автомобилями для аренды:*\n"
                         "*/addcar* | */editcar* | */deletecar*\n\n"
                         "*Управление персоналом:*\n"
                         "*/addstaff <ID> <role>* - Добавить сотрудника (manager, mechanic, accountant)\n"
                         "*/removestaff <ID>* - Удалить сотрудника\n"
                         "*/liststaff* - Список сотрудников\n\n"
                         "*Уведомления и выплаты:*\n"
                         "*/payout <ID> <сумма>* - Уведомить собственника о выплате.\n"
                         "*/servicedone <ID>* - Уведомить собственника о завершении ТО.")
                         
            if str(chat_id) == main_admin_id:
                help_text += "\n\n*Главный админ:*\n*/listadmins* | */addadmin <ID>* | */removeadmin <ID>*"
                
            send_message(chat_id, help_text)
            
        elif command == '/listusers' and (is_admin or (is_staff and staff_role == 'manager')):
            users = load_from_json(USERS_FILE)
            if not users:
                send_message(chat_id, "Пользователи не найдены.")
                return
                
            users_list = ["*ID* | *Роль* | *Имя* | *Статус* | *Username* | *Язык*"]
            role_map = {
                'client': 'Client',
                'owner': 'Owner',
                'admin': 'Admin'
            }
            
            for uid, udata in users.items():
                username = f"@{udata.get('username')}" if udata.get('username') else "N/A"
                lang = udata.get('lang', 'N/A').upper()
                role = role_map.get(udata.get('role', 'client'), 'N/A')
                users_list.append(f"`{uid}` | {role} | {udata.get('name', 'N/A')} | {udata.get('status', 'N/A')} | {username} | {lang}")
                
            message_parts = []
            current_part = ""
            
            for line in users_list:
                if len(current_part) + len(line) + 1 > 4096:
                    message_parts.append(current_part)
                    current_part = ""
                current_part += line + "\n"
                
            if current_part:
                message_parts.append(current_part)
                
            for part in message_parts:
                if part.strip():
                    send_message(chat_id, part)
                    time.sleep(0.3)
                    
        elif command in ['/userinfo', '/block', '/unblock', '/deleteuser'] and (is_admin or (is_staff and staff_role == 'manager')):
            if not args:
                send_message(chat_id, f"Использование: `{command} <ID или @username>`")
                return
                
            user_id, u_data = find_user_by_identifier(args)
            if not u_data:
                send_message(chat_id, get_text('admin_user_not_found', chat_id, user=args))
                return
                
            if command == '/userinfo':
                roles = u_data.get('active_roles', [])
                roles_text = ", ".join([r.capitalize() for r in roles]) if roles else "Не определена"

                info_text = (f"👤 *Профиль пользователя {user_id}*\n\n"
                             f"▪️ *Username:* @{u_data.get('username', 'N/A')}\n"
                             f"▪️ *Роли:* {roles_text}\n"
                             f"▪️ *Статус:* {u_data.get('status', 'N/A')}\n"
                             f"▪️ *Язык:* {u_data.get('lang', 'N/A').upper()}\n")

                if 'client_profile' in u_data:
                    cp = u_data['client_profile']
                    info_text += (f"\n*--- Профиль Водителя ---*\n"
                                  f"▪️ *ФИО:* {cp.get('name', 'N/A')}\n"
                                  f"▪️ *Телефон:* `{cp.get('phone', 'N/A')}`\n"
                                  f"▪️ *Город:* {cp.get('city', 'N/A')}\n"
                                  f"▪️ *Адрес:* {cp.get('address', 'N/A')}\n"
                                  f"▪️ *Дата рождения:* {cp.get('dob', 'N/A')}\n"
                                  f"▪️ *Стаж:* {cp.get('driving_experience', 'N/A')} лет\n"
                                  f"▪️ *В/У:* `{cp.get('license', 'N/A')}`\n"
                                  f"▪️ *Категория:* {cp.get('license_category', 'N/A')}\n"
                                  f"▪️ *Предп. авто:* {cp.get('preferred_car_type', 'N/A')}\n"
                                  f"▪️ *Опыт в такси:* {cp.get('taxi_experience', 'N/A')}\n"
                                  f"▪️ *ID Водителя:* {cp.get('driver_id', 'N/A')}\n"
                                  f"▪️ *Залог:* {cp.get('deposit', 'N/A')}\n")

                if 'owner_profile' in u_data:
                    op = u_data['owner_profile']
                    owner_car_data = load_from_json(OWNER_CARS_FILE).get(user_id, {})
                    info_text += (f"\n*--- Профиль Собственника ---*\n"
                                  f"▪️ *ФИО Регистратора:* {op.get('registrator_name', 'N/A')}\n"
                                  f"▪️ *ФИО Собственника:* {op.get('owner_name', 'N/A')}\n"
                                  f"▪️ *Телефон:* `{op.get('phone', 'N/A')}`\n")
                    if owner_car_data:
                        info_text += (f"▪️ *Авто:* {owner_car_data.get('car_brand', 'N/A')}\n"
                                      f"▪️ *Гос. номер:* `{owner_car_data.get('car_plate', 'N/A')}`")

                send_message(chat_id, info_text)

            elif command == '/deleteuser':
                if user_id == str(MAIN_ADMIN_CHAT_ID):
                    send_message(chat_id, "❗️ Нельзя удалить главного администратора.")
                    return
                    
                users = load_from_json(USERS_FILE)
                if user_id in users:
                    if users[user_id].get('role') == 'owner':
                        owner_cars = load_from_json(OWNER_CARS_FILE)
                        if user_id in owner_cars:
                            del owner_cars[user_id]
                            save_to_json(owner_cars, OWNER_CARS_FILE)
                            
                    del users[user_id]
                    save_to_json(users, USERS_FILE)
                    send_message(chat_id, get_text('admin_user_deleted', chat_id, name=u_data.get('name'), id=user_id))
                    send_message(user_id, get_text('admin_user_profile_deleted_notification', user_id))
                    
            else:
                new_status = 'blocked' if command == '/block' else 'active'
                users = load_from_json(USERS_FILE)
                users[user_id]['status'] = new_status
                save_to_json(users, USERS_FILE)
                
                send_message(chat_id, get_text('admin_status_changed_notification', chat_id, 
                                             name=u_data.get('name'), id=user_id, status=new_status))
                send_message(user_id, get_text('admin_user_status_changed_notification', user_id, status=new_status))
                
        elif command == '/broadcast' and (is_admin or (is_staff and staff_role == 'manager')):
            if not args:
                send_message(chat_id, "Использование: `/broadcast <текст>`")
                return
                
            users = load_from_json(USERS_FILE)
            active_users_count = sum(1 for u in users.values() if u.get('status') == 'active')
            
            if active_users_count == 0:
                send_message(chat_id, "Активных пользователей для рассылки нет.")
                return
                
            self.user_states[chat_id] = State.AWAITING_BROADCAST_CONFIRMATION
            self.user_data[chat_id] = {'broadcast_message': args}
            
            send_message(chat_id, get_text('admin_broadcast_confirm', chat_id, 
                                         count=active_users_count, text=args))
                                         
        elif command == '/reply' and (is_admin or is_staff):
            try:
                target_identifier, reply_text = args.split(' ', 1)
            except ValueError:
                send_message(chat_id, "Использование: `/reply <ID|@user> <текст>`")
                return
                
            target_id, target_data = find_user_by_identifier(target_identifier)
            if not target_id:
                send_message(chat_id, get_text('admin_user_not_found', chat_id, user=target_identifier))
                return
                
            user_lang_text = get_text('admin_reply_prefix', target_id, text=reply_text)
            
            if send_message(target_id, user_lang_text):
                send_message(chat_id, get_text('admin_reply_sent', chat_id, 
                                             name=target_data.get('name', 'пользователю'), id=target_id))
            else:
                send_message(chat_id, get_text('admin_reply_failed', chat_id))
                
        elif command == '/listallrentals' and (is_admin or (is_staff and staff_role == 'manager')):
            rentals = load_from_json(RENTALS_FILE)
            if not rentals:
                send_message(chat_id, "Заявок на аренду пока нет.")
                return
                
            rental_list = ["*Все заявки на аренду:*\n`ID|Статус|Клиент|Телефон|Машина|Дата`\n"]
            sorted_rentals = sorted(rentals.items(), key=lambda item: item[1]['timestamp'], reverse=True)
            
            for rid, r in sorted_rentals:
                user_info = users.get(r.get('chat_id'), {})
                username = f" (@{user_info.get('username')})" if user_info.get('username') else ""
                date_str = datetime.fromisoformat(r['timestamp']).strftime('%d.%m.%y')
                rental_list.append(f"`{rid}`|*{r.get('status')}*|{r.get('user_name')}{username}|`{user_info.get('phone', 'N/A')}`|{r.get('car')}|{date_str}")
                
            message_parts = []
            current_part = ""
            
            for line in rental_list:
                if len(current_part) + len(line) + 1 > 4096:
                    message_parts.append(current_part)
                    current_part = ""
                current_part += line + "\n"
                
            if current_part:
                message_parts.append(current_part)
                
            for part in message_parts:
                if part.strip():
                    send_message(chat_id, part)
                    time.sleep(0.3)
                    
        elif command == '/addcar' and (is_admin or (is_staff and staff_role == 'manager')):
            self.user_states[chat_id] = State.AWAITING_NEW_CAR_NAME
            self.user_data[chat_id] = {}
            send_message(chat_id, "🚗 Введите название нового автомобиля (например, Kia K5):")
            
        elif command == '/editcar' and (is_admin or (is_staff and staff_role == 'manager')):
            cars = get_available_cars()
            if not cars:
                send_message(chat_id, "❗️Нет автомобилей для редактирования.")
                return
                
            car_names = list(cars.keys())
            self.user_states[chat_id] = State.AWAITING_EDIT_CAR_SELECT
            self.user_data[chat_id] = {}
            
            send_message(chat_id, "📝 Выберите автомобиль для редактирования:", 
                       reply_markup=create_keyboard([car_names[i:i + 2] for i in range(0, len(car_names), 2)], one_time=True))
                       
        elif command == '/deletecar' and (is_admin or (is_staff and staff_role == 'manager')):
            cars = get_available_cars()
            if not cars:
                send_message(chat_id, "❗️Нет автомобилей для удаления.")
                return
                
            buttons = [
                [{'text': f'❌ {name}', 'callback_data': f'car_delete_{name.replace(" ", "%20")}'}]
                for name in cars.keys()
            ]
            
            send_message(chat_id, "🗑️ Нажмите на кнопку для удаления автомобиля:", 
                       reply_markup={'inline_keyboard': buttons})
        
        elif command == '/payout' and (is_admin or (is_staff and (staff_role == 'accountant' or staff_role == 'manager'))):
            try:
                owner_id, amount = args.split(' ', 1)
                if not owner_id.isdigit() or not amount.isdigit():
                    raise ValueError()
                amount = int(amount)
            except ValueError:
                send_message(chat_id, "Использование: `/payout <ID собственника> <сумма>`")
                return
                
            owner_cars = load_from_json(OWNER_CARS_FILE)
            if owner_id in owner_cars:
                car_info = owner_cars[owner_id]
                self.notifications.notify_owner_of_payout(owner_id, car_info, amount)
                send_message(chat_id, f"✅ Уведомление о выплате {amount} сомони отправлено собственнику {owner_id}.")
            else:
                send_message(chat_id, f"❗️Собственник с ID {owner_id} не найден или у него нет зарегистрированных авто.")

        elif command == '/servicedone' and (is_admin or (is_staff and (staff_role == 'mechanic' or staff_role == 'manager'))):
            owner_id = args
            if not owner_id.isdigit():
                send_message(chat_id, "Использование: `/servicedone <ID собственника>`")
                return

            owner_cars = load_from_json(OWNER_CARS_FILE)
            if owner_id in owner_cars:
                car_info = owner_cars[owner_id]
                self.notifications.notify_owner_of_maintenance_completion(owner_id, car_info)
                send_message(chat_id, f"✅ Уведомление о завершении ТО отправлено собственнику {owner_id}.")
            else:
                send_message(chat_id, f"❗️Собственник с ID {owner_id} не найден.")

        elif command == '/addstaff' and (is_admin or (is_staff and staff_role == 'manager')):
            try:
                staff_id, role = args.split(' ', 1)
                if role not in ['manager', 'mechanic', 'accountant']:
                    raise ValueError()
            except ValueError:
                send_message(chat_id, get_text('admin_add_staff_usage', chat_id))
                return
                
            staff = get_staff()
            user_id, user_data = find_user_by_identifier(staff_id)
            
            if not user_id:
                send_message(chat_id, get_text('admin_user_not_found', chat_id, user=staff_id))
                return
                
            if user_id in staff:
                send_message(chat_id, get_text('admin_staff_exists', chat_id, role=staff[user_id]['role']))
                return
                
            staff[user_id] = {'role': role, 'name': user_data.get('name', 'N/A')}
            save_to_json(staff, STAFF_FILE)
            
            send_message(chat_id, get_text('admin_staff_added', chat_id, name=user_data.get('name'), id=user_id, role=role))
            send_message(user_id, f"🎉 Вы были назначены на должность *{role}* в системе аренды автомобилей.")
            
        elif command == '/removestaff' and (is_admin or (is_staff and staff_role == 'manager')):
            if not args:
                send_message(chat_id, "Использование: `/removestaff <ID>`")
                return
                
            staff = get_staff()
            user_id, user_data = find_user_by_identifier(args)
            
            if not user_id or user_id not in staff:
                send_message(chat_id, get_text('admin_staff_not_found', chat_id))
                return
                
            del staff[user_id]
            save_to_json(staff, STAFF_FILE)
            
            send_message(chat_id, get_text('admin_staff_removed', chat_id, name=user_data.get('name'), id=user_id))
            send_message(user_id, "⚠️ Вы были удалены из списка сотрудников системы аренды автомобилей.")
            
        elif command == '/liststaff' and (is_admin or (is_staff and staff_role == 'manager')):
            staff = get_staff()
            if not staff:
                send_message(chat_id, "Сотрудников пока нет.")
                return
                
            staff_list = ["*Список сотрудников:*\n`ID` | *Роль* | *Имя*"]
            for staff_id, staff_data in staff.items():
                staff_list.append(f"`{staff_id}` | *{staff_data['role']}* | {staff_data.get('name', 'N/A')}")
                
            send_message(chat_id, "\n".join(staff_list))
            
        elif command == '/approverental' and (is_admin or (is_staff and staff_role == 'manager')):
            if not args:
                send_message(chat_id, "Использование: `/approverental <ID заявки>`")
                return
                
            rentals = load_from_json(RENTALS_FILE)
            if args not in rentals:
                send_message(chat_id, "❗️Заявка не найдена.")
                return
                
            rentals[args]['status'] = 'approved'
            save_to_json(rentals, RENTALS_FILE)
            
            user_id = rentals[args]['chat_id']
            send_message(chat_id, f"✅ Заявка {args} одобрена.")
            send_message(user_id, get_text('rental_status_changed', user_id, 
                                         id=args, car=rentals[args]['car'], 
                                         emoji='✅', status='Approved') + 
                        get_text('status_approved_details', user_id))
                        
            # Уведомляем собственника
            owner_cars = load_from_json(OWNER_CARS_FILE)
            for owner_id, car_data in owner_cars.items():
                if car_data.get('car_brand') == rentals[args]['car']:
                    driver_info = users.get(user_id, {})
                    self.notifications.notify_owner_of_driver_assignment(owner_id, driver_info, car_data)
                    break
            
        elif command == '/rejectrental' and (is_admin or (is_staff and staff_role == 'manager')):
            if not args:
                send_message(chat_id, "Использование: `/rejectrental <ID заявки>`")
                return
                
            rentals = load_from_json(RENTALS_FILE)
            if args not in rentals:
                send_message(chat_id, "❗️Заявка не найдена.")
                return
                
            rentals[args]['status'] = 'rejected'
            save_to_json(rentals, RENTALS_FILE)
            
            user_id = rentals[args]['chat_id']
            send_message(chat_id, f"❌ Заявка {args} отклонена.")
            send_message(user_id, get_text('rental_status_changed', user_id, 
                                         id=args, car=rentals[args]['car'], 
                                         emoji='❌', status='Rejected') + 
                        get_text('status_rejected_details', user_id))
                        
            # Уведомляем других водителей о свободной машине
            car_info = get_available_cars().get(rentals[args]['car'], {})
            if car_info:
                self.notifications.notify_all_drivers_about_free_car(car_info)
                
        elif command == '/listpending' and (is_admin or (is_staff and staff_role == 'manager')):
            rentals = load_from_json(RENTALS_FILE)
            pending_rentals = [r for r in rentals.values() if r.get('status') == 'pending']
            
            if not pending_rentals:
                send_message(chat_id, "Нет ожидающих заявок на аренду.")
                return
                
            rentals_list = ["*Ожидающие заявки на аренду:*\n`ID` | *Клиент* | *Машина* | *Дата*"]
            for rid, r in rentals.items():
                if r.get('status') == 'pending':
                    user_info = users.get(r.get('chat_id'), {})
                    username = f" (@{user_info.get('username')})" if user_info.get('username') else ""
                    date_str = datetime.fromisoformat(r['timestamp']).strftime('%d.%m.%y')
                    rentals_list.append(f"`{rid}` | {r.get('user_name')}{username} | {r.get('car')} | {date_str}")
                    
            send_message(chat_id, "\n".join(rentals_list))
            
        elif command == '/listmaintenance' and (is_admin or (is_staff and staff_role == 'mechanic')):
            owner_cars = load_from_json(OWNER_CARS_FILE)
            if not owner_cars:
                send_message(chat_id, "Нет зарегистрированных автомобилей.")
                return
                
            cars_list = ["*Список автомобилей для обслуживания:*\n`ID` | *Марка* | *Гос. номер* | *Пробег*"]
            for owner_id, car_data in owner_cars.items():
                owner_info = users.get(owner_id, {})
                cars_list.append(f"`{owner_id}` | {car_data.get('car_brand', 'N/A')} | `{car_data.get('car_plate', 'N/A')}` | {car_data.get('car_mileage', 'N/A')} км")
                
            send_message(chat_id, "\n".join(cars_list))
            
        elif command in ['/dailycheck', '/acceptfromowner', '/givetodriver'] and (is_admin or (is_staff and staff_role == 'mechanic')):
            cars = load_from_json(OWNER_CARS_FILE)
            if not cars:
                send_message(chat_id, "❗️ Нет автомобилей, зарегистрированных собственниками, для проведения проверок.")
                return

            checklist_type = command.replace('/', '') 

            self.user_data[chat_id] = {
                'checklist_type': checklist_type,
                'step': 0, 
                'answers': {}
            }
            self.user_states[chat_id] = State.AWAITING_CHECKLIST_INPUT

            buttons = [
                [{'text': f"{data.get('car_brand', 'N/A')} ({data.get('car_plate', 'N/A')})", 'callback_data': f"checklist_car_{data.get('car_plate')}"}]
                for plate, data in cars.items()
            ]
            keyboard = create_keyboard(buttons, inline=True)
            send_message(chat_id, f"️️️️️️✈️ *{get_text(f'mechanic_{checklist_type}_cmd', chat_id)}*\n\nПожалуйста, выберите автомобиль для начала:", reply_markup=keyboard)

        elif command == '/edithandover' and (is_admin or (is_staff and staff_role == 'mechanic')):
            send_message(chat_id, "⚙️ *Функция 'Изменение данных приемки/выдачи' в разработке.*\n\nЭта команда позволит механику скорректировать ранее введенные данные в чек-листах.")
            
        elif str(chat_id) == main_admin_id:
            if command == '/listadmins':
                admins = load_from_json(ADMIN_FILE)
                admin_list = [
                    f"- `{aid}` ({'Главный' if key == 'main_admin' else 'Админ'})"
                    for key, aid in admins.items()
                ]
                send_message(chat_id, "*Список администраторов:*\n" + "\n".join(admin_list))
                
            elif command == '/addadmin':
                if not args or not args.isdigit():
                    send_message(chat_id, "Использование: `/addadmin <ID>`")
                    return
                    
                admins = load_from_json(ADMIN_FILE)
                if args in admins.values():
                    send_message(chat_id, f"❗️ `{args}` уже является админом.")
                    return
                    
                admins[f'admin_{len(admins)}'] = args
                save_to_json(admins, ADMIN_FILE)
                send_message(chat_id, f"✅ Администратор `{args}` добавлен.")
                send_message(args, "🎉 Вы были назначены администратором бота!")
                
            elif command == '/removeadmin':
                if not args or not args.isdigit():
                    send_message(chat_id, "Использование: `/removeadmin <ID>`")
                    return
                    
                if args == main_admin_id:
                    send_message(chat_id, "❗️Нельзя удалить главного администратора.")
                    return
                    
                admins = load_from_json(ADMIN_FILE)
                key_to_remove = None
                
                for key, admin_id_val in admins.items():
                    if admin_id_val == args:
                        key_to_remove = key
                        break
                        
                if key_to_remove:
                    del admins[key_to_remove]
                    save_to_json(admins, ADMIN_FILE)
                    send_message(chat_id, f"✅ Администратор `{args}` удален.")
                    send_message(args, "❌ Вы были удалены из списка администраторов.")
                else:
                    send_message(chat_id, f"❗️ Администратор `{args}` не найден.")
                    
        else:
            send_message(chat_id, "🤷 Неизвестная команда.")

def set_bot_commands():
    """Устанавливает команды для кнопки 'Меню' в Telegram для разных языков."""
    
    commands_ru = [
        {'command': 'start', 'description': '🚀 Перезапустить бота'},
        {'command': 'rent', 'description': '🚙 Арендовать автомобиль'},
        {'command': 'listcars', 'description': '📋 Посмотреть список авто'},
        {'command': 'myrentals', 'description': '📄 Мои заявки на аренду'},
        {'command': 'profile', 'description': '👤 Мой профиль'},
        {'command': 'help', 'description': 'ℹ️ Помощь'},
        {'command': 'contact', 'description': '📞 Контакты'},
        {'command': 'language', 'description': '🌐 Сменить язык'},
    ]
    
    commands_tj = [
        {'command': 'start', 'description': '🚀 Бозсозии бот'},
        {'command': 'rent', 'description': '🚙 Иҷораи мошин'},
        {'command': 'listcars', 'description': '📋 Рӯйхати мошинҳо'},
        {'command': 'myrentals', 'description': '📄 Дархостҳои ман'},
        {'command': 'profile', 'description': '👤 Профили ман'},
        {'command': 'help', 'description': 'ℹ️ Ёрӣ'},
        {'command': 'contact', 'description': '📞 Тамос'},
        {'command': 'language', 'description': '🌐 Иваз кардани забон'},
    ]
    
    payloads = [
        {'commands': commands_ru, 'language_code': 'ru'},
        {'commands': commands_tj, 'language_code': 'tg'}
    ]

    for payload in payloads:
        send_request('setMyCommands', payload)
    
    default_payload = {'commands': commands_ru}
    send_request('setMyCommands', default_payload)
    print("Команды меню успешно установлены для всех языков.")


def main():
    bot = CarRentalBot()
    
    set_bot_commands()

    print("Бот запущен...")
    offset = 0
    last_scheduled_check = datetime.now() - timedelta(days=1)

    while True:
        try:
            # Ежедневная проверка по расписанию
            if datetime.now() - last_scheduled_check >= timedelta(hours=24):
                daily_scheduler_check()
                last_scheduled_check = datetime.now()

            response = send_request('getUpdates', {'offset': offset, 'timeout': 60})
            if response and response.get('ok'):
                for update in response['result']:
                    offset = update['update_id'] + 1
                    try:
                        bot.process_update(update)
                    except Exception as e:
                        print(f"--- ОШИБКА ОБРАБОТКИ UPDATE ---\nUpdate: {update}")
                        traceback.print_exc()
                        
        except KeyboardInterrupt:
            print("\nБот остановлен.")
            break
            
        except requests.exceptions.ReadTimeout:
            print("Таймаут сети, повторяем запрос...")
            time.sleep(1)
            continue
            
        except Exception as e:
            print(f"Критическая ошибка в главном цикле: {e}")
            traceback.print_exc()
            time.sleep(10)

if __name__ == '__main__':
    main() 